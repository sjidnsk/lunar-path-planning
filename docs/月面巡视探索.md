# 月面巡视探索

> 来源：[Notion 页面](https://www.notion.so/36b6bbebd15c814fb03add3de0511b20)
> 导出时间：2026-05-26 11:07:55 CST
> 同步方式：单向导出；Notion 为主源，本地文件为副本。

本页面汇总“月面环境复杂约束下无人平台自主智能探索与规划研究”的资料整理。

## 子页面

- 笔记：用于记录原始想法、概念拆解和研究问题。
- 报告：用于形成毕设研究背景、研究内容、技术路线和风险分析。
- 图谱：用于记录环境、平台、约束、可信度、探索、规划之间的关系。
- 文献补充版：基于公开学术文献补充研究脉络、关键方法和可引用依据。

## 主题

月面未知环境中，轮式无人平台需要在坡度、障碍、崎岖度、光照、平台能力、任务价值和地图可信度共同约束下，自主选择探索目标并规划局部与全局路径。

## 研究问题逻辑关系

三个研究问题不是并列孤立模块，而是“建模认知 -> 探索决策 -> 路径执行 -> 观测反馈”的闭环关系。

1. 环境-平台-约束模型与可信度是底座，回答世界状态、平台能力和地图可靠性如何表达。
2. 自主探索目标选择是决策层，依赖模型与可信度来判断下一步探索哪里，并需要路径规划反馈目标是否可达、代价是否合理。
3. 全局与局部路径规划是执行层，依赖探索目标和综合代价地图生成可执行路径，执行后的观测再反过来更新地图与可信度。

推荐从最小可用的多层动态地图与可信度地图开始做，然后建立 A* 全局规划基线，再加入探索目标效用函数、观测更新与重规划闭环，最后扩展局部安全走廊、IRIS/GCS 或轨迹优化。

## 工程实现状态

当前本地工程已拆分为三个子项目，并且 `path-planner` 已经取代 `a_gcs_ws-2.0.1` 成为仓库内路径执行评估层：

| 子项目 | 定位 | 当前进展 |
|---|---|---|
| `dev-platform-constraints` | 建模底座 | P0/P1/P2 已形成可运行研究原型，覆盖地图契约、地形特征、平台配置、硬约束、非负代价、可信度更新、覆盖率收益、候选目标和多步序列解释。 |
| `model-explorer` | 探索决策与闭环编排 | 已有 `model-explorer-contract/v1` 加载、目标排序、重规划原因、合成 benchmark、策略训练/评估 smoke、`path_planner_route` CLI/JSON 适配和 sidecar 半真实路径反馈入口；下一步主要固化多场景诊断解释。 |
| `path-planner` | 路径规划与执行可行性评估 | 已重建到 Phase 8，包含平台约束 A*、后处理安全走廊、平滑、曲率检查、可跟踪路径、跟踪仿真、固定走廊轨迹优化、执行感知指标和可选 IRIS/区域图诊断。 |

旧的 `a_gcs_ws-2.0.1` 只保留为历史参考和禁用依赖检查，不再作为联调目标。后续接口应围绕 `model-explorer-contract/v1`、`path-planner-request/v1` 和 `path-planner-route/v1` 收敛。

## 下一阶段推进

1. 固化端到端 JSON 联调：由 `dev-platform-constraints` 生成契约报告和 sidecar，`model-explorer` 选择 Top-K 目标并生成 `path-planner-request/v1`，`path-planner` 输出 `path-planner-route/v1`，再把可达性、代价、安全和失败原因回写到目标排序。
2. 以 `bash scripts/run_path_feedback_validation.sh --scenario-set all --diagnostic-profile all --top-k 3` 作为下一阶段主验收链路，覆盖 smoke、stress 和 mixed-stress 场景，并要求 `open_grid_fallback_used = false`。
3. 固化 `path-feedback-summary/v1` 结果面：目标选择变化、路径失败、重规划、安全违规、优化 fallback、IRIS 状态、region graph 来源/回退/断连和场景组聚合都必须可复现。
4. 扩展 stress / mixed-stress 解释能力：下一阶段重点不是让所有场景成功，而是让失败、绕行、replan 和诊断原因可比较、可写入实验表格。
5. 推进 `path-planner` 的生产化边界：保持当前 A* 和固定走廊优化为可靠 fallback，IRIS/region graph 仍只是 2D workspace 诊断；只有当它能稳定解释路径失败或高风险暴露后，才评估完整 GCS/Drake 替换。

当前首版 `model-explorer` 适配器已能通过 `path_planner_route` 后端生成 `path-planner-request/v1`，并通过 `path-planner` CLI 子进程消费 `path-planner-route/v1`。由于 `model-explorer-contract/v1` 稳定字段暂不包含完整 `cost` 和 `passable_mask` 数组，合成 smoke 可使用 open-grid fallback；半真实验证必须补充来自 `dev-platform-constraints` 的真实代价图和硬约束掩膜。

半真实闭环的当前推进方式是由 `dev-platform-constraints` 导出成对 JSON：`model-explorer-contract/v1` 保持候选目标与观测摘要，`path-planner-sidecar/v1` 补充 `cost`、`passable_mask` 和 terrain layers。`model-explorer` 使用 `path_planner_route + path_planner_sidecar` 配置批量评估 Top-K 目标，并把不可达、路径代价、失败原因、跟踪安全和优化 fallback 写入路径反馈摘要。IRIS/region graph 当前只作为诊断特征；只有当它能稳定解释路径失败或高风险暴露后，才进入完整 GCS/Drake 替换评估。

下一步实验入口已收敛为 `model_explorer path-feedback run <manifest>` 和根目录一键脚本。该入口记录路径反馈前后的目标变化、`open_grid_fallback_used`、路径失败数、重规划数、跟踪安全违规数、轨迹优化 fallback 数、region graph 断连数和单位路径代价覆盖收益。半真实实验结论必须基于 sidecar 的真实 `cost/passable_mask`，不能基于 open-grid fallback。IRIS/region graph 字段不等同于 GCS trajectory，也不宣称输出 Ackermann/skid-steer feasible trajectory。

## 关联页面

- [研究构想笔记：月面复杂约束下无人平台自主探索与规划](https://www.notion.so/36b6bbebd15c8141bfaacf83f216cbc0)
- [研究报告：月面环境复杂约束下无人平台自主智能探索与规划](https://www.notion.so/36b6bbebd15c81c09987f259d120a785)
- [研究图谱：月面巡视探索知识结构](https://www.notion.so/36b6bbebd15c810eb6abc0d02f9984a4)
- [文献补充版：月面无人平台自主探索与规划研究框架](https://www.notion.so/36b6bbebd15c81fca6eaff04fe988932)

## 关联数据库

- [Class Notes](https://www.notion.so/6216bbebd15c8214897681111d5613d7)
