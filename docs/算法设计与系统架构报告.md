# `path-planner` 算法设计与系统架构报告

生成日期：2026-06-07

## 1. 资料边界

本报告面向根仓库当前使用的子项目 `path-planner`，而不是旧的
`a_gcs_ws-2.0.1-direction-cone-curvature` 实验目录。报告依据如下：

- `path-planner/src/path_planner/`
- `path-planner/tests/`
- `path-planner/docs/superpowers/specs/`
- 根仓库与 `path-planner` 之间的 JSON 契约

当前 `path-planner` 的执行层边界是 2D 几何路径规划与诊断。它可以评估 sampled
geometric candidate 的碰撞、地形代价、方向锥约束和曲率/航向风险，但不宣称输出
Ackermann-feasible trajectory。

## 2. 总体结论

`path-planner` 当前是一个分层路径规划与执行层评估原型。稳定主链路仍然是：

```text
path-planner-request/v1
  -> cost/passable grid
  -> default A* baseline path
  -> postprocess corridor around the selected seed path
  -> fallback_box / workspace_iris region sequence
  -> optional pydrake_direction_cone_program / pydrake_control_point_direction_cone_program
  -> sampled 2D geometric candidate
  -> collision / cost / direction_cone / motion feasibility diagnostics
  -> path-planner-route/v1
```

`Channel-Aware A* Seed Path v1` 已从设计目标推进到 opt-in audit 代码路径。它没有绕过
A* 直接生成 corridor，而是在默认 A* baseline 之外增加
`channel_aware_astar` backend：把搜索目标从“单条 path cells 的累计低成本”扩展为
“围绕 seed path 可形成的局部通道综合低成本”。默认 A* baseline 的
reachable/path cost 语义保持不变，channel-aware 证据仍只能作为候选质量审计与校准输入。

当前仍不能把 channel-aware 结果解释为默认执行路线替代。本轮已经补齐
`Channel-Aware Route Execution Alignment v1` 的 opt-in 链路：当
`--planning-backend channel_aware_astar` 被选中时，CLI 会让 postprocess corridor、
region sequence、GCS candidate 和执行诊断围绕 selected seed path 重建，并在
`planning_backend_report.execution_alignment` 中标注
`default_route_replacement_verified=false`、`verification_scope=opt_in_audit_only`。
这表示 opt-in route artifact 内部已对齐，但默认 A* baseline 的 reachable/path cost 语义
仍保持不变，不能把 channel-quality 差异表述为默认 route replacement 成功。

当前 GCS 方向的核心增量是 `direction_cone` 与 control-point derivative proxy。它在固定
`convex_region_sequence` 上使用 Drake `MathematicalProgram` 求解 2D waypoint
变量或 control point 变量，并对相邻 region seed edge 或 successive control-point
derivative proxy 添加方向锥线性约束。control-point backend 现在额外加入确定性的
terrain-cost proxy，用 region 内 inverse-cost weighted passable-cell centroid 作为
`control_point_terrain_anchor_quadratic` 目标项，并在 route JSON 中区分 solver proxy
与 sampled 后验 terrain cost。当前 backend 包括
`pydrake_direction_cone_program` 和 opt-in
`pydrake_control_point_direction_cone_program`，不是完整 GCS graph search，也不是
production Bezier/B-spline 轨迹优化器。

本项目当前不加入 `hard` 曲率 fallback。若 `direction_cone` 未评估、未实际
backend-enforced、存在 violation，或 sampled candidate 发生碰撞、运动风险不可接受、
成本劣化，候选路径必须被阻断，而不是静默替换 route。

## 3. 当前系统架构

| 层级 | 当前模块 | 职责 |
| --- | --- | --- |
| CLI / 契约层 | `path_planner.cli`, `adapters.py` | 读取 request，输出 `path-planner-route/v1`。 |
| 搜索层 | `search/` | 生成默认 A* baseline，提供稳定 reachable 与 path cost 语义；已新增 opt-in channel-aware A*，用通道综合代价生成候选 seed path 审计证据。 |
| 后处理层 | `postprocess/` | 围绕选定 seed path 生成 corridor 和 smoothed geometric path；默认情况下 seed path 来自 A* baseline。 |
| 区域层 | `drake_backend/corridor_regions.py`, `iris.py` | 生成 `ConvexRegionSequenceReport`；当前默认可用边界是 `fallback_box`，可选 `workspace_iris` 只作为工作空间 H-polyhedron backend。 |
| GCS 原型层 | `drake_backend/gcs_trajectory.py`, `gcs_control_point_trajectory.py` | 在固定 region sequence 上求解 waypoint 或 control-point direction-cone prototype。 |
| 诊断层 | `drake_backend/gcs_diagnostics.py`, `gcs_motion_feasibility.py` | 输出 direction cone、terrain cost、high-cost exposure、curvature/heading/turning-radius 后验诊断。 |
| 候选选择层 | `drake_backend/gcs_candidate.py` | 只在约束、碰撞、运动诊断和成本门槛均通过时选择 GCS geometric candidate。 |
| CLI batch 证据层 | `drake_backend/gcs_cli_batch.py` | 通过真实 `path_planner.cli` route JSON 汇总 GCS direction-cone、motion-feasibility 与 control-point terrain-cost 场景证据。 |
| 测试层 | `tests/test_drake_backend.py` 等 | 覆盖 route additive fields、fallback reason、candidate blocking、可选 Drake backend。 |

## 4. 明确非目标

当前 `path-planner` 不使用 IrisNp、IrisNp2、IrisZo 或 C-space IRIS。文档中若提到
IRIS，只指本项目已有的 2D workspace/fallback region 诊断边界。

当前 `path-planner` 不实现完整 Graph of Convex Sets branch-and-bound、rounding、
graph search 或 fixed-path re-solve。`pydrake_direction_cone_program` 是固定 region
sequence 上的连续优化原型。

当前 `path-planner` 已有 opt-in control-point derivative proxy 原型，但不实现
production Bezier/B-spline trajectory optimizer。当前 control point 仍服务于 sampled
2D geometric candidate diagnostic。

当前 `path-planner` 不加入 `hard` 曲率 fallback，也不把 sampled geometric path 表述为
Ackermann-feasible trajectory。

## 5. `direction_cone` 约束设计

给定相邻 region seed `s_i, s_{i+1}`，waypoint backend 为每个 region 建一个 2D 连续变量
`p_i = [x_i, y_i]`；control-point backend 为每个 region 建一个 2D control point
`cp_i = [x_i, y_i]`。两者都添加：

- region containment：`A_i p_i <= b_i` 或 `A_i cp_i <= b_i`
- start/goal fixed：首尾 waypoint/control point 固定到首尾 seed
- direction cone per edge 或 derivative proxy：

```text
t^T d_i >= rho
n^T d_i - eta * t^T d_i <= 0
-n^T d_i - eta * t^T d_i <= 0
```

其中 waypoint backend 使用 `d_i = p_{i+1} - p_i`，control-point backend 使用
`d_i = cp_{i+1} - cp_i` 作为 derivative proxy。`t` 是相邻 seed 的参考切向，`n` 是法向，
`eta = tan(theta_max)`。`rho` 不再只作为一个不可解释常数；报告中需要说明它来自
seed distance、相邻 region portal/overlap width 和 support width 的保守下界组合。

`gcs_trajectory_constraint_summary` 应报告：

- `backend_enforced`
- `enforcing_backend`
- `solver_constraint_count`
- `eta`
- `rho_lower_bound_min_m`
- `rho_source_counts`
- `portal_width_min_m`
- `support_width_min_m`
- `constraint_tightness_min`
- `trajectory_parameterization`
- `control_point_count`
- `derivative_constraint_count`
- `control_point_region_containment_count`
- `objective_terms`
- `objective_term_weights`
- `terrain_objective_source`
- `terrain_objective_weight`
- `terrain_objective_boundary`
- per-edge/per-sample risk flags

只有 solver 实际添加 direction-cone 线性约束并成功求解时，才允许
`backend_enforced=true`。

## 6. 成本设计

当前 objective 保持简洁：

- segment length quadratic proxy
- low-cost anchor tracking proxy
- second-difference smoothness proxy
- control-point second-difference proxy
- control-point terrain anchor quadratic proxy

route JSON 的成本解释以 sampled path 后验计算为准：

- `path_length`
- `terrain_path_cost`
- `sampled_terrain_cost`
- `control_point_terrain_cost`
- `terrain_objective_source`
- `terrain_objective_weight`
- `terrain_objective_boundary=proxy_not_continuous_field_integral`
- `high_cost_exposure`
- `baseline_high_cost_exposure`
- `energy_proxy`
- `smoothness_proxy`
- `sampled_cell_count`
- `blocked_sample_count`
- baseline/postprocess cost delta
- candidate decision 和 quality gate

候选选择不能只看 solver success。若 candidate 未带来 baseline/postprocess 质量收益、
与 baseline 重复、碰撞、运动诊断 infeasible、high-cost exposure 相对 baseline/postprocess
变差，或 direction cone 未 enforce/violate，必须输出明确 fallback reason。

当前 terrain-cost objective 仍是工程代理：它使用固定 region sequence 中的 passable
cell cost 生成 control-point anchor，不是连续地形场积分，也不替代 sampled path 后验成本。

## 7. Ackermann 边界

月面巡视车后续当然需要车辆运动学、执行控制与闭环跟踪验证，但当前
`path-planner` 这一阶段只输出 2D geometric candidate 和 sampled diagnostics。

“不宣称 Ackermann-feasible trajectory”的含义是：

- 不声明当前输出已经满足前轮转角、轴距、速度/加速度连续性和控制输入约束。
- 不声明当前 sampled path 可直接由 Ackermann 底盘闭环执行。
- 当前 motion feasibility 只是曲率、heading、turning-radius 后验诊断，用来阻断高风险
  geometric candidate。

后续若要进入 Ackermann feasibility 层，需要新增独立模型、约束、控制接口和验证门槛，
不能由当前 `direction_cone` summary 自动推出。

## 8. 当前风险与后续阶段

当前主要剩余风险：

- control-point derivative proxy 仍弱于 production Bezier/B-spline trajectory optimizer。
- portal/support width 的 `rho` 参数已进入 route JSON 和 scenario summary，但仍是保守工程下界。
- terrain/high-cost 通过 control-point terrain anchor proxy 影响 solver，不是连续地形场积分成本。
- 可选 Drake backend 不可用时只能输出明确 not-evaluated summary，不能宣称 GCS 求解。

Portal/Cost Calibration、Scenario Matrix、`GCS Direction-Cone CLI Scenario Batch Evidence v1`
与 `GCS Motion-Feasibility Batch Gate v1` 已完成到 route JSON evidence 层。随后完成
`GCS Control-Point Direction-Cone Prototype v1` 与
`GCS Control-Point Terrain-Cost Calibration v1` 已推进到 `path-planner` route JSON
evidence 层：

- 新增 opt-in `pydrake_control_point_direction_cone_program`，用 successive control-point
  difference 作为 derivative proxy。
- 将 direction-cone 线性约束施加到 derivative proxy 上，并记录
  `trajectory_parameterization=control_point_derivative_proxy`、`control_point_count`、
  `derivative_constraint_count`、`control_point_region_containment_count` 和 `objective_terms`。
- 新增 `control_point_terrain_anchor_quadratic` 目标项、`objective_term_weights`、
  `terrain_objective_source`、`sampled_terrain_cost`、`control_point_terrain_cost` 和
  `terrain_objective_boundary=proxy_not_continuous_field_integral`。
- candidate gate 新增 high-cost exposure 不劣化检查：若总成本改善但 high-cost exposure
  相对 baseline/postprocess 变差，候选被阻断并输出明确 fallback reason。
- 通过 `--gcs-control-point-candidate` 从真实 `path_planner.cli` route JSON 验证 control-point
  backend、candidate decision、motion feasibility blocking、heading violation、
  turning-radius violation 和 pydrake unavailable。
- 新增 `--batch-kind control-point-terrain-cost`，输出
  `gcs_control_point_terrain_cost_cli_batch/v1`，覆盖 low-cost selected、cost-dominated、
  high-cost exposure blocked、motion-infeasible 和 pydrake-unavailable。
- 保留 `gcs_direction_cone_cli_scenario_batch/v1` 与 `gcs_motion_feasibility_cli_batch/v1`
  行为，避免旧证据合同回归。
- 保持 route contract additive，不改变 `trajectory_kind=geometric_path` 和 `reachable` 语义。

根仓库当前新增的是 `GCS Control-Point Semi-Real Evidence Bridge v1`：在
`scripts/run_path_feedback_validation.sh` 中显式 opt-in 转发
`--gcs-control-point-candidate`，让半真实 `cost/passable_mask` path-feedback validation
可以收集 control-point terrain-cost GCS 证据。这个 flag 不属于
`--diagnostic-profile all` 的默认语义，默认 acceptance gate 仍保持原有行为。

启用该 flag 后，`path-feedback-summary/v1` 和 batch summary 只新增 additive
`gcs_control_point_*` 字段，例如 report/attempt/success count、backend count、
candidate selected count、fallback reason count、terrain objective source count、
`sampled_terrain_cost` 统计、`high_cost_exposure_delta` 统计和 candidate audit。若
`pydrake` 不可用，summary 必须显示 `not_evaluated`，不能伪造为 GCS success。该桥接
不把 GCS candidate 当成默认 route 替代，不改变 `path-planner-route/v1`、
`model-explorer-contract/v1` 或 `path-feedback-summary/v1` 的既有稳定字段含义。

下一阶段新增的是 `GCS Control-Point Candidate Triage & Calibration v1`：summary
固定输出 `gcs_control_point_candidate_triage`，schema 为
`gcs-control-point-candidate-triage-summary/v1`；同时输出
`gcs_control_point_candidate_artifacts`，schema 为
`gcs-control-point-candidate-artifact-index/v1`。artifact index 为每个
control-point 候选记录 scenario/action、route JSON、request JSON、contract 和 sidecar
路径，使 `cost_dominated`、`direction_cone_constraint_violation`、
`unsupported_route_replacement` 等结果可以按候选复查。
`scripts/export_gcs_control_point_candidate_triage.py` 可把该 triage block 从
`path-feedback-summary.json` 导出为独立 JSON/Markdown 审查材料。

当前 evidence 根目录为 `outputs/path_feedback_gcs_control_point_triage_current/`；
独立导出文件为 `gcs-control-point-candidate-triage-summary.json` 与
`gcs-control-point-candidate-triage-summary.md`。该 run 的关键计数是：23 个
candidates、10 个 attempted、10 个 success、0 个 selected、23 个 route artifacts；
fallback counts 为 `cost_dominated=5`、`direction_cone_constraint_violation=5`、
`unsupported_route_replacement=13`；`calibration_sweep` 保持
`default_change_recommended=false`。
随后 `scripts/run_gcs_control_point_calibration_matrix.py` 将这些 request artifacts
按显式 solver 参数矩阵重跑，输出
`gcs-control-point-calibration-matrix-summary/v1` 与
`gcs-control-point-targeted-sweep-summary/v1`。默认矩阵已扩展为直接调参比较：
覆盖 terrain objective weight、second-difference weight、`direction_cone`
tolerance/rho 的单因子变化、少量 joint tuning 组合，以及 opt-in high-cost exposure
proxy entries。前一阶段 targeted sweep
evidence 根目录为
`outputs/path_feedback_gcs_control_point_targeted_sweep_current/`：11 个 matrix
settings、23 个 candidates、253 个 route artifacts，`safety_regression_count=29`。
没有任何组合提高 selected count；wide/relaxed `direction_cone` 组合将 14 个 case
从 `direction_cone_constraint_violation` 迁移为 `cost_dominated`，terrain 梯度和
tight-cone 组合会引入额外 safety regression。因此直接调参比较的结论是：
`direction_cone` 确实阻断部分候选，但放宽后主要暴露为 cost/quality gate blocker，
尚不支持默认参数更新，`recommendation=no_default_change_recommended`。
随后 `scripts/analyze_gcs_control_point_cost_gate.py` 对 targeted sweep 的
cost-gate cases 输出
`gcs-control-point-cost-gate-decomposition-summary/v1`。当前 decomposition evidence
根目录为
`outputs/path_feedback_gcs_control_point_cost_gate_decomposition_current/`：69 个
cost-gate cases，其中 `high_cost_exposure_blocked=38`、
`true_cost_dominated=20`、`safety_regression_excluded=11`。当前没有 case 被主分类为
`terrain_proxy_mismatch`、`baseline_overlap_or_duplicate` 或
`insufficient_cost_diagnostics`。因此现有证据更支持“候选确实暴露高风险/高成本”
而不是“缺少诊断字段或默认参数过保守”。但 high-cost exposure 本身不是不可通行硬约束；
它暴露的问题更准确地说是：当前默认 A* seed path 只优化单条路径累计代价，围绕它生成的
corridor/region sequence 不一定是综合低成本通道。下一步若继续推进，应优先新增
channel-aware A* seed-path backend，把 corridor mean/max cost、high-cost exposure
proxy、passable margin/clearance、blocked-nearby penalty 和 smoothness/direction proxy
纳入搜索代价，而不是直接更新默认 solver 参数或继续盲扫 GCS objective 权重。

当前推进阶段新增的是 `GCS Control-Point High-Cost Exposure Proxy v1`：在
control-point GCS backend 中增加 opt-in
`--gcs-control-point-high-cost-exposure-weight`，仅在
`--gcs-control-point-candidate` 下生效。它使用当前 cost/passable/sidecar 信息构造
`region_high_cost_exposure_proxy`，作为 control-point anchor objective 的保守 proxy；
route JSON 在 `gcs_trajectory_cost_summary` 与 constraint summary 中记录
`high_cost_exposure_objective_weight`、`high_cost_exposure_proxy_cost`、
`high_cost_exposure_proxy_source`、objective term 权重，并保留
`proxy_not_continuous_field_integral` 边界。candidate quality gate 仍以后验
`high_cost_exposure_delta_vs_baseline`、terrain cost、collision、`direction_cone`
和 motion diagnostics 不劣化为准。

high-cost proxy evidence 根目录为
`outputs/path_feedback_gcs_control_point_high_cost_proxy_current/`：13 个 matrix
settings、23 个 candidates、299 个 cases、`selected_count=0`、
`safety_regression_count=36`、`high_cost_exposure_proxy_case_count=46`、
`high_cost_exposure_proxy_evaluated_count=20`，仍为
`recommendation=no_default_change_recommended`。对应 cost-gate decomposition 位于
`outputs/path_feedback_gcs_control_point_high_cost_proxy_current/cost_gate_decomposition/`：
84 个 cost-gate cases，其中 `high_cost_exposure_blocked=46`、
`true_cost_dominated=24`、`safety_regression_excluded=14`，且
`terrain_proxy_mismatch=0`、`insufficient_cost_diagnostics=0`。这说明生成阶段 proxy 已经
被纳入求解目标和诊断，但当前仍未产生可通过 safety/quality gate 的替代 route。该结果
不应解读为“高成本区域绝对不可走”，而应解读为“当前单条 A* seed path 形成的局部通道
不足以稳定产生更优 GCS candidate”。默认参数不得更新。

因此下一阶段的优先目标调整为 `Channel-Aware A* Seed Path v1`：保留 default A*
baseline 作为稳定参照，新增 opt-in `channel_aware_astar` backend。该 backend 仍使用
现有 cost/passable/sidecar 信息，但搜索目标从单点 path cost 扩展为通道代价：

```text
channel_step_cost =
  center_cell_cost
  + corridor_neighborhood_mean_or_max_cost
  + high_cost_exposure_proxy
  + passable_margin_or_clearance_penalty
  + blocked_nearby_penalty
  + smoothness_or_direction_proxy
```

channel-aware A* 输出的仍是 seed path；后续仍由 postprocess 生成 corridor，再由
fallback_box/workspace_iris 生成 region sequence，并进入 control-point GCS 与 candidate
gate。该阶段不绕过 A*，不直接生成无 seed path 的 corridor，也不把 GCS candidate 变成
默认 route replacement。

当前半真实 evidence 的解释边界是：

- smoke 组 control-point program 可以求解，但候选仍可能因 `cost_dominated` 或
  `direction_cone_constraint_violation` 被阻断。这表示质量门槛、terrain-cost proxy 和
  `direction_cone` 约束需要校准，不表示应放宽 safety gate。
- stress 组若 region sequence 不连通或 route replacement 不支持，`not_evaluated` 是预期
  诊断结果，不应被解读为 GCS 成功或失败后静默替换 route。
- triage 内含 `calibration_sweep`
  （`gcs-control-point-candidate-calibration-sweep/v1`），这是基于已记录候选的诊断
  sweep，覆盖 terrain objective weight、second-difference weight、direction-cone
  rho/eta/tolerance 和 quality-gate 阈值。targeted sweep 是对该矩阵的真实 solver
  rerun；cost-gate decomposition 是对 sweep 后 `cost_dominated` 结果的解释层。默认参数
  只有在 selected count 增加、target blockers 减少，且 terrain cost、high-cost exposure、
  collision、`direction_cone` 和 motion diagnostics 均不劣化时才可更新。
- high-cost exposure 是软风险/代价信号，不是 passability 硬约束。下一阶段应把它前置到
  channel-aware A* 的通道搜索代价中，同时保留 collision、passable_mask、
  `direction_cone` 和 motion diagnostics 作为 candidate gate 的硬边界。
- 该阶段仍不实现完整 GCS graph search/rounding/branch-and-bound，不使用 C-space IRIS，
  不恢复 hard 曲率 fallback，不宣称 Ackermann-feasible trajectory。

## 9. Platform-Aware Target/Anchor Contract v1

本阶段已经把平台 footprint 语义从事后诊断提升为明确的 target/anchor 合约边界。核心变化是：

- `dev-platform-constraints` 的 `path-planner-sidecar/v1` metadata 新增
  `platform_goal_admissibility`。该字段不改变既有 `cost` 和 `passable_mask` 语义，但会记录
  `schema_version=platform-goal-admissibility/v1`、`footprint_radius_m`、
  `passable_source=inflated_passable_mask`、`inflated_passable_mask`、
  `original_blocked_count`、`inflated_blocked_count`、`cell_roles` 和 `training_use`。
  因此下游可以直接判断目标 cell 是否 inflated-passable。
- `model-explorer` 的 `platform_goal_feasibility/v1` 新增
  `policy_target_cell`、`execution_goal_cell` 和 `anchor_projection`。当目标 cell
  `original_passable=true` 但 `inflated_passable=false` 时，`execution_goal_cell=null`，
  `nearest_inflated_passable_anchor` 只作为 audit projection candidate；其
  `training_use=not_positive_evidence`，`same_cell_positive_evidence=false`。
- `platform_inflated_goal_blocked` 继续被排除在 positive training evidence 之外。当前
  `audit_proxy_anchor_not_same_cell` 既不是 same-cell positive evidence，也不是可直接训练的
  negative evidence；只有 future anchor-projection contract 明确允许时，才可能进入训练样本。
- `Channel-Aware Route Execution Alignment v1` 已补齐：`channel_aware_astar` selected 后，
  `postprocess.raw_path` 与 route `geometric_path` 对齐，后续 corridor、region sequence、
  GCS candidate 和 motion diagnostics 围绕 selected seed path 重建。route 和
  path-feedback summary 同时保留 `execution_alignment`，其中
  `default_route_replacement_verified=false`、`verification_scope=opt_in_audit_only`。

稳定合约边界保持不破坏：`model-explorer-contract/v1` 的稳定字段语义不变，
`path-planner-route/v1` 和 `path-planner-sidecar/v1` 只新增 additive metadata/report 字段；
default A*、PPO、network architecture、action space 均未修改。

## 10. Current-HEAD Evidence 与当前瓶颈

最新 current-HEAD evidence 根目录已刷新为
`outputs/path_feedback_batch_anchor_projection_candidate_generation_v1/`，刷新日期为
2026-06-07。上一轮 `outputs/path_feedback_batch_anchor_projection_contract_v1/` 是提交
`d15c667 Add anchor projection evidence contract` 前的旧证据，当前 HEAD 下会触发
`current_git_provenance_mismatch`，不能再作为训练或验收输入。

新 evidence root 已重跑 batch、stability、sample-quality、policy-robustness、
channel-aware、readiness、goal-blocked、anchor-projection contract，以及
candidate-generation summary。所有 validate-only 消费链路均未再出现
`current_git_provenance_mismatch` 或 `git_provenance_mismatch`。精确父仓库与子模块 SHA
以 `batch-run-index.json` 和各 summary 的 `git_provenance` 为准。

刷新后的关键计数：

- batch evaluation：8 runs、8 passed、0 failed，`open_grid_fallback_used_count=0`。
- 新增 `anchor-projection-candidate-generation-summary/v1`：
  `status=passed`，`current_git_provenance_mismatch_count=0`，
  `git_provenance_mismatch_count=0`，`fallback_or_open_grid_count=0`，
  `safety_regression_count=0`。
- anchor-projection candidate-generation：
  `platform_goal_contract_mismatch_count=78`，
  `trainable_anchor_projection_count=18`，
  `nontrainable_blocked_target_count=60`，
  `source_selected_candidate_changed_rate=0.23076923076923078`，
  `positive_training_evidence_contains_audit_proxy_anchor_count=0`。
  这里按 source policy target context 去重：只有 source selection 实际选择 projected
  execution target 的 context 才计入 trainable。
- `anchor_projection_coverage_diagnosis/v1` 已能解释剩余 60 个 nontrainable context：
  `projected_candidate_generated_count=42`，
  `projected_candidate_source_selected_count=18`，
  `projected_candidate_not_source_selected_count=24`，
  `anchor_unreachable_not_generated_count=36`。主因统计为
  `anchor_unreachable=36` 和 `source_candidate_not_selected=24`；projection distance
  分布为 72 个 1-cell、6 个 2-cell，未发现 audit proxy 被写入 positive evidence。
- 本轮 selection improvement 使用显式 opt-in
  `--anchor-projection-selection-path-cost-bonus 6.0`。Risk Closure v1 进一步加入
  manifest-only quality gate：
  `--anchor-projection-max-selection-path-cost-regression 6.0` 与
  `--anchor-projection-max-selection-risk-regression 0.5`。这些参数只影响
  `model-explorer` path-feedback 的 source selection 排序，只对可行、未 replan 的
  `projected_execution_target` 生效；不会转发给 `path-planner`，不会改变 default A*，
  也不会把不可达 anchor、fallback/open-grid 或 safety regression 样本伪装成正例。
  被 source selection 选中的 projected candidate 若相对最佳可行替代项出现超过阈值的
  raw path-cost/risk regression，会被标为
  `source_selected_quality_regression`，并保持
  `training_use=not_positive_evidence`、`sample_weight=0.0`。
  相比上一轮 12/78，新增的 6 个 trainable context 来自
  `npz_mixed_stress_detour`；剩余 24 个 generated-but-not-selected context 集中在
  `npz_high_risk_value_trap` 和 `npz_path_complexity_benefit_probe`，其
  path-cost margin 约为 2.17 到 10.88。
- channel-aware source selection audit 仍未改善：
  `channel-aware-policy-target-selection-evidence-summary/v1` 中
  `selected_candidate_changed_rate=0.0`；contrast calibration 层为
  `selected_candidate_changed_rate=0.45454545454545453`。
- policy training readiness review 仍为
  `training_readiness_status=needs_training_contract_refinement`，
  `training_blockers=["goal_blocked_candidates_excluded_from_training_positive_evidence",
  "anchor_projection_contract_trainable_count_below_candidate_generation"]`（在显式传入
  `anchor-projection-candidate-generation-summary/v1` 与
  `anchor-projection-evidence-contract-summary/v1` 后）。
  这表示 PPO 仍不能启动；新增 trainable projection 只覆盖部分 source contexts，且
  readiness review 已能消费 candidate-generation/contract summary，但二者训练口径尚未对齐。
  既有 readiness 字段 `platform_goal_trainable_anchor_projection_count` 当前仍来自
  channel-aware/calibrated audit 消费链路，`platform_goal_nontrainable_blocked_target_count`
  仍表示该旧消费链路中的 blocked target 余额；二者不应与新增
  `anchor-projection-candidate-generation-summary/v1` 的 trainable context count 混用。
- `Anchor-Projection Evidence Contract v1` 的
  `anchor-projection-evidence-contract-summary/v1` 仍保持“禁止 audit proxy 混入
  positive evidence”的边界：
  `platform_goal_contract_mismatch_count=42`，
  `trainable_anchor_projection_count=3`，
  `nontrainable_blocked_target_count=39`，
  `positive_training_evidence_contains_audit_proxy_anchor_count=0`，
  `recommended_next_action=rerun_policy_training_readiness_review_with_anchor_projection_contract`。
  该 summary 仍不是 PPO readiness 的直接放行条件，不能与 candidate-generation 的 18/78
  计数混作同一训练正样本口径。上一轮 audit-only 口径的
  `keep_platform_blocked_targets_out_of_training` 边界仍有效：任何
  `audit_proxy_anchor_not_same_cell` 都不能被直接当作 positive training evidence。

因此瓶颈已经从“anchor projection 完全不能产生训练候选”推进为更窄的问题：
`model-explorer` 已能在 opt-in candidate generation 阶段为一部分
`original_passable=true`、`inflated_passable=false` 的 policy target 生成 projected
execution target，并且真实 source selection 已选择其中 18 个 context。但仍有 36 个
anchor 不可达 context 和 24 个 generated-but-not-selected context；同时既有
channel-aware source audit 口径仍为 0。下一步不能启动 PPO，应继续缩小不可达 anchor
与 source-selection margin 的缺口，并把 readiness review 对新 summary 的消费边界设计清楚。

Risk Closure v1 对 evidence/provenance 风险做了收口：新增统一
`scripts/git_provenance.py`，记录 parent 与 `dev-platform-constraints`、
`model-explorer`、`path-planner` 的 `sha`、`branch`、`dirty`、
`tracked_modified_count`、`untracked_count`、`ignored_untracked_count`。新格式 source
snapshot 只要声明 dirty，`require_current_git_match=true` 的下游 gate 就不会通过；旧格式只含
SHA 的 legacy summary 仍按 SHA 兼容，避免误判历史证据。隔离重跑
`outputs/path_feedback_batch_anchor_projection_candidate_generation_v1_risk_closure_check/`
在当前未提交工作区下得到 8/8 batch passed，但
`anchor-projection-candidate-generation-summary/v1` 预期失败：
`reason_codes=["current_git_provenance_mismatch","git_provenance_mismatch"]`，
`current_git_provenance_mismatch_count=1`，`git_provenance_mismatch_count=1`。同一 summary
的算法计数仍为 `trainable_anchor_projection_count=18`、
`nontrainable_blocked_target_count=60`、
`source_selection_quality_regression_count=0`；这说明本轮失败来自 dirty provenance gate，
不是 anchor-projection 候选能力回退。提交后需要在 clean worktree 下重跑该 root，才能把新
quality/provenance 字段作为正式 current-HEAD evidence 发布。

Risk Closure v1 还修复了几何与选择 gate：

- `AStarPlanner`、`ChannelAwareAStarPlanner` 和 `RegionGraphGuidedPlanner` 的
  `prevent_corner_cutting=True` 统一为严格语义：对角移动两侧 side cell 任一 blocked 即禁止，
  避免“一侧贴障穿角”的路径被当作可行。
- `channel_aware_astar` 不能仅凭 `high_cost_exposure` 下降就接受
  `channel_cost_delta>0` 的候选；此类 case 返回
  `channel_candidate_quality_regression`，保持 opt-in audit-only 边界。

Risk Closure v2 继续收敛剩余工程风险，重点不是扩大样本，而是修正 evidence gate 的
一致性和解释口径：

- `scripts/git_provenance.py` 新增 `inspect_source_git_provenance(...)`，并迁移
  policy readiness、anchor-projection contract、goal-blocked、calibrated-policy smoke、
  channel-aware、sample-quality、policy-decision robustness 与 path-feedback stability
  链路。`require_current_git_match=true` 时，如果 source summary 缺少
  `git_provenance.current`，现在必须失败，并输出
  `current_git_provenance_missing` 与 `<label>_current_git_provenance_missing`；这堵住了
  `current_matches_sources=false` 但 `status=passed` 的假通过口径。
- readiness 的 anchor-projection path/risk margin blocker 改为只消费
  `source_selected_quality_regression` 或 `training_use=trainable_anchor_projection_contrast`
  的 context。未被 source selection 选中的 projected candidate 的大 margin 只保留在
  `diagnostic_max_source_selection_*_margin_vs_best_alternative`，不能单独触发
  training blocker。
- `model-explorer` 明确 best-alternative 集合为
  `reachable_non_replan_candidates_including_policy_and_projected_targets`：policy target 和
  projected target 都可作为 source-selection contrast，但必须 `reachable=true` 且
  `replan_required=false`。该 scope 与 alternative role 会写入 candidate annotation，并由
  `anchor-projection-candidate-generation-summary/v1` 透传。
- `path-planner` 的 direction-cone CLI batch 将过期的
  `sampled_trajectory_collision` 内置 case 重定标为
  `direction_cone_obstacle_detour`。严格 corner-cutting 后该 3x3 中心障碍场景的 sampled
  collision count 为 0，真实 blocker 是 `direction_cone_constraint_violation`；真正的
  `sampled_trajectory_collision` 代码路径仍由独立 GCS trajectory/candidate 测试覆盖。

Risk Closure v2 后的剩余瓶颈仍是算法证据层面的：clean worktree 下需要重新生成正式
anchor-projection evidence，并继续解释 36 个 anchor 不可达与 24 个 generated-but-not-selected
context；不能把 provenance 修复或 scenario 重定标解读为 PPO readiness。

Risk Closure v2 当前验证结果：

- 父仓库 `tests`：`110 passed, 10 subtests passed`。
- `model-explorer/tests`：`191 passed, 13 subtests passed`。
- `dev-platform-constraints/tests`：使用 `PYTHONPATH=src` 后
  `86 passed, 1 skipped, 36 subtests passed`。
- `path-planner/tests`：从子模块 cwd 使用 `PYTHONPATH=src` 全量 pytest 退出码为 0；
  本次收敛的 GCS CLI scenario batch 用例已通过。
- anchor-projection batch dry-run 成功展开 8 个 run；dirty-worktree 正式隔离 batch
  `outputs/path_feedback_batch_anchor_projection_candidate_generation_v1_risk_closure_v2_check/`
  为 8/8 passed。
- dirty worktree 下对该隔离 root 执行
  `run_anchor_projection_candidate_generation.sh --validate-only` 按预期失败：
  `reason_codes=["current_git_provenance_mismatch","git_provenance_mismatch"]`，
  `current_git_provenance_mismatch_count=1`，`git_provenance_mismatch_count=1`，
  `trainable_anchor_projection_count=18`。这仍是 provenance gate 的正确阻断，不是算法能力回退。
- 本地提交后 clean-worktree 正式隔离 batch
  `outputs/path_feedback_batch_anchor_projection_candidate_generation_v1_risk_closure_v2_final_clean_check/`
  为 8/8 passed；同 root 的 candidate-generation validate-only 和正式 summary 生成均通过：
  `status=passed`，`reason_codes=[]`，
  `current_git_provenance_mismatch_count=0`，`git_provenance_mismatch_count=0`，
  `trainable_anchor_projection_count=18`，`nontrainable_blocked_target_count=60`，
  `source_selection_quality_regression_count=0`。

当前 clean-worktree evidence refresh 已证明 candidate-generation gate 可在提交后通过；后续仍需要把
candidate-generation 的 18/78 计数与 evidence-contract/readiness 的训练正样本契约继续对齐。

## 11. 下一阶段推进目标

`Anchor-Projection Candidate Generation v1` 已落地为 opt-in candidate generation 层，而不是
默认 route replacement 或 PPO 训练层。新增产物包括：

- `model-explorer/src/model_explorer/policy/planning.py` 中的
  `AnchorProjectionCandidateConfig` 与 projected execution target evaluation。
- `model-explorer/src/model_explorer/policy/path_feedback.py` 中的
  source-selected trainable 标注与 aggregate counts。
- `model-explorer/src/model_explorer/policy/feedback_selection.py` 对 synthetic candidate
  的 source selection 支持。
- `scripts/run_path_feedback_validation.sh` 的
  `--anchor-projection-candidate-generation` 与
  `--anchor-projection-selection-path-cost-bonus`、source-selection path/risk regression
  gate opt-in 开关。
- `configs/path_feedback_batch_anchor_projection_candidate_generation_v1.json`
- `configs/anchor_projection_candidate_generation_v1.json`
- `scripts/run_anchor_projection_candidate_generation.py`
- `scripts/run_anchor_projection_candidate_generation.sh`
- `tests/test_anchor_projection_candidate_generation.py`
- `docs/superpowers/specs/2026-06-07-anchor-projection-candidate-generation.md`

当前阶段的正样本边界是：projected candidate 必须来自 source candidate generation，
`comparison_scope=projected_target_anchor_contrast`，`source_selection_status=source_selected`，
`training_use=trainable_anchor_projection_contrast`，且不能来自
`audit_proxy_anchor_not_same_cell`。未被 source selection 选中的 projected candidate 仍保持
`training_use=not_positive_evidence`、`sample_weight=0.0`。

`Anchor-Projection Readiness Contract Integration v1` 进一步把这条链路从“两个
summary 口径并存”推进为同一 current-HEAD contract：

- `anchor-projection-candidate-generation-summary/v1` 新增顶层
  `nontrainable_anchor_unreachable_count`、
  `nontrainable_source_candidate_not_selected_count` 和
  `audit_proxy_positive_count`，避免只在嵌套 diagnosis 中才能解释 60 个
  nontrainable context。
- `anchor-projection-evidence-contract-summary/v1` 在同一 batch root 存在
  candidate-generation summary 时，优先以
  `contract_source=anchor_projection_candidate_generation_summary` 分类
  projected execution target；旧 goal-blocked/readiness audit summary 缺失时不再导致
  contract gate 假失败。旧 audit-only source 仍保持向后兼容。
- `policy-training-readiness-review-summary/v1` 新增 anchor-only review 模式：当显式传入
  candidate-generation 与 evidence-contract summary 且旧 channel-aware 四件套不存在时，
  readiness 只审查 anchor-projection contract，不再把旧 audit/channel-aware 计数与
  candidate-generation 计数混用。

当前 integration evidence root：
`outputs/path_feedback_batch_anchor_projection_contract_integration_v1/`。
该 root 的 batch 为 8/8 passed；candidate-generation、evidence-contract 与 readiness
summary 均为 `status=passed`，`reason_codes=[]`，且 current/source provenance mismatch 为 0。
关键计数已经对齐：

- candidate-generation：`platform_goal_contract_mismatch_count=78`，
  `trainable_anchor_projection_count=18`，
  `nontrainable_blocked_target_count=60`，
  `nontrainable_anchor_unreachable_count=36`，
  `nontrainable_source_candidate_not_selected_count=24`，
  `source_selection_quality_regression_count=0`，
  `positive_training_evidence_contains_audit_proxy_anchor_count=0`。
- evidence-contract：同一 candidate-generation source 下
  `trainable_anchor_projection_count=18`，
  `nontrainable_blocked_target_count=60`，
  `candidate_contract_alignment_gap_count=0`，
  `contract_blockers=[]`。
- policy readiness：`application_scope=anchor_projection_readiness_contract_review_only`，
  `anchor_projection_readiness_trainable_count=18`，
  `anchor_projection_candidate_contract_alignment_gap_count=0`，
  `training_readiness_status=needs_training_contract_refinement`，
  `training_blockers=["anchor_projection_nontrainable_contexts_remain"]`。

因此本阶段解决的是 evidence contract/readiness 口径不一致问题，而不是 PPO 放行。
PPO 仍然 blocked，剩余瓶颈被收敛为 36 个 anchor unreachable context 与 24 个
source-candidate-not-selected context。下一阶段应优先分析这两类 nontrainable context
的地形/平台约束来源与 source-selection margin，而不是扩大样本或修改 action/network。

`Anchor-Reachability-Aware Candidate Generation v1` 已把上述 36 个
`anchor_unreachable` 从“最近 anchor 不可达即不生成候选”推进为“先做 inflated-mask
连通分量诊断，再在 start 所在连通分量内寻找可达替代 anchor”。当前实现保留
`require_anchor_reachable=True`，不把 unreachable anchor 标为正样本；新增
`anchor_selection_status`、`start_component_id`、`nearest_anchor_component_id`、
`projected_anchor_component_id`、component size 与
`reachable_substitute_anchor_count` 等诊断字段。候选生成策略的核心变化是：

- 旧策略：全图按几何距离找 `nearest_inflated_passable_anchor`，再检查 start 到该
  anchor 的 inflated-mask 路径；若不可达，则直接不生成 projected candidate。
- 新策略：仍记录几何最近 anchor，但当它不在 start 可达连通分量内时，优先在
  start 连通分量内寻找可达替代 anchor；替代 anchor 按目标投影距离与 start 路径步长排序。
- 训练边界：candidate generation 可以生成可达替代候选，但只有满足 source selection、
  `training_use=trainable_anchor_projection_contrast`、无质量回退，并且仍在
  evidence contract 的 `max_trainable_projection_distance_m=1.0` /
  `max_trainable_projection_distance_cells=2` 内，才计为 trainable。

当前 evidence root 为
`outputs/path_feedback_batch_anchor_reachability_aware_candidate_generation_v1/`。
该 root 的 batch 为 8/8 passed，`open_grid_fallback_used_count=0`。candidate-generation
summary 为 `status=passed`、`reason_codes=[]`，且 current/source provenance mismatch 为 0。
关键计数如下：

- `platform_goal_contract_mismatch_count=78`，
  `projected_candidate_generated_count=78`，
  `anchor_selection_status_counts={"nearest_anchor_reachable":42,"reachable_substitute_anchor_found":36}`。
- 原 36 个 `anchor_unreachable` 均被分解为
  `reachable_substitute_anchor_found_count=36`、
  `anchor_unreachable_repaired_by_reachable_substitute_count=36`、
  `true_geometry_unreachable_count=0`；因此当前不是样本几何完全不可达，而是旧候选生成策略漏选可达替代 anchor。
- 受训练距离 contract 限制，36 个 reachable substitute 均为
  `projection_distance_contract_rejected`；其中 12 个虽被 source selection 选中，但超过
  2 cells / 1.0 m，不计入训练正例。
- `trainable_anchor_projection_count` 仍为 18，
  `nontrainable_blocked_target_count=60`，
  `nontrainable_anchor_unreachable_count=0`，
  `nontrainable_source_candidate_not_selected_count=48`，
  `source_selection_quality_regression_count=0`。

evidence-contract 同一 root 下为 `status=passed`、`contract_blockers=[]`、
`candidate_contract_alignment_gap_count=0`，与 candidate-generation 的训练正样本口径一致。
policy readiness 仍为
`training_readiness_status=needs_training_contract_refinement`，
`training_blockers=["anchor_projection_nontrainable_contexts_remain"]`，
`anchor_projection_readiness_trainable_count=18`。因此本阶段解决的是“可达替代 anchor
候选生成与诊断”，不是 PPO 放行；下一阶段瓶颈已经从 anchor_unreachable 转为
source-selection 质量排序与训练距离 contract 的取舍。

非目标保持不变：不启动 PPO，不宣称 Ackermann-feasible trajectory，不使用 C-space
IRIS/IrisNp/IrisNp2/IrisZo，不修改 default A*、network architecture 或 action space，不把
channel-aware opt-in audit 结果称为默认 route replacement。

`Anchor Projection Source-Selection & Distance-Contract Calibration v1` 进一步把瓶颈从
“可达替代 anchor 是否能生成”收敛为“source selection 排序与训练距离 contract 的取舍”。
本阶段只扩展 audit summary 字段，不修改 PPO、`training.py`、`ppo.py`、
`architectures.py`、network、action space 或 default A*。

当前 evidence root 为
`outputs/path_feedback_batch_anchor_projection_source_selection_distance_contract_v1/`。
该 root 的 batch 为 8/8 passed，`failed_count=0`，
`open_grid_fallback_used_count=0`。candidate-generation summary 为
`status=passed`、`reason_codes=[]`、`current_git_provenance_mismatch_count=0`、
`git_provenance_mismatch_count=0`、`fallback_or_open_grid_count=0`、
`safety_regression_count=0`。evidence-contract 为 `status=passed`、
`contract_blockers=[]`、`candidate_contract_alignment_gap_count=0`。readiness review
消费了新 summary，仍保持
`training_readiness_status=needs_training_contract_refinement` 与
`training_blockers=["anchor_projection_nontrainable_contexts_remain"]`。

60 个 nontrainable context 的完整分解如下：

- `nontrainable_primary_reason_counts={"audit_proxy_scope_not_positive_evidence":12,"source_candidate_not_selected":48}`。
  其中 12 个 audit-proxy reason 实际是 source-selected projected candidate，但超过当前
  训练距离 contract，因此新增字段记为
  `source_selected_but_distance_rejected_count=12` 和
  `distance_contract_rejected_source_selected_count=12`。
- `distance_contract_rejected_by_distance_bin.count=36`，其中
  `source_selected_count=12`、`not_source_selected_count=24`。距离分箱为：
  3 cells / 1.5 m 共 18 个，6 个 source-selected、12 个 not-selected，集中在
  `npz_near_blocked_corridor`；7 cells / 3.5 m 共 12 个，全部 not-selected，集中在
  `npz_dense_rock_choke`；11 cells / 5.5 m 共 6 个，全部 source-selected，集中在
  `npz_dense_rock_choke`。
- 48 个 `source_candidate_not_selected` 的归因为：
  `distance_contract_rejected=24`、`higher_path_cost_and_risk=12`、
  `higher_path_cost=12`、`higher_risk=0`、`lower_utility_or_coverage=0`、
  `ranking_weight_tradeoff_or_unobserved_utility=0`、
  `no_selected_candidate_comparison=0`。
- source-selected 且 distance-rejected 的 12 个样本相对最终 selected candidate 的
  path/risk margin 均为 0；说明 source selection 本身未出现质量回退。但 36 个
  distance-rejected 样本仍超过 `max_trainable_projection_distance_cells=2` /
  `max_trainable_projection_distance_m=1.0`，因此本阶段不放宽默认训练 contract，只记录
  `distance_contract_relaxation_recommendation=record_only_keep_current_training_distance_contract`。

结论：当前瓶颈不是样本数量不足，也不是 anchor 仍不可达；可达候选已经全量生成并与
evidence/readiness 训练口径对齐。阻塞 PPO 的真实原因是现有训练正样本 contract 太保守：
18 个可训练样本之外，12 个已被 source selection 选中的远距离替代 anchor 缺少足够证据证明
可以安全放宽距离阈值，另有 48 个未被 source selection 选中。下一阶段应做
Distance-Contract Relaxation Safety Audit：只在证明远距离替代 anchor 无 path/risk/safety
回退、且 readiness blocker 能被消除时，才考虑显式放宽距离 contract；否则继续保持
audit-only，不启动 PPO。

`Distance-Contract Relaxation Safety Audit v1` 已新增为 audit-only 入口，用于把上述“是否
放宽距离 contract”的问题变成可复验 summary，而不是直接修改训练代码。新增产物包括
`scripts/run_anchor_projection_distance_contract_relaxation_safety_audit.py`、对应 `.sh` 包装、
`configs/anchor_projection_distance_contract_relaxation_safety_audit_v1.json`、
`configs/path_feedback_batch_anchor_projection_distance_contract_relaxation_safety_audit_v1.json`、
`tests/test_anchor_projection_distance_contract_relaxation_safety_audit.py` 与
`docs/superpowers/specs/2026-06-08-anchor-projection-distance-contract-relaxation-safety-audit.md`。

该 summary 在验证链路中紧跟 candidate-generation 运行，因此 candidate-generation summary 是
必需输入；evidence-contract 与 readiness summary 若已存在则一并校验，若尚未生成则记录为
optional input，不阻断后续 gate。它继续执行 provenance、fallback/open-grid、safety、
source-selection quality regression 与 audit proxy positive gate。默认 opt-in 审计上限为
3 cells / 1.5 m，path/risk margin 均不得为正，且默认训练 contract 不变。
若仍存在 not-source-selected distance-rejected context、7/11 cell 远距离样本、provenance
mismatch 或任何安全门失败，结论必须保持 `keep_current_training_distance_contract`。

本阶段正式验证在不包含本地 `AGENTS.md` 修改的 clean worktree 中完成，证据根为
`outputs/path_feedback_batch_anchor_projection_distance_contract_relaxation_safety_audit_v1/`。
batch 为 `run_count=8`、`passed_count=8`、`failed_count=0`、`open_grid_fallback_used_count=0`。
candidate-generation、distance-contract audit、evidence-contract 与 readiness summary 均为
`status=passed`、`reason_codes=[]`，provenance mismatch 为 0。

关键结论：

- candidate-generation 仍为 `78 = 18 trainable + 60 nontrainable`，
  `source_selected_but_distance_rejected_count=12`，
  `nontrainable_source_candidate_not_selected_count=48`，
  `source_selection_quality_regression_count=0`，
  `positive_training_evidence_contains_audit_proxy_anchor_count=0`。
- distance-contract audit 输出
  `recommendation=keep_current_training_distance_contract`；
  36 个 distance-rejected context 中，12 个 source-selected、24 个 not-source-selected；
  12 个 source-selected 中只有 6 个满足当前保守 opt-in 上限，另 6 个仍 ineligible。
- evidence-contract 为 `contract_blockers=[]`、
  `candidate_contract_alignment_gap_count=0`。
- readiness 仍为 `training_readiness_status=needs_training_contract_refinement`，
  `training_blockers=["anchor_projection_nontrainable_contexts_remain"]`。

因此该阶段完成的是安全审计与保守结论固化，而不是 PPO 放行；PPO 仍不启动。

`Anchor Projection Nontrainable Context Reduction / Source-Selection Candidate Quality v1`
是上述保守结论之后的下一阶段。该阶段新增
`scripts/run_anchor_projection_nontrainable_context_reduction.py`、对应 `.sh` 包装、
`configs/anchor_projection_nontrainable_context_reduction_v1.json`、
`configs/path_feedback_batch_anchor_projection_nontrainable_context_reduction_v1.json` 与
`tests/test_anchor_projection_nontrainable_context_reduction.py`，目标证据根为
`outputs/path_feedback_batch_anchor_projection_nontrainable_context_reduction_v1/`。

该 summary 的职责不是把 60 个 nontrainable context 直接转成训练正样本，而是给出逐类去向：

- 默认训练可安全转化数必须保持 `safe_default_training_conversion_count=0`，因为默认
  2 cells / 1.0 m distance contract 不变。
- 6 个 source-selected、3 cells / 1.5 m、path/risk margin 为 0 的 context 只能记为
  `opt_in_relaxation_followup_candidate`，仍需显式 opt-in contract refinement，不能作为
  默认 PPO 训练放行。
- 另 6 个 source-selected 但 11 cells / 5.5 m 的 context 仍属于
  `blocked_source_selected_distance_too_far`。
- 24 个 not-source-selected 且 distance-rejected 的 context 仍属于
  `blocked_not_source_selected_distance_rejected`。
- 剩余 24 个 source-candidate-not-selected context 继续由 source-selection 质量与
  path/risk margin 归因解释。

因此下一阶段的主问题从“是否放宽默认 distance contract”转为“能否减少或解释
source-selection 未选中与 distance-rejected 的 nontrainable context”。验收时必须继续满足
`current_git_provenance_mismatch_count=0`、`git_provenance_mismatch_count=0`、
`candidate_contract_alignment_gap_count=0`、
`positive_training_evidence_contains_audit_proxy_anchor_count=0` 和
`source_selection_quality_regression_count=0`；若仍有 nontrainable context，readiness blocker
必须保留 `anchor_projection_nontrainable_contexts_remain`。

本阶段 clean evidence 已在临时 clean worktree 生成并同步到主工作区
`outputs/path_feedback_batch_anchor_projection_nontrainable_context_reduction_v1/`。batch 为
8/8 passed，`open_grid_fallback_used_count=0`；candidate-generation、evidence-contract、
readiness、distance-contract audit 与 nontrainable-reduction summary 均为 `status=passed`、
`reason_codes=[]`。nontrainable-reduction 结论为
`recommendation=keep_training_blocker_focus_source_selection_candidate_quality`，
`safe_default_training_conversion_count=0`，
`opt_in_relaxation_followup_candidate_count=6`，
`must_remain_blocked_count=60`。分类计数为：
`opt_in_relaxation_followup_candidate=6`、
`blocked_source_selected_distance_too_far=6`、
`blocked_not_source_selected_distance_rejected=24`、
`blocked_source_candidate_not_selected_quality=24`。

`Contract-Aware Trainable Target Generation v1` 是在上述瓶颈之后的本质修正阶段。该阶段不再继续
细化“为什么 60 个 context 不能训练”的审计解释，而是把训练契约前移到候选生成与 source
selection：当原始 `top_goals[source_action_index]` 因 platform footprint 被阻塞时，opt-in
生成 same-action execution substitute。该 candidate 保持原始 policy action index，
`policy_target_cell` 仍为原目标，`execution_goal_cell` 指向默认距离 contract 内的可达 anchor，
并显式记录 `target_binding_mode=same_action_execution_substitute`、
`ppo_consumable_action=true`、`contract_safe=true` 与 `trainability_gate`。

新增产物包括
`scripts/run_anchor_projection_contract_aware_trainable_target.py`、对应 `.sh` 包装、
`configs/anchor_projection_contract_aware_trainable_target_v1.json`、
`configs/path_feedback_batch_anchor_projection_contract_aware_trainable_target_v1.json`、
`tests/test_anchor_projection_contract_aware_trainable_target.py` 与
`docs/superpowers/specs/2026-06-08-anchor-projection-contract-aware-trainable-target.md`。
目标证据根为
`outputs/path_feedback_batch_anchor_projection_contract_aware_trainable_target_v1/`。

本阶段 acceptance gate 与之前不同：`trainable_anchor_projection_count` 变大还不够，必须同时证明
`ppo_consumable_trainable_target_count > 0`，且 candidate/evidence contract 无 alignment gap、
无 fallback/open-grid、无 safety regression、无 provenance mismatch。若 same-action 目标无法在当前
action/target contract 下产生，summary 必须设置
`next_required_change=action_or_target_contract_change_required`，readiness 继续保留
`anchor_projection_nontrainable_contexts_remain`。因此该阶段仍不启动 PPO、不修改
`training.py` 主训练逻辑、不改变 network/action space/default A*，也不放宽默认 2 cells / 1.0 m
distance contract。

当前 v1 的验证口径必须保持保守：same-action substitute 能产生
`ppo_consumable_trainable_target_count=18`，且
`candidate_contract_alignment_gap_count=0`，证明当前 action mask 下存在可消费训练目标；但
`nontrainable_blocked_target_count` 仍为 60，相对 60 基线没有下降。因此主成功条件尚未满足，
summary 必须设置 `next_required_change=action_or_target_contract_change_required`，
readiness 必须继续保留 `anchor_projection_nontrainable_contexts_remain`。这表明本阶段完成了
“是否存在 PPO 可消费目标”的突破，但尚未完成“减少剩余 nontrainable blocker”的训练放行门槛。

`Planner-Validated Trainable Target Mining v1` 是上述结果之后的下一层修正。它承认“前置按可训练目标生成”
只解决候选空间问题，训练正例仍必须从 path-planner/path-feedback 的实际结果中二次筛选。因此新增 opt-in
mining/filter 层：candidate generation 仍生成 same-action substitute，source selection 仍不能被绕过；
只有 planner 反馈确认 reachable、无 replan、无 fallback/open-grid、path/risk regression 在阈值内、且
PPO action mask 可消费的 source-selected context，才可在 mining summary 中成为最终正例。

新增产物包括
`scripts/run_planner_validated_trainable_target_mining.py`、对应 `.sh` 包装、
`configs/planner_validated_trainable_target_mining_v1.json`、
`configs/path_feedback_batch_planner_validated_trainable_target_mining_v1.json`、
`tests/test_planner_validated_trainable_target_mining.py` 与
`docs/superpowers/specs/2026-06-08-planner-validated-trainable-target-mining.md`。
目标证据根为
`outputs/path_feedback_batch_planner_validated_trainable_target_mining_v1/`。

该阶段的核心输出是每个 blocked context 的单一最终裁决：
`selected_default_contract_trainable`、`selected_planner_validated_distance_exception`、
`rejected_distance_contract`、`rejected_not_source_selected`、`rejected_quality_regression`
或 `rejected_not_ppo_consumable`。默认训练 distance contract 仍为 2 cells / 1.0 m；
3 cells / 1.5 m 仅作为 opt-in planner-validated exception，不是默认 contract 放宽。若
`planner_validated_trainable_target_count > 18` 且
`nontrainable_blocked_target_count < 60`、`candidate_contract_alignment_gap_count=0`、安全/回退/
provenance gate 均通过，readiness 才能移除同一批 context 的
`anchor_projection_nontrainable_contexts_remain`。若主成功失败，summary 必须设置
`next_required_change=source_selection_or_target_contract_change_required`，不得宣称 training ready。

当前验证结果已经达到该阶段主成功门槛：新 evidence root 的 batch 为 8/8 passed，fallback/open-grid、
safety regression、current git provenance mismatch 均为 0；candidate-generation 在默认 contract 下仍为
`18 trainable + 60 nontrainable`，但 planner-validated mining summary 报告
`planner_validated_trainable_target_count=24`、
`default_contract_trainable_target_count=18`、
`planner_validated_distance_exception_count=6`、
`nontrainable_blocked_target_count=54`、
`nontrainable_blocked_target_count_delta=-6`、
`candidate_contract_alignment_gap_count=0`、`next_required_change=null`。readiness review 因此给出
`training_readiness_status=ready_for_limited_policy_training_dry_run` 与 `training_blockers=[]`。该结论只表示
已达到 limited dry-run review gate，不表示已启动 PPO 或改变默认训练/网络/action-space。

`Limited Policy Training Dry-Run Input Materialization v1` 是当前阶段的最小训练链路闭环。它不继续处理
剩余 54 个 nontrainable blocker，而是只把 mining 得到的 24 个最终正例转换成现有
`RolloutEpisode` / `RolloutTransition` JSONL，并用当前 `validate_rollout_dataset` 与
`train_policy_on_episodes` 做一次受控 dry-run。新增产物包括
`configs/planner_validated_training_input_materialization_v1.json`、
`configs/limited_policy_training_dry_run_v1.json`、
`scripts/run_planner_validated_training_input_materialization.py`、对应 `.sh` 包装、
`scripts/run_limited_policy_training_dry_run.py`、对应 `.sh` 包装、
`tests/test_limited_policy_training_dry_run_input_materialization.py` 与
`docs/superpowers/specs/2026-06-08-limited-policy-training-dry-run-input-materialization.md`。

本阶段在同一 evidence root 下生成
`planner-validated-rollout-episodes.jsonl`、
`planner-validated-training-input-materialization-summary.json`、
`planner-validated-training-exclusion-report.json` 与
`limited-policy-training-dry-run-summary.json`。当前结果为
`input_positive_count=24`、`default_contract_positive_count=18`、
`planner_validated_exception_positive_count=6`、`excluded_nontrainable_count=54`、
`invalid_action_mask_count=0`、`empty_action_mask_count=0`；
dry-run summary 为 `dry_run_status=passed`、`train_policy_sample_count=24`、
`publishes_checkpoint=false`、`performance_claimed=false`。这证明 24 个 mined 正例已经可被现有训练函数消费，
但仍不是正式 PPO 训练、不是策略性能声明，也没有改变 network/action space/default A* 或默认 distance contract。

`Counterfactual Preference Training Samples v1` 进一步处理剩余 blocker 中的
`source_selection_not_selected_count=36`。这些样本的本质不是新的 hard positive：当前证据中它们全部为
`target_binding_mode=synthetic_projection` 且 `ppo_consumable_action=false`，因此不能写入
`RolloutEpisode.action_index` 的单动作监督标签。新阶段把它们转换为“当前 selected action 优于或权衡
alternative candidate”的 pairwise preference evidence，并明确 `hard_positive_added_count=0`。

当前结果在同一 evidence root 下生成
`counterfactual-preference-training-samples.jsonl`、
`counterfactual-preference-training-summary.json`、
`counterfactual-preference-exclusion-report.json` 与
`counterfactual-preference-training-dry-run-summary.json`。36 条 not-source-selected context 已全部唯一归因：
`preference_pair_count=24`，其中
`selected_over_alternative_negative_count=12`、
`tradeoff_preference_pair_count=12`；
`rejected_binding_or_distance_required_count=12` 继续保留给后续 action binding / distance-contract 阶段。
preference dry-run 为 `preference_dry_run_status=passed`、
`preference_train_sample_count=24`、`publishes_checkpoint=false`、
`performance_claimed=false`。该阶段只验证 pairwise ranking 样本可被独立 dry-run 消费，不改变默认
PPO 训练入口、network/action space/default A* 或默认 distance contract。

`Unified Sample Taxonomy and Residual Boundary Preference v1` 将剩余仍未利用的 30 个 context 纳入统一样本体系，
但不把它们伪装成 PPO action hard positive。该阶段新增
`scripts/run_unified_policy_sample_registry.py`、对应 `.sh` 包装、
`configs/unified_policy_sample_registry_v1.json`、
`scripts/run_residual_boundary_preference_training_dry_run.py`、对应 `.sh` 包装、
`configs/residual_boundary_preference_training_dry_run_v1.json`、
`tests/test_unified_policy_sample_registry.py` 与
`docs/superpowers/specs/2026-06-08-unified-sample-taxonomy-residual-boundary-preference.md`。

统一样本分类为四类：`action_label_positive=24`，继续来自现有 `RolloutEpisode` hard-positive 链路；
`counterfactual_preference_pair=24`，来自上一阶段 source-selection not-selected 的 ranking 样本；
`boundary_negative_preference_pair=12`，来自 `npz_near_blocked_corridor` 中 3 cells / 1.5 m、但仍为
`synthetic_projection` 且 `ppo_consumable_action=false` 的边界样本；`blocked_target_negative_pair=18`，
来自 `npz_dense_rock_choke` 中 7/11 cells、3.5/5.5 m 的远距离超合同目标，并标记
`hierarchical_subgoal_required=true`。

当前 registry summary 为 `status=passed`、`reason_codes=[]`、
`action_label_positive_count=24`、`existing_preference_pair_count=24`、
`boundary_negative_preference_pair_count=12`、`blocked_target_negative_pair_count=18`、
`residual_trainable_signal_count=30`、`pairwise_preference_signal_count=54`、
`unified_context_coverage_count=78`、`hard_positive_added_count=0`。residual dry-run 为
`residual_preference_dry_run_status=passed`、`residual_train_sample_count=30`、
`publishes_checkpoint=false`、`performance_claimed=false`。因此当前结论是：78 个 blocked contexts 已有统一训练信号覆盖，
但 PPO action-label 正例仍只有 24 个；这不是正式 PPO ready，也不是策略性能提升声明。

`Hybrid Training Objective Integration v1` 在统一样本体系之上新增一个 opt-in 联合训练 dry-run。该阶段新增
`scripts/run_hybrid_policy_training_dry_run.py`、对应 `.sh` 包装、
`configs/hybrid_policy_training_dry_run_v1.json`、
`tests/test_hybrid_policy_training_dry_run.py` 与
`docs/superpowers/specs/2026-06-08-hybrid-training-objective-integration.md`，并扩展
`scripts/run_policy_training_readiness_review.py`，允许读取
`hybrid-policy-training-dry-run-summary.json`。

Hybrid dry-run 的关键边界是：24 个 hard positive 继续使用现有
`RolloutEpisode/PolicyObservation/action_index` 标签语义；54 个 preference/negative signal 只进入 pairwise ranking
loss，不被写回 PPO action label。当前 summary 为 `status=passed`、`reason_codes=[]`、
`dry_run_status=passed`、`action_label_positive_count=24`、`existing_preference_pair_count=24`、
`residual_preference_pair_count=30`、`pairwise_preference_signal_count=54`、
`hybrid_train_signal_count=78`、`hard_positive_added_count=0`、
`invalid_action_mask_count=0`、`empty_action_mask_count=0`、
`publishes_checkpoint=false`、`performance_claimed=false`。readiness review 对通过的 hybrid summary 只能记录
`hybrid_training_dry_run_completed`，不能升级为正式 PPO ready 或策略性能提升声明。

`Current-HEAD Hybrid Evidence Refresh and Readiness Closure v1` 是 Hybrid v1 之后的证据刷新阶段。旧 root
`outputs/path_feedback_batch_planner_validated_trainable_target_mining_v1/` 已证明 hybrid dry-run 可消费
24 个 action-label positive 与 54 个 pairwise preference/negative signal，但旧 readiness review 仍因
`current_git_provenance_mismatch` 失败。因此本阶段新建
`outputs/path_feedback_batch_hybrid_current_head_readiness_closure_v1/`，在当前 HEAD 下重跑 batch validation、
anchor candidate generation、anchor evidence contract、planner-validated mining、materialization、limited dry-run、
counterfactual preference、unified registry、residual dry-run、hybrid dry-run 与 readiness review。

该刷新 root 的验收口径不改变训练语义：batch `failed_count=0`、fallback/open-grid `0`、safety regression `0`、
关键 summary `status=passed`、`reason_codes=[]`、current git provenance mismatch `0`；hybrid summary 必须继续保持
`action_label_positive_count=24`、`pairwise_preference_signal_count=54`、`hybrid_train_signal_count=78`、
`hard_positive_added_count=0`、`publishes_checkpoint=false`、`performance_claimed=false`。只有这些条件同时满足时，
readiness 才能记录 `training_readiness_status=hybrid_training_dry_run_completed`。这表示当前 HEAD 的证据链已经闭合，
但仍不是正式 PPO ready、不是 checkpoint 发布、不是策略性能提升，也不改变 network/action space/default A*、
默认 distance contract 或 Ackermann 可执行性边界。

`Controlled Hybrid Policy Training Candidate v1` 是在上述 dry-run 闭环之后的受控 candidate 阶段。该阶段新增
`scripts/run_controlled_hybrid_policy_training_candidate.py`、对应 `.sh` 包装、
`configs/controlled_hybrid_policy_training_candidate_v1.json`、
`scripts/run_controlled_hybrid_policy_holdout_evaluation.py`、对应 `.sh` 包装、
`configs/controlled_hybrid_policy_holdout_evaluation_v1.json`、
`tests/test_controlled_hybrid_policy_training_candidate.py` 与
`docs/superpowers/specs/2026-06-08-controlled-hybrid-policy-training-candidate.md`，并扩展
`scripts/run_policy_training_readiness_review.py`，允许读取 controlled candidate 与 holdout evaluation summary。

该阶段的 evidence root 为
`outputs/path_feedback_batch_controlled_hybrid_policy_training_candidate_v1/`。candidate training summary 必须继续保持
`action_label_positive_count=24`、`pairwise_preference_signal_count=54`、`hybrid_train_signal_count=78`、
`hard_positive_added_count=0`；本地 checkpoint 只能标记为 `experimental=true`，并保持
`publishes_checkpoint=false`、`replaces_default_policy=false`、`performance_claimed=false`。holdout evaluation 至少报告
`action_mask_invalid_count`、`empty_action_mask_count`、`fallback_or_open_grid_count`、`safety_regression_count`、
`contract_violation_count`、`path_cost_regression_count`、`risk_regression_count`、
`source_selection_agreement_count` 与 `preference_margin_improved_count`。当 action mask、fallback/open-grid、safety 与
contract gate 均为 0，且没有 path/risk/source-selection 回退时，readiness 可记录
`controlled_hybrid_training_candidate_evaluated`；若 path/risk/source-selection 有回退，则 readiness 保持 blocked，并设置
`next_required_change=training_objective_or_sample_weight_refinement_required`。该状态仍不是正式 PPO ready、不是生产
checkpoint，也不是策略性能提升声明。

`Fresh Holdout Policy Candidate Evaluation v1` 是 controlled candidate 之后的更严格泛化检查阶段。现有 controlled
holdout 已验证本地 experimental checkpoint 的基本消费性，但它并不是严格的 fresh/disjoint 评估。因此本阶段新增
`configs/path_feedback_batch_fresh_holdout_policy_candidate_evaluation_v1.json`、
`configs/fresh_holdout_policy_candidate_evaluation_v1.json`、
`scripts/run_fresh_holdout_policy_candidate_evaluation.py`、对应 `.sh` 包装、
`tests/test_fresh_holdout_policy_candidate_evaluation.py` 与
`docs/superpowers/specs/2026-06-08-fresh-holdout-policy-candidate-evaluation.md`，并继续扩展
`scripts/run_policy_training_readiness_review.py`，允许读取 fresh holdout summary。

该阶段的 holdout evidence root 为
`outputs/path_feedback_batch_fresh_holdout_policy_candidate_evaluation_v1/`。Evaluator 复用
`outputs/path_feedback_batch_controlled_hybrid_policy_training_candidate_v1/experimental-hybrid-policy-candidate.pt`，
但只接受与 source root
`outputs/path_feedback_batch_hybrid_current_head_readiness_closure_v1/` 和 candidate root
`outputs/path_feedback_batch_controlled_hybrid_policy_training_candidate_v1/` identity disjoint 的
candidate/sample context。Identity 优先使用 context id；没有 context id 时使用
`(scenario_id, sample_type, source_action_index, policy_target_cell, execution_goal_cell, target_binding_mode)`。
场景 id 可以重叠，但只能记录为 `scenario_overlap_count`，不得宣称 scenario-level 泛化。

Fresh summary 必须输出 `fresh_disjoint_context_count > 0`，accepted samples 的 identity overlap 与 identity-key-missing
均为 0，并保持 `fallback_or_open_grid_count=0`、`safety_regression_count=0`、`contract_violation_count=0`、
path/risk/source-selection regression 均为 0，同时继续记录 `experimental_checkpoint=true`、
`publishes_checkpoint=false`、`replaces_default_policy=false`、`performance_claimed=false`。通过时 readiness 最多推进到
`fresh_holdout_policy_candidate_evaluated`。若没有 disjoint 样本，则
`next_required_change=fresh_holdout_scenario_or_candidate_generation_required`；若出现质量回退，则
`next_required_change=training_objective_or_sample_weight_refinement_required`。该阶段仍不是正式 PPO rollout、不是
production checkpoint，也不是策略性能提升声明。

`Scenario-Disjoint Context-ID Generalization Closure v1` 进一步修复 Fresh Holdout v1 暴露出的两个证据质量问题：
一是 Fresh Holdout 仍有 `scenario_overlap_count=11`，只能说明 candidate/sample identity 不重叠，不能说明场景级
泛化；二是 identity 仍可能 fallback 到临时组合键，缺少正式的一等 `context_id`。因此本阶段新增
`model-explorer/src/model_explorer/policy/context_id.py`，并在 path-feedback candidate、anchor-projection
context records、planner-validated mining/materialization、preference samples、unified registry 和 fresh reports 中保留
`policy-context-id/v1` metadata：`context_id`、`context_id_schema_version`、`context_id_source`、
`legacy_identity_fallback_used`。该 id 使用 canonical JSON + sha256，字段包括 `scenario_id`、`scenario_group`、
`scenario_seed`、`scenario_variant_id`、`diagnostic_profile`、`planning_backend`、`top_k`、sample/candidate role、
`source_action_index`、`policy_target_cell`、`execution_goal_cell`、`target_binding_mode`。缺字段不得合成伪 id，只能
记录 `context_id_missing` 并在 strict gate 中失败。

场景层面新增 `holdout` scenario set，由
`dev-platform-constraints/scripts/generate_npz_validation_maps.py` 生成独立 `npz_holdout_*` 场景、seed 和
`scenario_variant_id`，覆盖 near-blocked、high-risk trap、dense choke、path-complexity、channel contrast 等同类难度。
根目录新增 `configs/path_feedback_batch_scenario_disjoint_policy_candidate_evaluation_v1.json` 与
`configs/scenario_disjoint_policy_candidate_evaluation_v1.json`。Strict fresh evaluator 要求
`require_context_id=true`、`require_scenario_disjoint=true`、`scenario_overlap_count=0`、`identity_overlap_count=0`、
`context_id_missing_count=0`、`legacy_identity_fallback_count=0`，并要求 candidate summary/checkpoint metadata 的 git
provenance 匹配当前 checkout。通过后 readiness 才能推进到
`scenario_disjoint_policy_candidate_evaluated`；若缺 context id，则
`next_required_change=stable_context_id_contract_required`；若没有场景级 disjoint，则
`next_required_change=scenario_disjoint_holdout_generation_required`；若质量回退，则
`next_required_change=training_objective_or_sample_weight_refinement_required`。

该阶段的目标仍是严格泛化评估证据闭环，不是正式 PPO rollout。它不发布 checkpoint、不替换默认策略、不修改
network/action space/default A*、不放宽默认 distance contract、不宣称 Ackermann-feasible trajectory，也不把
IRIS/GCS/path-planner 诊断直接当作训练放行。

### 10.29 Controlled Scenario-Disjoint Policy Rollout Evaluation v1

在 `scenario_disjoint_policy_candidate_evaluated` 之后，下一层门禁不是发布策略，而是封闭测试场式的
controlled shadow rollout。根目录新增
`configs/scenario_disjoint_policy_rollout_evaluation_v1.json`、
`scripts/run_scenario_disjoint_policy_rollout_evaluation.py`、对应 `.sh` 包装与
`tests/test_scenario_disjoint_policy_rollout_evaluation.py`。该 evaluator 读取 SRC/CAND/HOLD 三个 evidence root，
加载 CAND experimental checkpoint，对 HOLD `path-feedback-summary.json` 中的 scenario-disjoint candidates 进行
policy 打分，并输出到 HOLD root：
`scenario-disjoint-policy-rollout-decisions.jsonl`、
`scenario-disjoint-policy-rollout-regression-report.json` 与
`scenario-disjoint-policy-rollout-evaluation-summary.json`。

默认配置为 `shadow_mode=true`、`controlled_selection_mode=false`。policy 的 raw top choice 会被记录为诊断，
但不会替换 planner/source selection；受控 shadow decision 仍必须通过 action mask、contract、fallback、安全、
path/risk 与 source-selection gate。若 raw policy top choice 会造成质量回退，evaluator 会记录
`raw_policy_regression_count`，但受控 decision 回退到 source-selected candidate。readiness review 新增
`scenario_disjoint_policy_rollout_evaluated` 状态：只有 rollout summary `status=passed`、`reason_codes=[]`、
`scenario_disjoint_context_count>0`、`invalid_action_mask_count=0`、`regression_count=0`，且 fallback/safety/contract/path/risk/
source-selection regression 全为 0 时才允许进入该状态。

该阶段仍是受控 shadow evaluation，不是正式 PPO rollout，不发布 checkpoint，不替换默认策略，不修改
network/action space/default A*，不放宽默认 distance contract，不宣称 Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.30 Raw Policy Decision Alignment and Objective Calibration v1

Controlled rollout 暴露出的新问题是：受控 gate 可以把退化选择挡住，但 raw policy top choice 仍可能系统性选中
regressive candidate。形象地说，上一阶段证明了“教练副刹能兜底”，本阶段要训练“学员第一反应不要踩错方向”。

本阶段新增 opt-in raw alignment 闭环：
`configs/raw_policy_regression_mining_v1.json`、
`configs/raw_policy_decision_alignment_candidate_v1.json`、
`configs/raw_policy_strict_rollout_evaluation_v1.json`、
`scripts/run_raw_policy_regression_mining.py`、对应 `.sh` 包装、
`scripts/run_raw_policy_decision_alignment_candidate.py`、对应 `.sh` 包装、
`scripts/run_raw_policy_strict_rollout_evaluation.py`、对应 `.sh` 包装，以及
`scripts/run_raw_policy_decision_alignment_closure.sh`。

Mining 阶段读取 HOLD 的 `scenario-disjoint-policy-rollout-decisions.jsonl` 与 path-feedback candidates，将
`raw_policy_decision_class=regression` 的选择转为 `raw_policy_regression_preference_pair`：
source-selected/controlled-safe candidate 是 preferred side，raw-regressive candidate 是 alternative side。样本记录
context_id、scenario/action、path/risk delta、raw/source logit 与 regression reason codes。缺 context_id、候选指标或
action mask 无效时只进 exclusion。该阶段不得写入 `RolloutEpisode`，`hard_positive_added_count=0`。

Candidate 阶段复用现有 hybrid trainer，但只在配置显式提供 raw mining summary 和 raw preference JSONL 时追加 pairwise
loss。输出 root 为 `outputs/path_feedback_batch_raw_policy_decision_alignment_candidate_v1/`，summary 为
`raw-policy-decision-alignment-candidate-summary.json`。Strict rollout 重新在 scenario-disjoint HOLD 上评估 raw top
choice，输出 `raw-policy-strict-rollout-evaluation-summary.json`；若 `raw_policy_regression_count` 未低于配置的 baseline
门槛，则 summary 失败并设置 `next_required_change=policy_objective_or_feature_refinement_required`。

readiness review 新增 `raw_policy_decision_alignment_evaluated`：只有 strict rollout `status=passed`、`reason_codes=[]`、
`raw_policy_regression_count` 低于 baseline、controlled `regression_count=0`，且 invalid action mask、fallback/open-grid、safety、
contract、path/risk 与 source-selection regression 全为 0 时才允许进入。该状态仍是 experimental shadow-only，不发布
checkpoint、不替换默认策略、不启动正式 PPO rollout、不修改 network/action space/default A*、不放宽默认 distance
contract、不宣称 Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.31 Raw Policy Generalization and Anti-Overfit Closure v1

Raw alignment 之后暴露出的风险是过拟合：old HOLD 已经参与 raw-regression preference calibration，因此它只能继续作为
dev/calibration 证据，不能再当最终泛化验收。形象地说，旧 HOLD 是做过讲解的错题本；下一阶段必须另出 TRAIN/VAL/TEST
三套隔离卷，防止模型只是背下旧错题。

本阶段新增 `raw_align_train`、`raw_align_val`、`raw_align_test` scenario sets，并提供
`configs/path_feedback_batch_raw_policy_generalization_train_v1.json`、
`configs/path_feedback_batch_raw_policy_generalization_val_v1.json`、
`configs/path_feedback_batch_raw_policy_generalization_test_v1.json`。TRAIN/dev 可写
`raw_policy_regression_preference_pair`；VAL/TEST 使用
`configs/raw_policy_regression_mining_diagnostic_v1.json` 只写 diagnostic，不能进入训练样本。

`scripts/run_raw_policy_generalization_candidate.py` 负责合并 TRAIN/dev raw preference、检查 VAL/TEST context leakage、
按 seed 训练 experimental candidate，并按 VAL raw-regression count 选择 best seed。输出 root 为
`outputs/path_feedback_batch_raw_policy_generalization_candidate_v1/`，summary 为
`raw-policy-generalization-candidate-summary.json`，同时输出 training samples、training curves 与 overfit report。

`scripts/run_raw_policy_generalization_evaluation.py` 在 DEV/VAL/TEST 上分别评估 baseline 与 aligned candidate；TEST
是唯一最终验收 split。summary `raw-policy-generalization-evaluation-summary.json` 必须报告 TEST baseline/aligned
`raw_policy_regression_count`、reduction rate、controlled regression gates、context overlap 与 `overfit_gap`。readiness
review 新增 `raw_policy_generalization_evaluated`：只有 TEST controlled regression、invalid action mask、fallback/open-grid、
safety、contract、path/risk、source-selection regression 全为 0，TEST raw regression 相比 baseline 至少下降 50%，且
`overfit_gap<=0.15` 时才升级；失败时必须归因到 data volume、objective/sample weight、feature schema 或 scenario coverage。

该阶段仍是 shadow-only experimental closure：不启动正式 PPO rollout，不发布或替换策略，不修改 network/action space/default
A*，不放宽默认 distance contract，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。

### 10.32 Policy-Gated Canary Rollout v1

Raw generalization 证明候选策略在隔离 TEST 上不再犯已知 raw regression，但这仍像“考试时照着教练答案开”。Canary
rollout 的目标是更进一步：在封闭测试场里允许 raw policy 选择 source-selected 之外的候选；只要它改选，就必须经过
action mask、reachable、no replan、no fallback/open-grid、safety、contract、path/risk 与 source-selection quality gate。
通过 gate 才记为 `canary_accepted_policy_choice`；失败则回退 source-selected 并记录拒绝原因。

本阶段新增 `policy_canary` batch 入口、`configs/path_feedback_batch_policy_gated_canary_rollout_v1.json`、
`configs/policy_gated_canary_rollout_v1.json`、`scripts/run_policy_gated_canary_rollout.py/.sh` 与
`tests/test_policy_gated_canary_rollout.py`。输出 root 为
`outputs/path_feedback_batch_policy_gated_canary_rollout_v1/`，核心产物包括
`policy-gated-canary-rollout-summary.json`、`policy-gated-canary-decisions.jsonl`、
`policy-gated-canary-rejection-report.json` 与 `policy-gated-canary-opportunity-summary.json`。

验收不是只看 controlled regression 为 0；如果 policy 一直和 source-selected 对齐，也可能只是完全依赖教练答案。
主成功门槛是 `policy_decision_count>0`、`canary_opportunity_context_count>0`、
`policy_changed_decision_count>0`、`canary_accepted_policy_choice_count>0`，且 controlled invalid action mask、
fallback/open-grid、safety、contract、path/risk、source-selection regression 全为 0。readiness review 新增
`policy_gated_canary_rollout_evaluated`，并要求 canary summary 通过且 candidate/checkpoint provenance 与当前源码状态匹配。

该阶段仍是受控试驾，不启动正式 PPO rollout，不发布或替换策略，不修改 network/action space/default A*，不放宽默认
distance contract，不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.33 Canary Diversity and Safe-Takeover Robustness v1

上一阶段 canary 的 2 次安全改选都来自 `npz_mixed_stress_detour`，只能证明“单个封闭项目里能安全换道”。本阶段把
canary 扩展为多场景族试驾：新增 `policy_canary_diversity` scenario set，覆盖
`mixed_stress_detour`、`near_blocked_safe_alt`、`high_risk_tradeoff`、`dense_choke_safe_bypass`、
`channel_contrast` 与 `path_complexity_benefit`。同时新增
`configs/path_feedback_batch_policy_gated_canary_diversity_v1.json`、
`configs/policy_gated_canary_diversity_v1.json` 与
`scripts/run_canary_diversity_safe_takeover_closure.sh`，用于刷新 clean-HEAD SRC、candidate、canary diversity 与
readiness evidence。

canary summary 新增按 scenario family 聚合的 opportunity、changed、source-aligned、accepted、rejected 与 reason counts，
并输出 `accepted_scenario_family_count`、`accepted_decision_family_distribution` 和
`canary_diversity_passed`。验收要求至少 12 个机会、至少 5 个场景族、accepted choice 覆盖至少 3 个场景族、accepted
policy choice 至少 4 个，并保持 invalid action mask、fallback/open-grid、safety、contract、path/risk 与
source-selection regression 全为 0。通过后 readiness 只能升级到
`policy_gated_canary_diversity_evaluated`，仍表示受控 canary，而非正式 PPO、策略发布或性能提升。

当前 clean-HEAD closure 已通过：`policy_decision_count=12`、`canary_opportunity_context_count=12`、
`policy_changed_decision_count=6`、`canary_accepted_policy_choice_count=6`、
`canary_rejected_policy_choice_count=0`，accepted choice 覆盖 `channel_contrast`、`mixed_stress_detour`
与 `near_blocked_safe_alt` 三个场景族。candidate 与 checkpoint metadata 均匹配当前 git provenance，
readiness 为 `policy_gated_canary_diversity_evaluated` 且 `training_blockers=[]`。

### 10.34 Canary Opportunity Quality and Multi-Family Safe Choice Expansion v1

上一阶段暴露的关键事实是：不是每个场景族都发生了安全改选。正确处理方式不是假设 policy 是随机概率没选中，也不是直接扩大
accepted 计数，而是把问题拆成两层：第一，某个场景族是否真的产生了 `canary_gate_acceptable=true` 的替代候选；第二，如果有，
raw policy 是否仍然 source-aligned，从而错过了安全替代选择。前者是 scenario/candidate generation gap，后者才是 policy
objective 或 calibration gap。

本阶段新增 `policy_canary_opportunity_quality` scenario set，每个 family 提供两个 variant，覆盖
`mixed_stress_detour`、`near_blocked_safe_alt`、`channel_contrast`、`high_risk_tradeoff`、
`dense_choke_safe_bypass` 与 `path_complexity_benefit`。新增
`configs/path_feedback_batch_policy_gated_canary_opportunity_quality_v1.json`、
`configs/policy_gated_canary_opportunity_quality_v1.json`、
`configs/canary_missed_opportunity_preference_v1.json` 与
`scripts/run_canary_opportunity_quality_closure.sh`。评估 summary 新增
`family_with_acceptable_alternative_count`、`missing_acceptable_alternative_families`、
`source_aligned_with_acceptable_alternative_count`、
`canary_missed_opportunity_preference_pair_count`、`missed_safe_choice_family_count` 与
`hard_positive_added_count=0`。

验收要求至少 24 个 canary opportunity context、至少 6 个 scenario family、至少 5 个 family 存在可接受替代、
accepted choice 至少覆盖 5 个 family、accepted policy choice 至少 8 个，并保持 invalid action mask、fallback/open-grid、
safety、contract、path/risk、source-selection 与 raw/controlled regression 全为 0。若 family 没有 acceptable alternative，
`next_required_change=canary_opportunity_generation_gap`；若存在 acceptable alternative 但 policy source-aligned，则记录
`canary_missed_opportunity_preference_pair`，只作为 preference/calibration 信号，不新增 action-label positive。
通过后 readiness 只能升级到 `policy_gated_canary_opportunity_quality_evaluated`，仍不启动正式 PPO、不发布或替换策略、不修改
network/action space/default A*、不放宽默认 distance contract，也不宣称 Ackermann-feasible trajectory 或策略性能提升。

当前 closure 已通过：`policy_decision_count=24`、`canary_opportunity_context_count=24`、
`policy_changed_decision_count=10`、`canary_accepted_policy_choice_count=10`、
`canary_rejected_policy_choice_count=0`、`scenario_family_count=6`、
`accepted_scenario_family_count=5`、`family_with_acceptable_alternative_count=5`。accepted choices 覆盖
`channel_contrast`、`high_risk_tradeoff`、`mixed_stress_detour`、`near_blocked_safe_alt` 与
`path_complexity_benefit`；`dense_choke_safe_bypass` 仍无 acceptable alternative，归类为后续
scenario/candidate opportunity-generation 问题。`source_aligned_with_acceptable_alternative_count=0`、
`canary_missed_opportunity_preference_pair_count=0`、`hard_positive_added_count=0`，所有 controlled/raw regression、
invalid action mask、fallback/open-grid、safety、contract、path/risk、source-selection regression 均为 0。
candidate/checkpoint provenance 均匹配当前源码状态，readiness 为
`policy_gated_canary_opportunity_quality_evaluated` 且 `training_blockers=[]`。

### 10.35 Dense-Choke Safe Alternative Opportunity Closure v1

`Canary Opportunity Quality` 暴露出的唯一缺口是
`dense_choke_safe_bypass`：旧 evidence 中该 family 有 4 个 context、12 个 alternative，但
`acceptable_alternative_count=0`，拒绝原因集中在 `risk_regression=12`、
`path_cost_regression=8` 与 `invalid_action_mask=4`。这不是 policy 更激进就能解决的问题，而是
dense-choke candidate/opportunity generation 没有提供“合同内、action-mask 可消费、path/risk 不回退”的真实旁路。

本阶段新增 `policy_canary_dense_choke_opportunity` scenario set，并保留受控 canary 的全部 gate：
action mask valid、reachable、no replan、no fallback/open-grid、safety=0、contract-safe、path/risk 不回退、
source-selection quality 不回退。dense-choke 专用几何保留中央 choke/rock cluster，并让 blocked source target
通过 same-action execution substitute 进入 source selection；raw policy 若选择另一条低风险旁路，必须仍通过同一套 gate，
不能靠放宽 path/risk 或 source-selection 门槛通过。

新增产物包括：

- `configs/path_feedback_batch_policy_gated_canary_full_family_opportunity_v1.json`
- `configs/policy_gated_canary_full_family_opportunity_v1.json`
- `scripts/run_dense_choke_safe_alternative_diagnosis.py/.sh`
- `scripts/run_dense_choke_safe_alternative_opportunity_closure.sh`
- `outputs/path_feedback_batch_policy_gated_canary_full_family_opportunity_v1/`
- `outputs/dense_choke_safe_alternative_visual_diagnostics_v1/index.html`

canary summary 新增 `dense_choke_acceptable_alternative_count`、
`dense_choke_accepted_policy_choice_count` 与 `canary_full_family_opportunity_passed`。readiness review 新增
`policy_gated_canary_full_family_opportunity_evaluated`；若 dense choke 仍无 acceptable alternative，则返回
`next_required_change=dense_choke_opportunity_generation_gap`；若有 acceptable alternative 但 policy 仍 source-aligned，
则问题转为 `policy_safe_choice_alignment_insufficient`，只能生成 preference/calibration 信号，不能写 action-label
hard positive。

验收要求 `scenario_family_count=6`、`family_with_acceptable_alternative_count=6`、
`accepted_scenario_family_count=6`、`dense_choke_safe_bypass.acceptable_alternative_count>0`、
`dense_choke_safe_bypass.canary_accepted_policy_choice_count>0`、`canary_accepted_policy_choice_count>=12`、
`canary_rejected_policy_choice_count=0`，并保持 controlled/raw regression、invalid action mask、fallback/open-grid、
safety、contract、path/risk、source-selection regression 全为 0。该阶段仍是受控 canary opportunity closure，不启动正式
PPO rollout，不发布或替换策略，不修改 network/action space/default A*，不放宽默认 distance contract，不宣称
Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行或声明策略性能提升。

当前 clean-HEAD closure 已通过。SRC root 为
`outputs/path_feedback_batch_dense_choke_opportunity_clean_src_v1/`，candidate root 为
`outputs/path_feedback_batch_dense_choke_opportunity_candidate_v1/`，full-family canary root 为
`outputs/path_feedback_batch_policy_gated_canary_full_family_opportunity_v1/`。最终 canary summary 为
`status=passed`、`reason_codes=[]`、`policy_decision_count=32`、`canary_opportunity_context_count=32`、
`policy_changed_decision_count=18`、`canary_accepted_policy_choice_count=18`、`canary_rejected_policy_choice_count=0`、
`scenario_family_count=6`、`family_with_acceptable_alternative_count=6`、`accepted_scenario_family_count=6`、
`dense_choke_acceptable_alternative_count=8`、`dense_choke_accepted_policy_choice_count=8`、
`canary_full_family_opportunity_passed=true`。candidate/checkpoint provenance 均匹配当前源码；controlled/raw regression、
invalid action mask、fallback/open-grid、safety、contract、path/risk、source-selection regression 均为 0。
readiness 已升级为 `policy_gated_canary_full_family_opportunity_evaluated`，`training_blockers=[]`。

### Current-HEAD Evidence Refresh + Canary Value/Stability Evaluation v1

上一阶段已经证明 policy 能在 6 个 canary family 中做出安全改选，但这只回答了“能不能安全换道”。本阶段继续追问：
这种安全换道是否足够稳定，是否跨 family 出现，以及是否真的带来 path/risk/utility 价值，而不是只是换到一条等价路线。

本阶段先刷新当前 HEAD 下的 full-family canary evidence，要求 `current_git_provenance_mismatch=0`、
`training_readiness_status=policy_gated_canary_full_family_opportunity_evaluated` 且
`training_blockers=[]`。只有这个基线恢复后，才进入 value/stability closure。

新增 `policy_canary_value_stability` scenario set：6 个 family，每类 6 个几何 variant，共 36 个场景；batch
同时跑 `astar` 与 `channel_aware_astar`，形成 72 个 canary opportunity context。新增产物包括：

- `configs/path_feedback_batch_policy_gated_canary_value_stability_v1.json`
- `configs/policy_gated_canary_value_stability_v1.json`
- `scripts/run_canary_value_stability_closure.sh`
- `outputs/path_feedback_batch_value_stability_clean_src_v1/`
- `outputs/path_feedback_batch_value_stability_candidate_v1/`
- `outputs/path_feedback_batch_policy_gated_canary_value_stability_v1/`

canary summary 新增 `accepted_equal_choice_count`、`accepted_better_choice_count`、
`accepted_better_family_count`、`policy_change_rate`、`accepted_choice_rate`、
`accepted_value_delta_summary`、`family_value_stability_summary` 与
`canary_value_stability_passed`。`accepted_better` 必须先是
`canary_accepted_policy_choice`，并且没有任何 gate regression；随后还要满足
`path_cost_delta<=-0.25`、`risk_delta<=-0.01` 或 `utility_delta>=0.005` 之一。

验收要求 `scenario_family_count=6`、`canary_opportunity_context_count>=72`、
`family_with_acceptable_alternative_count=6`、`accepted_scenario_family_count=6`、
`canary_accepted_policy_choice_count>=24`、`canary_rejected_policy_choice_count=0`、
`accepted_better_choice_count>=8`、`accepted_better_family_count>=3`，dense choke accepted count 大于 0，
并保持 controlled/raw regression、invalid action mask、fallback/open-grid、safety、contract、path/risk、
source-selection regression 全为 0。通过后 readiness 只能升级到
`policy_gated_canary_value_stability_evaluated`。

若 safe opportunity 不足，summary 必须返回
`next_required_change=canary_value_opportunity_generation_gap`；若 safe accepted 足够但 better 不足，则返回
`next_required_change=policy_value_alignment_or_objective_refinement_required`。本阶段仍是受控 canary evidence：
不启动正式 PPO rollout，不发布或替换策略，不修改 network/action space/default A*，不放宽默认 distance contract，
不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。

### 10.37 Policy-Gated Sequential Canary Rollout v1

`Canary Value/Stability` 已经证明 policy 在 6 类 canary family 中能做出安全且有价值的单步改选，但它仍是“逐题考试”：
每个 context 独立出现，上一题的选择不会影响下一题。本阶段把问题推进到短序列：一个 episode 内连续 3 步，每一步的
`start_cell` 必须来自上一 step 的 `controlled_execution_goal_cell`。如果 policy choice 通过 action mask、reachable、no
replan、no fallback/open-grid、safety=0、contract-safe、path/risk/source-selection 不回退等 gate，下一步从
policy-selected execution goal 开始；如果不通过，则回退 source-selected execution goal，并记录 rejection。

本阶段新增 explicit scenario spec 输入，允许 NPZ validation map 生成器复用已有 `policy_canary_value_stability` 几何模板，
但覆盖 `scenario_id`、`scenario_group`、`scenario_seed`、`scenario_variant_id` 与 `start_cell`。这保证 sequential canary
不是把 108 个独立 context 分组假装 episode，而是每个 step 的输入状态由上一 step 的受控输出决定。

新增产物包括：

- `configs/policy_gated_sequential_canary_rollout_v1.json`
- `scripts/run_policy_gated_sequential_canary_rollout.py/.sh`
- `scripts/run_policy_gated_sequential_canary_closure.sh`
- `outputs/path_feedback_batch_policy_gated_sequential_canary_rollout_v1/`

summary schema 为 `policy-gated-sequential-canary-rollout-summary/v1`，核心指标包括 `episode_count`、`step_count`、
`completed_episode_count`、`policy_takeover_step_count`、`accepted_takeover_step_count`、`accepted_better_step_count`、
`multi_step_accepted_episode_count`、`family_with_multi_step_accepted_episode_count`、`accepted_takeover_family_count`、
`state_continuity_violation_count`、`episode_fallback_count` 与累计 safety/contract/path/risk/source-selection regression。
readiness review 新增 `policy_gated_sequential_canary_rollout_evaluated`；若连续性破坏，返回
`next_required_change=sequential_canary_state_continuity_required`；若有累计回归，返回
`next_required_change=sequential_canary_cumulative_regression_requires_policy_refinement`。

验收要求 36 个 episode、108 个 step、至少 30 个 completed episode、至少 24 个 accepted takeover step、
accepted takeover 覆盖 6 个 family、至少 12 个 multi-step accepted episode、`state_continuity_violation_count=0`、
`episode_fallback_count=0`、`canary_rejected_policy_choice_count=0`，并保持 invalid action mask、fallback/open-grid、
safety、contract、path/risk、source-selection regression 全为 0。该阶段仍是 cell-level sequential canary，不启动正式
PPO rollout，不执行 PPO 参数更新，不发布或替换策略，不修改 network/action space/default A*，不放宽默认 distance
contract，不宣称 Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.38 Sequential Safe-Choice Calibration and Hard-Negative Refinement v1

`Policy-Gated Sequential Canary Rollout v1` 的失败不是 runner 失败：36 个 episode / 108 个 step 全部完成，
`state_continuity_violation_count=0`，`episode_fallback_count=0`。真正暴露的是 policy 在连续短序列中仍会做出少量
path/risk 回归选择：当前 evidence 中有 2 个 rejected policy choice，导致
`cumulative_path_cost_regression_count=2`、`cumulative_risk_regression_count=1`，并且 multi-step accepted episode 覆盖不足。

本阶段不放宽 sequential gate，也不进入正式 PPO。新增 `Sequential Safe-Choice Calibration` 后，训练信号被拆成两类非
action-label preference：第一类是 rejected sequential step 的 hard-negative preference，preferred side 是
source-selected 或 controlled-safe choice，alternative side 是造成 path/risk regression 的 raw/policy choice；第二类是
source-aligned step 中仍存在 gate-safe 且更优 alternative 时的 missed-safe-choice preference，用于避免策略过度保守。
两类样本继续使用 pairwise preference loss，不写 RolloutEpisode hard positive，`hard_positive_added_count=0`。

新增产物包括：

- `configs/sequential_canary_failure_mining_v1.json`
- `configs/sequential_safe_choice_calibration_candidate_v1.json`
- `scripts/run_sequential_canary_failure_mining.py/.sh`
- `scripts/run_sequential_safe_choice_calibration_candidate.py/.sh`
- `scripts/run_sequential_safe_choice_calibration_closure.sh`
- `outputs/path_feedback_batch_policy_gated_sequential_safe_choice_rollout_v1/`

closure 先刷新 SRC/BASE/DEV/TRAIN/VAL/TEST evidence，再从
`outputs/path_feedback_batch_policy_gated_sequential_canary_rollout_v1/` mining sequential preference，训练新的
experimental candidate，并用同一 36 episode / 108 step sequential gate 复评。当前实证结果是：mining 产生 2 条
hard-negative preference 和 6 条 missed-safe-choice preference，`hard_positive_added_count=0`；balanced candidate 复评后
`policy_takeover_step_count=28`、`accepted_takeover_step_count=28`、`accepted_better_step_count=28`、`accepted_takeover_family_count=6`，
并且 `canary_rejected_policy_choice_count=0`、`state_continuity_violation_count=0`、`episode_fallback_count=0`、
path/risk/source-selection 等累计 regression 全为 0。

但本阶段不能升级 readiness：multi-step gate 仍未通过，
`multi_step_accepted_episode_count=6`、`family_with_multi_step_accepted_episode_count=2`，低于 12/6 的验收要求。进一步诊断显示，
balanced 复评中大量 source-aligned 后续 step 并不存在 gate-safe-better alternative；问题已从“策略会选出回归动作”转为
“短序列后续 step 的可接管机会分布不足”。summary 的下一步归因为
`sequential_opportunity_distribution_gap_requires_more_episodes`，下一阶段应优先扩展/重构 sequential multi-step opportunity，而不是放宽
gate 或进入正式 PPO。

验收要求 mining 至少产生 2 条 sequential hard-negative preference、`hard_positive_added_count=0`、VAL/TEST
context leakage 为 0；新 candidate 保持 `experimental_checkpoint=true`、不发布、不替换默认策略、不声明性能；新 sequential
rollout 必须保持 `state_continuity_violation_count=0`、`episode_fallback_count=0`、`canary_rejected_policy_choice_count=0`，
并且 invalid action mask、fallback/open-grid、safety、contract、path/risk、source-selection regression 全为 0；同时还必须满足
multi-step coverage 门槛。当前已满足安全/回归门槛，但未满足 multi-step coverage，因此 readiness 继续 blocked。本阶段仍不启动
正式 PPO rollout，不执行 PPO 参数更新，不修改 network/action space/default A*，不放宽默认 distance contract，不宣称
Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.39 Sequential Multi-Step Opportunity Generation v1

`Sequential Safe-Choice Calibration` 已经把连续短序列中的 policy 回归压住：36 个 episode / 108 个 step 全部完成，
`state_continuity_violation_count=0`、`episode_fallback_count=0`、`canary_rejected_policy_choice_count=0`，
path/risk/safety/contract/source-selection regression 全为 0，并且 6 类 family 都有 accepted takeover。但 readiness 仍未通过，
因为 `multi_step_accepted_episode_count=6`、`family_with_multi_step_accepted_episode_count=2`，低于 12/6 的覆盖要求。

这个问题的本质不是“policy 还会选错”，而是要先区分两种完全不同的原因：第一，后续 step 本身没有
gate-safe 且更优的 alternative，这属于场景/机会生成不足；第二，后续 step 存在 gate-safe-better alternative，但 policy
仍然 source-aligned，这才属于训练目标或 sample weight 问题。将两者混在一起会导致错误推进：如果没有客观机会，继续训练
只会逼 policy 选择不存在的好答案；如果有机会但 policy 没选，才应生成 missed-safe-choice preference 继续校准。

本阶段新增 `policy_canary_sequential_multi_step_opportunity` scenario set，覆盖 6 类 family，每类 6 个几何 variant。该场景集
使用新的 `npz_canary_sequential_multi_step_opportunity_*` template id、seed 和 variant id，并通过真实几何扰动产生连续机会链，
而不是只换 label。`run_policy_gated_sequential_canary_rollout.py` 已扩展为配置驱动：`generation.scenario_set` 控制
explicit spec 的 scenario set，`generation.template_scenario_id_prefix` 控制 episode template id，默认仍兼容旧
`policy_canary_value_stability`。

新增 diagnosis 工具：

- `configs/sequential_multi_step_opportunity_diagnosis_v1.json`
- `scripts/run_sequential_multi_step_opportunity_diagnosis.py/.sh`
- `sequential-multi-step-opportunity-diagnosis-summary.json`
- `sequential-multi-step-opportunity-diagnostics.jsonl`
- `sequential-multi-step-opportunity-exclusion-report.json`

diagnosis 对每个 episode/step 输出机会漏斗：alternative、action-mask valid、reachable、no replan、no fallback/open-grid、
contract-safe、path/risk non-regressive、source-selection non-regressive、better alternative 与 safe-better alternative。输出分类包括
`opportunity_missing`、`policy_missed_existing_opportunity`、`policy_used_existing_opportunity` 与
`policy_rejected_existing_opportunity`。

新增 closure：

- `configs/path_feedback_batch_sequential_multi_step_opportunity_v1.json`
- `configs/policy_gated_sequential_multi_step_opportunity_rollout_v1.json`
- `scripts/run_sequential_multi_step_opportunity_closure.sh`
- `outputs/path_feedback_batch_sequential_multi_step_opportunity_clean_src_v1/`
- `outputs/path_feedback_batch_sequential_multi_step_opportunity_candidate_v1/`
- `outputs/path_feedback_batch_policy_gated_sequential_multi_step_opportunity_preflight_v1/`
- `outputs/path_feedback_batch_policy_gated_sequential_multi_step_opportunity_rollout_v1/`

closure 先刷新 SRC/CAND，再用 preflight sequential root 运行 opportunity diagnosis。若 opportunity diagnosis 已通过但 policy
仍漏选或拒绝 safe-better alternative，closure 可复用现有 sequential failure/missed-safe-choice mining 再校准 candidate，然后在
final rollout root 重跑同一 gate；若 diagnosis 本身失败，则必须先修场景生成，不得通过训练或放宽 gate 掩盖问题。preflight 与
final root 分离的原因是：policy 校准后可能改变后续 episode 状态，不能让这种 policy-induced state distribution 反向污染
“场景是否客观存在连续机会”的验收。

readiness 新增 `policy_gated_sequential_multi_step_opportunity_evaluated`。通过条件是：diagnosis 至少 12 个 multi-step opportunity
episode、6 类 family 均覆盖、每类至少 2 个 step0+step1 multi-step opportunity episode、至少 24 个 safe-better opportunity
step、0 exclusion；最终 sequential rollout 至少 24 个 accepted takeover step、至少 12 个 multi-step accepted episode、6 类 family
均覆盖、0 rejected choice、0 state continuity violation、0 episode fallback，并保持 invalid action mask、fallback/open-grid、safety、
contract、path/risk、source-selection regression 全为 0。

本阶段仍是短序列 opportunity/canary evidence，不启动正式 PPO rollout，不执行 PPO 参数更新，不发布或替换 checkpoint，不改
network/action space/default A*，不放宽默认 distance contract，不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner
诊断当训练放行。

修复后的 closure 已达到验收。preflight opportunity root 为
`outputs/path_feedback_batch_policy_gated_sequential_multi_step_opportunity_preflight_v1/`，diagnosis summary 为 `status=passed`、
`reason_codes=[]`、`episode_count=36`、`step_count=108`、`safe_better_alternative_step_count=54`、
`multi_step_opportunity_episode_count=16`、`family_with_multi_step_opportunity_count=6`。每类 multi-step opportunity episode 计数为：
`channel_contrast=3`、`dense_choke_safe_bypass=2`、`high_risk_tradeoff=3`、`mixed_stress_detour=3`、
`near_blocked_safe_alt=2`、`path_complexity_benefit=2`。

final rollout root 为 `outputs/path_feedback_batch_policy_gated_sequential_multi_step_opportunity_rollout_v1/`，summary 为
`status=passed`、`reason_codes=[]`、`episode_count=36`、`step_count=108`、`policy_takeover_step_count=36`、
`accepted_takeover_step_count=36`、`accepted_better_step_count=36`、`accepted_takeover_family_count=6`、
`multi_step_accepted_episode_count=12`、`family_with_multi_step_accepted_episode_count=6`、
`canary_rejected_policy_choice_count=0`、`invalid_action_mask_count=0`，累计 path/risk regression 均为 0。readiness 已升级为
`policy_gated_sequential_multi_step_opportunity_evaluated`，`training_blockers=[]`。

这轮结果说明连续机会生成和受控短序列接管已经闭环，但它仍是 canary/shadow evidence；不能把该阶段解读为正式 PPO rollout
放行、checkpoint 发布或策略性能声明。

### 10.40 PPO Rollout Contract and Collector Dry-Run v1

`policy_gated_sequential_multi_step_opportunity_evaluated` 之后，下一道边界不应是直接执行 PPO 参数更新，而是先证明
PPO collector 能从受控 sequential canary 中采集合法训练输入。形象地说，前一阶段证明了学员能在封闭场地连续安全接管几步；
本阶段安装“训练行车记录仪”，确认每一步的 observation、action mask、controlled action、log_prob、value、reward 与 gate metadata
都能被稳定记录，并能被现有 rollout IO 与 dataset validator 消费。

本阶段新增 evidence consistency preflight：

- `configs/sequential_evidence_consistency_v1.json`
- `scripts/run_sequential_evidence_consistency_check.py/.sh`
- `sequential-evidence-consistency-summary.json`

该 preflight 检查 sequential final rollout、opportunity diagnosis 与 readiness 是否指向一致的证据边界。当前 final rollout 已
`status=passed`，而同 root 的 diagnosis summary 仍记录旧的 failed preflight 口径，因此配置中明确 `diagnosis_role=preflight_only`；
它不再覆盖 final rollout/readiness 的通过结论。若未来 diagnosis 被声明为 final gate，则 final rollout passed + diagnosis failed
必须失败，并设置 `next_required_change=sequential_evidence_consistency_required`。

新增 PPO collector dry-run：

- `configs/ppo_rollout_collector_dry_run_v1.json`
- `scripts/run_ppo_rollout_collector_dry_run.py/.sh`
- `scripts/run_ppo_rollout_collector_closure.sh`
- 输出 root：`outputs/path_feedback_batch_ppo_rollout_collector_dry_run_v1/`
- 输出文件：`ppo-rollout-episodes.jsonl`、`ppo-rollout-transitions.jsonl`、
  `ppo-rollout-collector-summary.json`、`ppo-rollout-rejection-report.json`、
  `ppo-rollout-reward-audit.json`

collector 继续使用现有 `RolloutEpisode/RolloutTransition`，不新增 PPO 输入格式。只有
`controlled_choice_source=policy`、`canary_gate_passed=true`、且无 raw/controlled/canary regression reason 的 step 才会进入
`ppo-rollout-episodes.jsonl` 并标记 `ppo_trainable=true`；`source_fallback` step 只进入 transition/rejection diagnostic，不写入
on-policy PPO 正样本。由于 path-feedback 中同一 source action 可能同时存在 blocked 原目标与 same-action substitute，collector
按 PPO action index 槽位重建 observation，并保留 `context_id`、`episode_id`、`step_index`、`scenario_family`、
raw/source/controlled action、execution goal、path/risk delta 与 reward components。

当前真实 dry-run 已能从 sequential multi-step root 物化 36 个 PPO-trainable transition：
`ppo_trainable_transition_count=36`，`source_fallback_trainable_count=0`，`invalid_action_mask_count=0`，
`empty_action_mask_count=0`，`missing_log_prob_count=0`，`missing_value_count=0`，`non_finite_reward_count=0`。输出的
`ppo-rollout-episodes.jsonl` 可由现有 `rollout_io.read_rollout_episodes` 回读，并通过 `validate_rollout_dataset`。

readiness review 已扩展为可消费 `ppo-rollout-collector-summary/v1`，通过后可升级到
`training_readiness_status=ppo_rollout_collector_dry_run_evaluated`。clean-HEAD closure 会先刷新 upstream sequential SRC/CAND/SEQ
evidence，再复制到 PPO collector roots 并物化 collector dataset；显式 `--validate-only` 已能在匹配 provenance 下返回
`status=passed`、`reason_codes=[]`、`training_blockers=[]`、`recommended_next_action=ppo_rollout_collector_dry_run_evaluated`。
该阶段仍不是正式 PPO rollout，不执行 PPO optimizer update，不发布或替换 checkpoint，不改 network/action space/default A*，不放宽
distance contract，不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.41 Limited PPO Update Smoke v1

`ppo_rollout_collector_dry_run_evaluated` 证明了受控 sequential canary 中的 policy takeover step 可以被记录成现有
`RolloutEpisode/RolloutTransition`，并且 action mask、log_prob、value、reward、state continuity 与 gate metadata 都能通过
collector contract。下一步不是正式 PPO rollout，而是一次极小的 optimizer smoke：从采集这些 transition 的同一个
experimental checkpoint 出发，只用 collector 中 `ppo_trainable=true` 且 `controlled_choice_source=policy` 的 transition 做一次
full-batch PPO 参数更新，然后把更新后的 experimental policy 放回 raw/generalization、sequential canary、collector 和 readiness
gate 中复查。

新增产物包括：

- `configs/limited_ppo_update_smoke_v1.json`
- `scripts/run_limited_ppo_update_smoke.py/.sh`
- `scripts/run_limited_ppo_update_smoke_closure.sh`
- `outputs/path_feedback_batch_limited_ppo_update_smoke_v1/`

smoke runner 不复用默认随机初始化的训练入口。它必须加载 base candidate checkpoint 的 `model_state_dict` 或 `state_dict`，
校验 observation feature schema、action count、context id、old log-prob/value 与 collector transition 一致；若重算误差超过
`1e-4`，说明 transition 不是由该 checkpoint 采集，必须失败并返回 `ppo_update_not_on_collector_policy`。PPO update 使用
`seed=0`、`epochs=1`、`learning_rate=1e-5`、`clip_ratio=0.2`、`discount_factor=0.99`、`max_grad_norm=1.0`；advantages 使用
discounted return minus old value。source-fallback transition 只保留为 diagnostic，不进入 optimizer。

输出包括 `limited-ppo-update-smoke-summary.json`、`limited-ppo-update-training-curves.json`、
`limited-ppo-update-diagnostics.json`、`experimental-hybrid-policy-candidate.pt`、
`experimental-hybrid-policy-candidate-metadata.json` 与 `raw-policy-generalization-candidate-summary.json`。summary 必须报告
`input_ppo_trainable_transition_count`、`optimizer_train_transition_count`、`source_fallback_trainable_count`、
`old_log_prob_max_abs_error`、`old_value_max_abs_error`、loss/grad/reward/return/advantage finite 检查、`parameter_l2_delta`、
`approx_kl` 与 `max_grad_norm_after_clip`。

readiness review 新增 `--limited-ppo-update-smoke-summary` 和状态
`limited_ppo_update_smoke_evaluated`。只有 smoke summary passed，更新后 raw generalization TEST raw regression 仍为 0，
sequential canary 仍满足 36 episode / 108 step、6 类 family、0 rejected choice 和全部 regression gate 为 0，updated collector
仍能物化足够 trainable transition 且 mask/log_prob/value/reward/state continuity 全部有效时，才允许升级 readiness。

该阶段仍是本地 smoke：不启动正式 PPO rollout，不发布或替换 checkpoint，不改 network/action space/default A*，不放宽
distance contract，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。

### 10.42 Iterative PPO Mini-Loop Stability v1

`limited_ppo_update_smoke_evaluated` 只证明“一次极小 PPO update 没把系统弄坏”。下一阶段要验证的是最小闭环的稳定性：
从更新后的 experimental checkpoint 出发，连续 3 轮执行
`sequential canary -> PPO collector -> limited PPO update -> raw/sequential/collector gate`。形象地说，前一阶段是在教室里
用不少于 24 条、当前为 36 条的记录做一次非常小的改作业；本阶段要确认连续改三次作业后，学员没有因为累计微调而重新犯安全、合同或路径风险错误。

新增产物包括：

- `configs/iterative_ppo_mini_loop_stability_v1.json`
- `configs/iterative_ppo_update_step_v1.json`
- `scripts/run_iterative_ppo_mini_loop_stability.py/.sh`
- `scripts/run_iterative_ppo_mini_loop_stability_closure.sh`
- `outputs/path_feedback_batch_iterative_ppo_mini_loop_stability_v1/`

每轮必须保持严格 on-policy：collector transition 中的 old `log_prob` 与 `value` 必须由同轮 base candidate 重算匹配，
source-fallback transition 不得进入 optimizer。每轮 update 仍使用 full-batch、`epochs=1`、`learning_rate=1e-5`、
`clip_ratio=0.2`、`max_grad_norm=1.0` 的 limited PPO step。summary 输出
`iterative-ppo-mini-loop-stability-summary.json`、`iterative-ppo-mini-loop-rounds.jsonl`、
`iterative-ppo-mini-loop-drift-report.json` 与 `iterative-ppo-mini-loop-rejection-report.json`，记录每轮 trainable transition
count、`approx_kl`、`parameter_l2_delta`、raw TEST regression、sequential rejection、collector regression 与累计漂移。

readiness review 新增 `--iterative-ppo-mini-loop-stability-summary` 和状态
`iterative_ppo_mini_loop_stability_evaluated`。通过条件是 3 轮全部完成、每轮 optimizer transition 不少于 24、old log-prob/value
误差不超过 `1e-4`、loss/grad/reward/return/advantage 全部 finite、`max_abs_approx_kl<=0.25`、
`cumulative_parameter_l2_delta<=0.05`，且每轮 post-update raw generalization、sequential canary 与 collector gate 全部保持 0
regression。

该阶段仍不是正式 PPO rollout：不发布或替换 checkpoint，不改 network/action space/default A*，不放宽 distance contract，不宣称
Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。

### 10.43 Guarded PPO Rollout Pilot v1

当前 HEAD 的 quasi-real iterative mini-loop 达到 `iterative_ppo_mini_loop_stability_evaluated` 后，下一阶段新增
Quasi-Real Guarded PPO Rollout Pilot，把“rollout 入口本身”作为被测对象：policy 在每个 step 先提出 action，系统仍用现有
gate 判断是否允许执行；只有 train split、`controlled_choice_source=policy` 且通过全部 gate 的 step 才能进入 PPO trainable
transition。形象地说，前一阶段证明
“学员连续改三次作业没有退步”，本阶段开始让学员在带副刹的正式试驾入口里逐步操作，并检查记录、更新和复考是否闭环。

新增产物包括：

- `configs/guarded_ppo_rollout_pilot_v1.json`
- `configs/guarded_ppo_rollout_update_v1.json`
- `scripts/run_guarded_ppo_rollout_pilot.py/.sh`
- `scripts/run_guarded_ppo_rollout_pilot_closure.sh`
- `outputs/path_feedback_batch_guarded_ppo_rollout_pilot_v1/`

pilot 默认使用现有 6 类 sequential multi-step opportunity 场景，保持 36 episode / 108 step 规模与上一阶段可比，并在 update
后显式复查 quasi-real teacher-following 与 quasi-real collector。每步 gate 不放宽：action mask valid、candidate present、
reachable、no replan、no fallback/open-grid、contract safe、path/risk/source-selection no regression。source fallback、
raw policy probe rejected choice、validation/test transition 只能作为 diagnostic，不能被标为 `ppo_trainable=true`，也不能进入 optimizer。

输出包括 `guarded-ppo-rollout-episodes.jsonl`、`guarded-ppo-rollout-transitions.jsonl`、
`guarded-ppo-rollout-reward-audit.json`、`guarded-ppo-rollout-update-summary.json`、
`guarded-ppo-rollout-rejection-report.json` 与 `guarded-ppo-rollout-pilot-summary.json`。pilot 会从同一 base checkpoint
重算 old log-prob/value，执行一次 `epochs=1`、`learning_rate=1e-5`、`clip_ratio=0.2`、`max_grad_norm=1.0` 的极小 PPO update，
然后复查 raw generalization、generated sequential canary、generated collector、quasi-real teacher-following 与 quasi-real collector
gate。raw probe rejection 会被记录为诊断；只有 controlled rollout 的安全、合同、path/risk/source-selection 回归才阻塞 pilot。

readiness review 新增 `--guarded-ppo-rollout-pilot-summary` 和状态
`guarded_ppo_rollout_pilot_evaluated`。通过条件是 pilot summary passed、至少 24 个 PPO-trainable transition、source-fallback
trainable 为 0、mask/log-prob/value/reward/state continuity 全有效、post-update raw TEST regression 仍为 0、post-update sequential
和 collector controlled regression 全为 0、post-update quasi-real teacher agreement 不低于 0.9、quasi-real collector trainable
transition 不低于 24，且所有 provenance 匹配当前 HEAD。

该阶段仍不是发布级 PPO：不发布或替换 checkpoint，不改 network/action space/default A*，不放宽 distance contract，不宣称
Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。

### 10.43.1 Training Progress Telemetry & Progress Bar v1

`Quasi-Real Guarded PPO Rollout Pilot v1` 闭合后，新的工程痛点不是 PPO 数值，而是长 closure 的可观测性：iterative
precondition、generated sequential path-feedback、compatibility replay、collector、PPO update 和 readiness 都可能连续几分钟
没有新的 stdout。`Training Progress Telemetry & Progress Bar v1` 给这些训练/验证链路增加一层“显示屏”，但不改变训练、
reward、gate、checkpoint 或 readiness 语义。

新增产物包括：

- `configs/training_progress_telemetry_v1.json`
- `scripts/training_progress.py`
- `training-progress-events.jsonl`
- `training-progress-summary.json`

相关入口新增 `--progress auto|plain|jsonl|off`。plain 进度写 stderr，现有 JSON stdout 保持机器可读；结构化事件写到
output root，记录 `run_id`、`stage`、`status`、`current/total`、`round_index`、`step_index`、`elapsed_seconds`、
`summary_path`、`reason_codes` 与 `metrics`。guarded pilot closure 的 progress artifacts 写入
`outputs/path_feedback_batch_guarded_ppo_rollout_pilot_v1/`。

该层覆盖粗粒度 closure stage，也覆盖关键内层信号：`round 2/3`、`sequential step 2/3`、`ppo epoch 1/1`、trainable 与
diagnostic transition 数、raw rejected policy choice、controlled regression、loss、approx KL、grad norm、parameter delta 以及
finite/non-finite counters。失败时 summary 会指向最后失败 stage、reason codes 和 recommended debug artifact。

非目标保持严格：不扩大 PPO batch，不启动 formal training，不发布或替换 checkpoint，不改 network/action space/default A*，
不放宽 safety/contract/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner
诊断当训练放行。

### 10.43.2 Guarded PPO Evidence Freeze & Reproducible Closure v1

`Training Progress Telemetry` 通过后，项目进入“证据封样”阶段。形象地说，前面已经完成一次带护栏试驾，并且仪表盘全程有记录；
本阶段不是再踩一次油门，而是把这次试驾的行车记录、检验单、车辆编号和最终放行理由封进同一个证据盒，防止旧 summary 或旧
readiness 文件混进最终判断。

新增产物包括：

- `configs/guarded_ppo_evidence_freeze_v1.json`
- `scripts/run_guarded_ppo_evidence_freeze.py/.sh`
- `outputs/path_feedback_batch_guarded_ppo_evidence_freeze_v1/`
- `guarded-ppo-evidence-freeze-summary.json`
- `evidence-manifest.json`
- `readiness-final.json`
- `progress-consistency-report.json`
- `reproducibility-report.md`

freeze 从 guarded pilot root 读取 `guarded-ppo-rollout-pilot-summary.json`、`training-progress-summary.json` 与
`training-progress-events.jsonl`，再显式以 `--guarded-ppo-rollout-pilot-summary` 运行 readiness validate-only。最终 summary
记录 git provenance、输入/输出路径、关键 transition 计数、progress event 计数、failed stage 计数、readiness status 与 reason
codes。manifest 对所有 required artifact 写入路径、存在性、schema/status 摘要和 sha256。consistency report 会标出旧
`policy-training-readiness-review-summary.json` 是否与显式 guarded readiness 不一致；如果存在漂移，只作为诊断，不允许覆盖最终
`guarded_ppo_rollout_pilot_evaluated` 结论。

验收口径是：guarded pilot summary 仍 `passed` 且 `reason_codes=[]`，progress summary 仍 `passed` 且
`failed_stage_count=0`，readiness final 为 `guarded_ppo_rollout_pilot_evaluated` 且 `training_blockers=[]`，manifest 中所有
required artifacts 存在且 sha256 非空。

该阶段只做证据固化和一致性审计：不启动新的 PPO update，不扩大 batch，不发布 checkpoint，不替换 default policy，不声明 formal
training ready 或性能提升，不修改 network/action space/default A*，不放宽 safety/contract/path-risk/source-selection gates，也
不宣称 Ackermann-feasible trajectory。

### 10.44 Policy Training CUDA Device Support v1

`guarded_ppo_rollout_pilot_evaluated`、progress telemetry 和 evidence freeze 已经证明 guarded pilot 入口可以完成受控
rollout、collector、一次极小 PPO update、post-update gate 复查，并能被封装成可复现证据包。后续若进入计算设备扩展，目标不是
扩大 PPO 或接入真实地图，而是补齐训练计算设备契约：让后续更大样本训练可以显式选择 CPU/GPU，同时保证旧配置、旧 evidence 默认仍走
CPU。

新增产物包括：

- `configs/policy_training_cuda_device_support_v1.json`
- `scripts/run_policy_training_cuda_device_support_smoke.py/.sh`
- `outputs/path_feedback_batch_policy_training_cuda_device_support_v1/`

训练配置新增 `training.device`，取值为 `cpu`、`cuda` 或 `auto`。默认仍为 `cpu`；`auto` 在 CUDA 可用时使用 GPU，不可用时回退
CPU 并记录 `fallback_to_cpu=true`；显式 `cuda` 在 CUDA 不可用时必须失败，返回 `cuda_requested_but_unavailable`。summary 记录
`requested_device`、`resolved_device`、`cuda_available`、`cuda_device_name` 与 `fallback_to_cpu`。

实现上，`train_policy_on_episodes` 和 limited PPO update smoke 都会把 network、PPO batch、old log-prob/value 重算、loss、grad
和 KL 计算放在同一 resolved device 上。保存 checkpoint 前会把 state dict 转回 CPU，保证 artifact 可跨机器用
`map_location="cpu"` 回读。guarded pilot 与 iterative mini-loop 本身不直接训练，但会把 update step 的 device provenance 提升到
summary，便于后续排查是否发生 CPU fallback。

readiness review 新增 `--policy-training-cuda-device-support-summary` 和状态
`policy_training_cuda_device_support_evaluated`。通过条件是 device smoke summary passed、optimizer transition 不少于 24、old
log-prob/value 误差不超过 `1e-4`、loss/grad/reward/return/advantage 全部 finite、`parameter_l2_delta>0`、
`abs(approx_kl)<=0.25`、`max_grad_norm_after_clip<=1.0`，且 checkpoint 可 CPU 回读。

该阶段只解决训练张量计算设备问题。GPU 支持不会加速 path planner、NPZ 生成或 evidence 生成的主要耗时；也不接入真实地图、不发布或替换
checkpoint、不修改 network/action space/default A*、不放宽 distance/path-risk/source-selection contract、不宣称
Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.45 Quasi-Real Map Domain Gap Evaluation v1

`policy_training_cuda_device_support_evaluated` 之后，训练链路已经能在生成场景里完成 guarded pilot、小 PPO update 与 GPU smoke。下一阶段的主要风险不再是
“能不能训练”，而是“生成训练场和真实/准真实月面地图切片是否同分布”。本阶段新增 quasi-real map domain-gap 评估，只做 shadow/domain-gap
诊断，不启动 PPO update。

新增产物包括：

- `configs/quasi_real_map_domain_gap_evaluation_v1.json`
- `scripts/run_quasi_real_lola_data_prepare.py/.sh`
- `scripts/run_quasi_real_map_path_feedback_bridge.py/.sh`
- `scripts/run_quasi_real_map_domain_gap_evaluation.py/.sh`
- `scripts/run_quasi_real_map_domain_gap_closure.sh`
- `outputs/path_feedback_batch_quasi_real_map_domain_gap_v1/`

数据准备阶段读取 `model-explorer/data/manifests/lunar_south_pole_lro_lola_gdr_875s_20m.json`，把缺失的 LOLA raw product
下载到 ignored 的 `model-explorer/data/raw/...`，并校验 bytes 与 SHA-256。若文件缺失、下载失败或 hash 不匹配，summary 必须返回
`real_map_raw_data_provision_required` 或 `real_map_raw_data_hash_mismatch`，不能继续伪造准真实 evidence。

Bridge 阶段复用现有 quasi-real ROI matrix，将 LOLA ROI window 转成 `model-explorer-contract/v1` 与
`path-planner-sidecar/v1`。sidecar 的 `cost`、`passable_mask` 与 terrain layers 来自 DEM 和 observation_count 派生，不写假 route；
每个 slice 记录 `map_source.kind=lola_quasi_real_roi`、`dataset_id`、`roi_name`、`split`、`map_id`、`slice_id` 和稳定
`context_id`。

Domain-gap evaluator 对比 quasi-real ROI 与当前 generated evidence 的 obstacle/passable ratio、risk/cost distribution、reachable
candidate count、fallback/replan、path/risk delta 和 policy shadow opportunity。输出 verdict 为
`acceptable_for_next_pilot`、`scenario_expansion_required` 或 `planner_contract_gap`。

readiness review 新增 `--quasi-real-map-domain-gap-summary` 和状态 `quasi_real_map_domain_gap_evaluated`。该状态只说明准真实地图域差检查完成，
不表示正式 PPO ready，也不发布或替换策略；仍不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection
contract，不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.46 Quasi-Real Shadow Policy Behavior Audit v1

`quasi_real_map_domain_gap_evaluated` 之后，准真实 LOLA ROI 已经能进入 path-feedback，并且域差 verdict 为
`acceptable_for_next_pilot`。下一步仍不是让 policy 接管准真实路线，而是先做 shadow 行为审计：让 experimental policy 在每个
quasi-real context 上报出它“想选的 action”，再用同一套 action-mask、contract、path/risk 与 source-selection gate 判断这个意图是否可靠。

新增产物包括：

- `configs/quasi_real_shadow_policy_behavior_audit_v1.json`
- `scripts/run_quasi_real_shadow_policy_behavior_audit.py/.sh`
- `scripts/run_quasi_real_shadow_policy_behavior_closure.sh`
- `outputs/path_feedback_batch_quasi_real_shadow_policy_behavior_v1/`

审计会读取 guarded pilot 的 experimental candidate、quasi-real domain-gap root 中的
`quasi-real-map-path-feedback-summary.json`、`quasi-real-map-slices.jsonl` 与
`quasi-real-map-domain-gap-summary.json`。每个 context 输出一条
`quasi-real-shadow-policy-decision/v1`，分类为 `source_aligned`、`policy_changed_gate_passed`、
`policy_changed_gate_rejected` 或 `not_scored`，并记录 `context_id`、ROI group/name、split、map/slice id、source/raw policy
action、logit margin、action-mask validity、path/risk delta 和 gate reason codes。

readiness review 新增 `--quasi-real-shadow-policy-behavior-summary` 和状态
`quasi_real_shadow_policy_behavior_audited`。通过条件是 shadow summary passed、`shadow_context_count>=12`、每个 ROI group
至少有一个 shadow decision、`context_id_missing_count=0`、invalid action mask/fallback/open-grid/safety/contract/path/risk/source-selection
regression 全为 0，且 `behavior_verdict=acceptable_for_quasi_real_guarded_pilot`。

该阶段只审计 policy 在准真实地图上“想往哪走”。它不执行 policy 接管、不写 PPO transition、不启动 PPO optimizer update、不发布或替换
checkpoint、不修改 network/action space/default A*、不放宽 distance/path-risk/source-selection contract、不宣称
Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.47 Quasi-Real Shadow Failure Taxonomy and Anti-Overfit Alignment v1

Quasi-real shadow audit 暴露出一个有价值的失败样本：12 个 LOLA ROI context 中 11 个
`source_aligned`，但 `lola_qreal_mixed_risk_test_011` 中 raw policy 从 source action `1`
改选 action `2`，同时触发 `path_cost_regression` 与 `risk_regression`。这不是单点修补目标，
而是 mixed-risk 准真实地形下“可改选边界”迁移不稳的症状。正确处理方式是把该场景当作 failure
seed，抽象成规则型 hard-negative preference，而不是把它变成 hard positive 或放宽 gate。

新增产物包括：

- `configs/quasi_real_shadow_failure_taxonomy_v1.json`
- `configs/quasi_real_shadow_alignment_splits_v1.json`
- `configs/quasi_real_shadow_alignment_preference_v1.json`
- `configs/quasi_real_shadow_alignment_candidate_v1.json`
- `scripts/run_quasi_real_shadow_failure_taxonomy.py/.sh`
- `scripts/run_quasi_real_shadow_alignment_dataset.py/.sh`
- `scripts/run_quasi_real_shadow_alignment_preference_mining.py/.sh`
- `scripts/run_quasi_real_shadow_alignment_candidate.py/.sh`
- `scripts/run_quasi_real_shadow_alignment_closure.sh`
- `outputs/path_feedback_batch_quasi_real_shadow_failure_taxonomy_v1/`
- `outputs/path_feedback_batch_quasi_real_shadow_alignment_dataset_v1/`
- `outputs/path_feedback_batch_quasi_real_shadow_alignment_preference_v1/`
- `outputs/path_feedback_batch_quasi_real_shadow_alignment_candidate_v1/`

taxonomy 阶段把当前失败唯一归类为 `path_risk_joint_regression`，并确认
`bridge_or_feedback_gap_count=0`、`action_mask_or_contract_gap_count=0`。alignment split
围绕 failure seed 派生 train/val/holdout 准真实变体，要求 `context_id`、`scenario_id` 与
`slice_id` 不重叠。preference mining 只从 train split 写入
`raw_policy_regression_preference_pair`：当 raw policy alternative 在 path cost 和 risk 上同时更差时，
source-selected candidate 必须被排在前面。该阶段不增加 hard positive，不写 PPO transition。

alignment candidate 从 guarded experimental checkpoint 初始化，执行一个很小的 pairwise calibration，
然后重新 shadow 评估 train/val/holdout 变体和原 12 个 ROI context。当前功能性 closure 已显示：
`failure_count=1`、`quasi_real_hard_negative_preference_count=3`、split overlap 均为 0、
`hard_positive_added_count=0`、`ppo_transition_added_count=0`，校准后 holdout 与原 ROI 的 path/risk/source-selection
regression 均为 0。训练前 source-vs-alternative margin 为负，校准后转为正，说明模型确实吸收了
mixed-risk hard-negative 信号。

readiness review 新增 `--quasi-real-shadow-alignment-summary` 和状态
`quasi_real_shadow_alignment_evaluated`。该状态只有在 alignment summary passed、holdout 无回归、原 ROI 不退化、
无 train/holdout 泄漏、且所有 consumed evidence 的 current git provenance 匹配当前 HEAD 时才可进入。当前 tracked
代码和文档已变化，因此正式 readiness 仍需要提交后重刷 guarded/CUDA/domain-gap/shadow/alignment evidence，不能直接拿
dirty-tree 产物声明放行。

该阶段仍不是准真实接管或正式 PPO：不执行 policy takeover，不发布或替换 checkpoint，不修改 network/action space/default
A*，不放宽 distance/path-risk/source-selection contract，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner
诊断当训练放行，也不声明策略性能提升。

### 10.48 Current-HEAD Sequential/Guarded Evidence Re-closure v1

`Quasi-Real Shadow Alignment` 的功能闭环已经通过，但 readiness 不能跳过上游证据卫生。当前 HEAD 下重新检查 generated
sequential/guarded evidence 时，旧 root 暴露出短序列机会分布回潮：有足够多单步 safe-better choice，但不够多 episode
能连续两步安全接管。旧 evidence 不能给新代码背书，否则 readiness 会触发 `current_git_provenance_mismatch`。

本阶段修复的是 current-HEAD 上游 evidence，而不是继续扩准真实样本或扩大 PPO。具体变化是：

- `configs/policy_gated_sequential_multi_step_opportunity_rollout_v1.json` 新增两个经 focused probe 验证的起点：
  `seq-mixed_stress_detour-b=[8,9]` 与 `seq-path_complexity_benefit-b=[8,9]`。这两个 episode 在单独 3-step probe
  中均可产生 step0、step1、step2 的 accepted-better takeover。
- `scripts/run_sequential_multi_step_opportunity_diagnosis.py` 的 summary 新增
  `family_opportunity_summary`，按 family 报告 episode/step 数、safe-better step、policy-used/missed/rejected
  opportunity、missing opportunity、accepted takeover step、multi-step accepted episode 与 `gap_reason`。
- 新增测试锁定 family gap summary，避免未来再把“场景没有连续机会”和“policy 有机会但没选”混成一个 blocker。

下一轮 clean-HEAD closure 的验收顺序必须是：先跑 sequential multi-step closure，再跑 guarded PPO rollout pilot closure，
再刷新 CUDA smoke、quasi-real domain gap、quasi-real shadow audit 和 quasi-real shadow alignment。最终 readiness 只有在所有
consumed summary 的 git provenance 都匹配当前 HEAD，且 `training_readiness_status=quasi_real_shadow_alignment_evaluated`、
`training_blockers=[]` 时才成立。

该阶段仍是 current-HEAD evidence re-closure：不启动正式 PPO rollout，不执行准真实 policy 接管，不发布或替换 checkpoint，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection contract，不宣称
Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.49 Quasi-Real Guarded Policy Pilot v1

`quasi_real_shadow_alignment_evaluated` 之后，准真实 LOLA ROI 上的 mixed-risk 误判已经通过失败分类和反过拟合校准被压住。
下一步不是直接进入准真实 PPO collector，而是增加一个更小的 guarded pilot：policy 仍然只在每个 quasi-real context
上提出 action，但如果它选择了 source-selected 之外的候选，系统会用同一套 action mask、candidate present、reachable、
no fallback/open-grid、contract-safe、path/risk no-regression 与 source-selection no-regression gate 审核。只有全部通过时，
该 decision 才记录为 `controlled_choice_source=policy`；失败时必须回退 source-selected，并把 gate reason 写入 rejection
report。

新增产物包括：

- `configs/quasi_real_guarded_policy_pilot_v1.json`
- `scripts/run_quasi_real_guarded_policy_pilot.py/.sh`
- `scripts/run_quasi_real_guarded_policy_pilot_closure.sh`
- `outputs/path_feedback_batch_quasi_real_guarded_policy_pilot_v1/`

该 pilot 复用 `Quasi-Real Shadow Policy Behavior Audit` 的 policy scoring 和 gate 逻辑，不另写一套准真实判定规则。输出
`quasi-real-guarded-policy-decisions.jsonl`、`quasi-real-guarded-policy-pilot-summary.json`、
`quasi-real-guarded-policy-rejection-report.json` 和 ROI group report。summary 必须报告 `quasi_real_context_count`、
`policy_changed_gate_passed_count`、`policy_changed_gate_rejected_count`、`source_fallback_count`、ROI group coverage 以及所有
invalid-mask/fallback/safety/contract/path/risk/source-selection regression counter。

readiness review 新增 `--quasi-real-guarded-policy-pilot-summary` 和状态
`quasi_real_guarded_policy_pilot_evaluated`。该 v1 pilot 原本是 value-oriented 试驾：准真实 context 和 ROI group
覆盖达标、`context_id_missing_count=0`、存在 gate-passed changed choice、`policy_changed_gate_rejected_count=0`、
所有 gate regression 为 0，且 summary 的 git provenance 与 current HEAD 匹配时才算 value pilot 通过。若 policy
全部 source-aligned，旧口径会归因为 `quasi_real_guarded_policy_over_conservative`；但在 10.52 的新路线下，
source-aligned 不再自动代表失败，而是 teacher-equivalent 主线需要首先验收的行为。

该阶段仍不是准真实 PPO rollout：不执行 PPO optimizer update，不写 PPO transition，不发布或替换 checkpoint，不修改
network/action space/default A*，不放宽 distance/path-risk/source-selection contract，不宣称 Ackermann-feasible trajectory，
也不声明策略性能提升。

### 10.50 Quasi-Real Safe-Alternative Opportunity Diagnosis v1

`Quasi-Real Guarded Policy Pilot v1` 暴露出的最新 blocker 不是安全回归，而是 value-oriented 改选机会没有出现：
12 个 LOLA quasi-real context 全部 `source_aligned`，`policy_changed_gate_passed_count=0`，但 invalid
mask、fallback/open-grid、安全、contract、path/risk 与 source-selection regression 均为 0。形象地说，
学员在准真实路面上没有乱打方向，也没有主动安全改选。后续 10.52 将该现象重新解释为：如果 top-k 中不存在
gate-safe + better alternative，source-aligned 更接近“学会教练”，而不是“不会开车”。

本阶段的目标不是训练，也不是准真实接管，而是回答一个更基础的问题：这些 quasi-real ROI 的 top-k candidate
里，客观上有没有 `gate-safe + better` alternative。如果没有，问题在 ROI/start/target 的机会生成；如果有但 policy
全部不选，问题才是准真实 safe-choice alignment 不足。

新增产物包括：

- `configs/quasi_real_safe_alternative_opportunity_diagnosis_v1.json`
- `scripts/run_quasi_real_safe_alternative_opportunity_diagnosis.py/.sh`
- `outputs/path_feedback_batch_quasi_real_safe_alternative_opportunity_diagnosis_v1/`

诊断读取 `outputs/path_feedback_batch_quasi_real_map_domain_gap_v1/`、
`outputs/path_feedback_batch_quasi_real_guarded_policy_pilot_v1/` 和
`outputs/path_feedback_batch_quasi_real_shadow_alignment_candidate_v1/`。每个 context 以 source-selected candidate
为 baseline，对 top-k alternative 逐层统计 funnel：candidate present、action mask valid、reachable、no replan、
no fallback/open-grid、contract safe、path cost no regression、risk no regression、source-selection no regression、
safe alternative、safe-better alternative。`safe-better` 沿用 canary value/stability 口径：无 gate regression，且
`path_cost_delta <= -0.25` 或 `risk_delta <= -0.01` 或 `utility_delta >= 0.005`。

输出 `quasi-real-safe-alternative-opportunity-diagnostics.jsonl`、
`quasi-real-safe-alternative-opportunity-summary.json`、
`quasi-real-safe-alternative-opportunity-exclusion-report.json` 与 Markdown report。summary 必须按 ROI group 汇总
safe alternative、safe-better opportunity、policy missed safe-better、policy selected safe-better 与 opportunity missing。
候选被 gate 筛掉的原因写入 funnel 计数；它不等同于系统级 path/risk regression。

readiness review 新增 `--quasi-real-safe-alternative-opportunity-summary` 和状态
`quasi_real_safe_alternative_opportunity_diagnosed`。该状态只表示诊断完成：若 verdict 为
`quasi_real_safe_alternative_opportunity_gap`，下一步应扩展准真实 ROI/start/target；若 verdict 为
`acceptable_for_quasi_real_safe_choice_calibration` 且 policy miss 了 safe-better opportunity，下一步才进入
`Quasi-Real Safe-Choice Calibration v1`。

该阶段不启动 PPO update、不写 PPO transition、不执行 policy takeover、不发布或替换 checkpoint、不修改
network/action space/default A*、不放宽 distance/path-risk/source-selection gate、不宣称 Ackermann-feasible trajectory，
也不声明策略性能提升。

### 10.51 Quasi-Real Safe-Better Opportunity Expansion v1

`Quasi-Real Safe-Alternative Opportunity Diagnosis v1` 已经把准真实 guarded pilot 的 blocker 从“policy
训练问题”推进成“机会生成问题”：12 个 LOLA quasi-real context 覆盖 4 个 ROI group，`context_id_missing=0`，
exclusion=0，invalid action mask、fallback/open-grid、安全、contract、path/risk 与 source-selection regression
均为 0，但 `safe_alternative_context_count=0`、`safe_better_opportunity_context_count=0`。形象地说，
准真实道路可以检查，但当前 12 个切片里没有足够明确的安全岔路口可供 policy 改选。

本阶段新增 start-cell aware 的准真实 ROI 扩展，而不是训练 policy。`RoiSpec` 支持可选 `start_cell`，
默认仍为 `[0,0]`，旧 manifest 语义不变；`run_quasi_real_map_path_feedback_bridge.py` 会把该起点传入
`LolaSouthPoleRoiConfig.start_cell`、path-feedback `current_cell`、slice metadata 与 stable context id。
这使同一个 LOLA ROI 可以用不同起点、邻近窗口和 split 派生多个准真实 context，而不伪造地形或放宽 gate。

新增产物包括：

- `configs/quasi_real_safe_better_opportunity_expansion_v1.json`
- `scripts/run_quasi_real_safe_better_opportunity_expansion.py/.sh`
- `scripts/run_quasi_real_safe_better_opportunity_expansion_closure.sh`
- `model-explorer/data/manifests/lunar_south_pole_lro_lola_safe_better_opportunity_matrix_v1.json`
- `outputs/path_feedback_batch_quasi_real_safe_better_opportunity_expansion_v1/`

closure 流程为：生成 expansion matrix -> bridge -> path-feedback -> domain-gap check -> safe-alternative
opportunity diagnosis -> 合并 diagnosis 结果生成最终 expansion summary。最终验收必须回到严格 `top_k=3`，
不能用 sweep 阶段的更大 top-k 当成功证据。summary 需要报告 `candidate_context_count`、ROI group 覆盖、
start-cell 分布、context/scenario overlap、`safe_alternative_context_count`、
`safe_better_opportunity_context_count` 和 `roi_group_with_safe_better_opportunity_count`。只有达到 safe
alternative >= 8、safe-better opportunity >= 4、且至少 2 个 ROI group 有 safe-better opportunity 时，
readiness 才能升级到 `quasi_real_safe_better_opportunity_expanded`；否则保留
`next_required_change=quasi_real_roi_start_target_expansion_required`。

该阶段仍不是准真实 policy 接管或 PPO：不启动 PPO update，不写 PPO transition，不发布或替换 checkpoint，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection contract，不宣称
Ackermann-feasible trajectory，也不声明策略性能提升。

### 10.52 Quasi-Real Teacher-Equivalent Development Roadmap v1

`Quasi-Real Safe-Better Opportunity Expansion v1` 的 current evidence 将准真实路线重新分成主线和增益支线。扩展后的
LOLA evidence 已覆盖 108 个 quasi-real context、4 个 ROI group 和 9 个 start cell；bridge、domain-gap、
context id、action mask、fallback/open-grid、安全、contract、path/risk 与 source-selection gate 均为 0 回归。
但 strict `top_k=3` 诊断仍得到 `safe_alternative_context_count=0`、
`safe_better_opportunity_context_count=0`、`roi_group_with_safe_better_opportunity_count=0`。这说明当前 ROI/top-k
下没有客观可用的安全更优岔路口，而不是证明 policy 没有学会 teacher。

因此，后续准真实主线应改为 **teacher-equivalent 能力闭环**：如果 teacher/source-selection 已经是当前 path/risk/contract
候选集下的最优或近似最优选择，policy 复现 teacher 就是合格能力。大量 `source_aligned` 不是失败；只有 unsafe
disagreement、action-mask/contract 断裂、fallback/open-grid、安全回归、path/risk 回归或 source-selection 回归才是主线
blocker。safe-better opportunity 仍然有价值，但它属于“超越或补充 teacher”的增益目标，不应阻塞 teacher-equivalent
主线 readiness。

重新排序后的主线阶段为：

1. `Quasi-Real Teacher-Equivalent Validation`：在准真实 ROI 上验证 policy 是否稳定复现 source-selected teacher，
   允许高 source-aligned rate，要求所有 gate regression 为 0。
2. `Quasi-Real Teacher Distillation Robustness`：扩大 ROI、split、start/target 覆盖，建立 train/validation/holdout
   隔离，验证 teacher agreement 不依赖单一 ROI 或单一 seed。
3. `Quasi-Real Guarded Teacher-Following Pilot`：policy 仍先提出 action；teacher-aligned step 视为有效受控行为，
   changed choice 继续经过完整 gate，失败则回退 teacher 并记录 diagnostic。
4. `Quasi-Real PPO Collector Dry-Run`：只物化 contract-valid 的 teacher-following 或 gate-passed policy transition；
   source fallback、rejected changed choice 和不可 action-bind 样本只进入诊断。
5. `Limited Quasi-Real PPO Update Smoke`：从 experimental checkpoint 初始化，做极小 PPO update，并要求 generated
   sequential、quasi-real teacher-equivalent、quasi-real guarded 与 collector gate 全部保持无回归。
6. `Broader Real/Quasi-Real Domain Evaluation`：扩大 LOLA ROI 区域、地形类型、观测质量和 holdout 覆盖，验证 teacher
   imitation 与 guarded behavior 的域稳健性。

并行增益支线包括：

- `Safe-Better Opportunity Search`：继续寻找真正存在的 gate-safe + better candidate，但找不到不阻塞 teacher-equivalent
  主线。
- `Source-Selection Blind Spot Mining`：当 safe-better 存在而 teacher 未选时，定位 source-selection 的盲点。
- `Reward/Preference Calibration`：只在存在明确 preference 或 hard-negative 证据时校准，不把 source-aligned 误判为失败。

该路线仍不宣称正式 PPO ready，不发布或替换 checkpoint，不修改 network/action space/default A*，不放宽
distance/path-risk/source-selection contract，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner
诊断当训练放行，也不声明策略性能提升。

### 10.53 Quasi-Real Teacher-Equivalent Validation v1

`Quasi-Real Teacher-Equivalent Validation v1` 是 10.52 路线调整后的第一个主线实现阶段。它不再把
`policy_changed_gate_passed_count>0` 当作准真实主线的硬要求，而是把问题改写为：在当前 LOLA quasi-real
ROI/top-k 下，policy 能否稳定复现 source-selected teacher，并且任何与 teacher 不一致的选择都不触发安全、合同、
path/risk 或 source-selection 回归。

新增产物包括：

- `configs/quasi_real_teacher_equivalent_validation_v1.json`
- `scripts/run_quasi_real_teacher_equivalent_validation.py/.sh`
- `scripts/run_quasi_real_teacher_equivalent_validation_closure.sh`
- `outputs/path_feedback_batch_quasi_real_teacher_equivalent_validation_v1/`

实现复用现有 `Quasi-Real Shadow Policy Behavior Audit` 的 policy scoring、candidate feature 和 gate 逻辑，但外层解释口径不同：
`source_aligned` 是 teacher-equivalent 正常行为；`policy_changed_gate_passed` 是安全 disagreement，可记录但不是通过所必需；
`policy_changed_gate_rejected` 是 unsafe disagreement，必须失败并写入 disagreement report。该阶段只输出
`quasi-real-teacher-equivalent-decisions.jsonl`、`quasi-real-teacher-equivalent-summary.json`、
`quasi-real-teacher-equivalent-disagreement-report.json` 和 ROI group report，不执行 controlled takeover，也不写 PPO
transition。

summary 需要报告 `teacher_equivalent_context_count`、`policy_decision_count`、`teacher_aligned_count`、
`teacher_agreement_rate`、`safe_disagreement_count`、`unsafe_disagreement_count`、`policy_changed_gate_rejected_count`、
ROI group teacher agreement breakdown，以及 invalid action mask、fallback/open-grid、安全、contract、path/risk 与
source-selection regression counter。readiness review 新增
`--quasi-real-teacher-equivalent-validation-summary` 和状态
`quasi_real_teacher_equivalent_validated`。通过门槛是准真实 context 覆盖不少于 48、ROI group 不少于 4、
teacher agreement rate 不低于 0.90、unsafe disagreement 为 0、所有 gate regression 为 0，且所有 consumed evidence
的 git provenance 匹配 current HEAD。

该阶段保持和前序准真实阶段相同的非目标边界：不启动 PPO update，不写 PPO transition，不执行准真实 policy 接管，不发布或替换
checkpoint，不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection contract，不宣称
Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升。safe-better
opportunity 缺失仍只阻塞 value/增益支线，不阻塞 teacher-equivalent 主线。

当前实现已经能生成 108-context teacher-equivalent summary，并暴露出默认 guarded candidate 的准真实 teacher imitation
不足：`teacher_agreement_rate=0.8333`、`unsafe_disagreement_count=18`、`path_cost_regression_count=18`、
`risk_regression_count=5`。已有 quasi-real shadow-alignment candidate 的 probe 结果更好：
`teacher_agreement_rate=0.8796`、`unsafe_disagreement_count=13`，但仍低于 0.90 agreement gate。
因此该 blocker 被交给 10.54 的 teacher-distillation robustness 闭环处理，而不是放宽 gate 或把 safe-better 缺失继续当作主线
blocker。

### 10.54 Quasi-Real Teacher Distillation Robustness v1

`Quasi-Real Teacher Distillation Robustness v1` 解决的是准真实主线里的 teacher-equivalent 校准问题：policy 在 LOLA
准真实地形上不应把 path-cost 或 risk 更差的 alternative 排到 source-selected teacher 之前。该阶段仍不启动 PPO、不执行
policy 接管、不写 PPO transition、不发布 checkpoint，也不修改 network/action space/default A*。

实现新增了四段闭环：

1. `quasi_real_teacher_distillation_taxonomy`：读取 108-context teacher-equivalent validation 的 unsafe disagreement，
   输出 taxonomy、feature audit 和 report。当前 baseline 中 `unsafe_disagreement_count=18`，
   `classified_disagreement_count=18`，其中 `path_cost_only_regression_count=13`，
   `path_risk_joint_regression_count=5`，`bridge_or_feedback_gap_count=0`，
   `action_mask_or_contract_gap_count=0`。
2. `quasi_real_teacher_distillation_dataset`：物化 train/validation/holdout 隔离 slices，并记录 context/scenario/slice
   overlap。该阶段使用稳定 `taxonomy_record_id` 区分同一 scenario 下的多个 regressive alternative，避免候选覆盖。
3. `quasi_real_teacher_distillation_preference_mining`：只写 pairwise preference，不写 hard positive、不写 PPO transition。
   preferred 为 source-selected teacher candidate，alternative 为 gate-regressive candidate。当前生成
   `teacher_distillation_preference_count=648`，`hard_positive_added_count=0`，
   `ppo_transition_added_count=0`，`holdout_leakage_count=0`。
4. `quasi_real_teacher_distillation_candidate`：从 experimental checkpoint 初始化，使用 teacher-distillation preference
   做 opt-in pairwise 校准，然后重新运行 108-context teacher-equivalent validation。

当前 evidence 已通过：

- `quasi-real-teacher-distillation-summary.json`：`status=passed`、`reason_codes=[]`
- post-distillation teacher validation：`teacher_agreement_rate=1.0`
- `unsafe_disagreement_count=0`
- `policy_changed_gate_rejected_count=0`
- invalid action mask、fallback/open-grid、safety、contract、path/risk、source-selection regression 全为 0

Readiness 新增参数 `--quasi-real-teacher-distillation-summary`，通过时可升级到
`quasi_real_teacher_distillation_robustness_evaluated`。该状态只表示准真实 teacher-equivalent shadow 校准闭环完成；
它不表示正式 PPO ready、不表示准真实 policy takeover ready、不发布或替换策略，也不声明性能提升。

### 10.55 Quasi-Real Guarded Teacher-Following Pilot v1

`Quasi-Real Guarded Teacher-Following Pilot v1` 是 teacher-distillation 后的准真实主线推进阶段。它不再停留在
shadow-only 判断，而是让 policy 在 LOLA quasi-real ROI 中参与受控决策；但该阶段的能力定义仍是
teacher-following，而不是 safe-better 改选。当前 108-context LOLA evidence 中没有 safe alternative /
safe-better opportunity，因此 `source_aligned` 不是过度保守失败，而是有效的 teacher-following 行为。

该阶段新增独立入口，避免改变 10.49 中 value-oriented `Quasi-Real Guarded Policy Pilot` 的语义：

- `source_aligned` 生成 `controlled_choice_source=policy_teacher_aligned`，计入
  `teacher_following_step_count`。
- `policy_changed_gate_passed` 生成 `controlled_choice_source=policy_safe_disagreement`，计入
  `safe_disagreement_count`。
- `policy_changed_gate_rejected` 生成 `controlled_choice_source=teacher_fallback`，计入
  `unsafe_disagreement_count`，summary 必须失败并完整写入 rejection report。

新增产物包括：

- `configs/quasi_real_guarded_teacher_following_pilot_v1.json`
- `scripts/run_quasi_real_guarded_teacher_following_pilot.py/.sh`
- `scripts/run_quasi_real_guarded_teacher_following_closure.sh`
- `outputs/path_feedback_batch_quasi_real_guarded_teacher_following_pilot_v1/`

验收门禁为：`quasi_real_context_count >= 108`、`policy_decision_count == quasi_real_context_count`、
`roi_group_count >= 4`、`teacher_agreement_rate >= 0.90`、`teacher_following_step_count >= 90`、
`unsafe_disagreement_count=0`、`policy_changed_gate_rejected_count=0`，并且 invalid action mask、fallback/open-grid、
safety、contract、path/risk、source-selection regression 全部为 0。

Readiness 新增参数 `--quasi-real-guarded-teacher-following-pilot-summary`，通过时升级到
`quasi_real_guarded_teacher_following_pilot_evaluated`。该状态只表示准真实 teacher-following guarded pilot
通过；它仍不表示正式 PPO rollout、不写 PPO transition、不执行 PPO optimizer update、不发布或替换 checkpoint、
不声明性能提升、不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.56 Quasi-Real PPO Collector Dry-Run v1

`Quasi-Real PPO Collector Dry-Run v1` 是 10.55 后的准真实主线推进阶段。它仍不是 PPO update，而是把已经通过的
guarded teacher-following 行为物化成现有 `RolloutEpisode/RolloutTransition` 可消费格式，并显式保持
train/validation/test split 隔离。

新增产物包括：

- `configs/quasi_real_ppo_collector_dry_run_v1.json`
- `scripts/run_quasi_real_ppo_collector_dry_run.py/.sh`
- `scripts/run_quasi_real_ppo_collector_closure.sh`
- `outputs/path_feedback_batch_quasi_real_ppo_collector_dry_run_v1/`

collector 读取 `outputs/path_feedback_batch_quasi_real_guarded_teacher_following_pilot_v1/`，从
`quasi-real-guarded-teacher-following-pilot-summary.json` 派生 `candidate_root` 与 `quasi_real_root`，消费
`quasi-real-guarded-teacher-following-decisions.jsonl`，并从准真实 path-feedback scenario candidates 重建
`PolicyObservation`。只有 `train` split 且 `controlled_choice_source` 为 `policy_teacher_aligned` 或
`policy_safe_disagreement`、无 gate reason、observation/log-prob/value/reward 均合法的 step 才进入
PPO-trainable episodes；`validation`、`test`、`teacher_fallback`、`none`、`not_scored`、unsafe disagreement 和
任意 gate reason 非空样本均只进入 diagnostic/rejection audit。

当前 closure 已通过：

- `episode_count=108`
- `step_count=108`
- `materialized_episode_count=36`
- `ppo_trainable_transition_count=36`
- `diagnostic_transition_count=72`
- `source_fallback_trainable_count=0`
- invalid/empty action mask、missing log-prob/value、non-finite reward、fallback/open-grid、安全、contract、
  path/risk、source-selection regression 全部为 0
- `reward_all_zero=false`，`trainable_reward_all_zero=false`
- readiness validate-only 返回
  `training_readiness_status=ppo_rollout_collector_dry_run_evaluated`、`training_blockers=[]`

readiness 对该阶段的解释边界是：显式传入的 quasi-real collector summary 通过后，可用它作为当前阶段的
git provenance 与 rollout contract 证据；旧 batch-root 中较早阶段 summary 的 current-HEAD provenance mismatch 不再阻塞
该 collector dry-run 状态，但 collector summary 自身的 provenance、schema、log-prob/value、reward、mask 和
regression gate 仍必须通过。

下一阶段才是 `Limited Quasi-Real PPO Update Smoke`：从同一个 experimental checkpoint 初始化，只使用这 36 条
PPO-trainable transition 做极小 optimizer smoke，并要求 generated sequential、quasi-real teacher-following 和
collector gate 全部保持无回归。当前阶段仍不发布或替换 checkpoint，不修改 network/action space/default A*，不放宽
distance/path-risk/source-selection contract，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner
诊断当训练放行，也不声明策略性能提升或正式训练 ready。

### 10.59 Limited Quasi-Real PPO Update Smoke v1

本阶段已实现准真实 PPO update smoke 的入口、配置、closure 和 readiness 接口。current-HEAD rebaseline 说明：
optimizer 和准真实 gate 本身是干净的，但 strict generated sequential raw takeover gate 仍失败，因此 wrapper summary
仍保留 `status=failed`，由后续 compatibility、accounting 和 long-horizon contract 共同解释这个失败。

新增产物包括：

- `configs/limited_quasi_real_ppo_update_smoke_v1.json`
- `scripts/run_limited_quasi_real_ppo_update_smoke.py/.sh`
- `scripts/run_limited_quasi_real_ppo_update_smoke_closure.sh`
- `outputs/path_feedback_batch_limited_quasi_real_ppo_update_smoke_v1/`

实现上复用 `limited-ppo-update-smoke-summary/v1` 的 optimizer 和 checkpoint 输出，但新增
`limited-quasi-real-ppo-update-smoke-summary/v1` 作为准真实 wrapper summary。底层 limited PPO smoke 的 trainable
filter 已改为 config-driven：默认仍只接受 generated `controlled_choice_source=policy`，准真实配置只允许
`split=train` 且 `controlled_choice_source` 为 `policy_teacher_aligned` 或 `policy_safe_disagreement`、无 gate reason
的 transition 进入 optimizer。validation/test、teacher fallback、unsafe/gated diagnostic 样本不得进入训练。

当前 closure 的 PPO update 本身是干净的：输入和 optimizer 均为 36 条准真实 trainable transition，old
`log_prob`/`value` 最大重算误差为 0，`parameter_l2_delta≈4.37e-4`，`approx_kl≈7.7e-5`，`max_grad_norm_after_clip≈0.95`。
更新后的准真实 gate 也保持通过：

- quasi-real teacher-following：`status=passed`，`teacher_agreement_rate=1.0`
- quasi-real collector：`status=passed`，`ppo_trainable_transition_count=36`，`diagnostic_transition_count=72`

严格的 generated sequential post-update gate 仍失败，主要 reason codes 为：

- `multi_step_accepted_episode_count_below_threshold`
- `family_with_multi_step_accepted_episode_count_below_threshold`
- `canary_rejected_policy_choice_count_above_threshold`

accounting split 后，当前证据不再把 raw policy probe rejection 计入受控 rollout 累计回归：
`canary_rejected_policy_choice_count=6`、`raw_policy_path_cost_regression_count=6`、
`raw_policy_risk_regression_count=2`、`controlled_path_cost_regression_count=0`、
`controlled_risk_regression_count=0`。

因此本阶段的真实结论是：准真实 PPO update smoke 的代码路径已经打通，quasi-real post-update gates 通过；strict
generated sequential gate 失败需要由后续 contract alignment 解释，不能直接等同为受控 rollout 性能回归。拆分前不得发布
checkpoint、替换默认 policy、声明性能提升或 formal training ready。

### 10.60 Quasi-Real / Generated Sequential Contract Compatibility Diagnosis v1

本阶段不是继续 PPO，也不是放宽 generated sequential gate，而是专门回答一个归因问题：generated sequential
post-update failure 是极小 PPO update 引入的新回归，还是 quasi-real teacher-following candidate 与 generated
sequential canary contract 原本就不兼容。

新增产物包括：

- `configs/quasi_real_generated_sequential_contract_compatibility_diagnosis_v1.json`
- `scripts/run_quasi_real_generated_sequential_contract_compatibility_diagnosis.py/.sh`
- `scripts/run_quasi_real_generated_sequential_contract_compatibility_closure.sh`
- `outputs/path_feedback_batch_quasi_real_generated_sequential_contract_compatibility_diagnosis_v1/`

诊断入口读取当前 failed
`outputs/path_feedback_batch_limited_quasi_real_ppo_update_smoke_v1/`，包括 wrapper summary、post-update generated
sequential summary/steps/rejection report、post-update quasi-real teacher-following summary 和 post-update quasi-real
collector summary。随后它把原始 quasi-real base candidate checkpoint 复制到
`diagnostic-base-candidate/`：权重保持不变，只刷新 current git provenance，并写入
`diagnostic_clone=true`、`publishes_checkpoint=false`、`replaces_default_policy=false`、
`performance_claimed=false`、`formal_training_ready_claimed=false`。

诊断会用同一个 generated sequential config 跑 diagnostic base replay，并与 updated replay 对比 episode/step/family
级失败、6 个 rejected policy choices、path/risk regression、source/raw/policy action index、policy logit、selected
target cell 和 step-level delta。输出包括
`quasi-real-generated-sequential-contract-compatibility-summary.json`、`failed-step-comparison.jsonl`、
`baseline-vs-updated-sequential-summary.json` 和 `compatibility-diagnosis-report.md`。

summary 必须给出唯一 `diagnosis_verdict`：

- `ppo_update_induced_generated_regression`：base sequential 通过、updated sequential 失败；下一步修 update
  objective/reward/learning-rate guard。
- `pre_existing_generated_sequential_contract_mismatch`：base 与 updated 在同类 generated sequential step 上失败；
  下一步做 contract alignment，而不是继续 PPO。
- `gate_accounting_or_metric_mismatch`：step record 的 path/risk delta 与 rejection reason 不一致；下一步审计
  generated sequential gate metric/accounting。
- `stale_or_unreplayable_base_candidate` 或 `diagnosis_inconclusive`：证据不足，不能推进 readiness。

current-HEAD 诊断结果为 `status=passed`、`failed_step_count=6`、
`diagnosis_verdict=pre_existing_generated_sequential_contract_mismatch`。这说明 diagnostic base 和 updated candidate
在相同 generated sequential step 上同类失败，不能把失败归因于 PPO 一小步更新。本阶段本身不推进 readiness；它给 10.61
和 10.62 提供归因证据。非目标保持不变：不运行新的 PPO update，不进入 iterative PPO mini-loop，不发布 checkpoint，
不替换 default policy，不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，
不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.61 Generated Sequential Gate Metric / Accounting Audit v1

本阶段解决 10.60 暴露出的计数口径问题：generated sequential summary 里原本把 raw policy probe rejection 和
controlled rollout cumulative regression 混在一起，使报告看起来像受控 rollout 出现了 path/risk 累计回归；但实际
6 个 failed step 都已经 fallback 到 source，最终受控执行的 `policy_selected_path_cost_delta=0`、
`policy_selected_risk_delta=0`。

新增产物包括：

- `configs/generated_sequential_gate_metric_accounting_audit_v1.json`
- `scripts/run_generated_sequential_gate_metric_accounting_audit.py/.sh`
- `scripts/run_generated_sequential_gate_metric_accounting_closure.sh`
- `outputs/path_feedback_batch_generated_sequential_gate_metric_accounting_audit_v1/`

实现上，`policy_gated_sequential_canary_rollout` 的 summary 口径被拆成三套账：

- `canary_rejected_policy_choice_count`：raw policy 试图接管但被 canary gate 拒绝，仍是 acceptance blocker。
- `raw_policy_path_cost_regression_count` / `raw_policy_risk_regression_count`：只描述 raw probe 想选的 action。
- `controlled_path_cost_regression_count` / `controlled_risk_regression_count`：只描述最终受控 rollout 的实际回归。

修正后，`cumulative_path_cost_regression_count` 和 `cumulative_risk_regression_count` 只来自 controlled rollout，
不再把 raw probe rejection 混入累计执行回归。rejection report 保留 failed steps，但每个 reason 都带
`reason_origin_counts`，区分 `raw_policy_probe`、`controlled_rollout` 和 `canary_gate`。

当前真实 closure 结果为：

- `status=passed`、`reason_codes=[]`
- `legacy_mismatch_count=6`
- `raw_policy_path_cost_regression_count=6`
- `raw_policy_risk_regression_count=2`
- `controlled_path_cost_regression_count=0`
- `controlled_risk_regression_count=0`
- corrected shadow `cumulative_path_cost_regression_count=0`
- corrected shadow `cumulative_risk_regression_count=0`
- `diagnosis_verdict_after_origin_split=pre_existing_generated_sequential_contract_mismatch`
- `recommended_next_action=generated_sequential_contract_alignment_required`

这表示“裁判口径”已经拆清：PPO update 后没有新增受控 rollout path/risk 累计回归；真正剩余的问题是 base 与
updated 在 generated sequential contract 下都有同类 raw policy rejected choices，且 multi-step accepted coverage
仍不足。因此后续要做 generated sequential contract alignment，而不是继续 PPO、放宽 gate 或发布 checkpoint。

readiness 新增可选输入 `--generated-sequential-gate-metric-accounting-audit-summary`。即使 audit passed，也只会把
`recommended_next_action` 精炼为 `generated_sequential_contract_alignment_required`；readiness 是否能继续推进，必须由
10.62 的 long-horizon teacher-skill contract 单独证明。

非目标保持不变：不运行新的 PPO update，不进入 iterative PPO mini-loop，不发布或替换 checkpoint，不修改
network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible
trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.62 Generated Sequential Long-Horizon Teacher-Skill Contract Alignment v1

本阶段把 10.61 得到的分账结论继续落到 generated sequential acceptance contract 上。核心语义调整是：主线不是要求
policy 每一步都和老师不同，而是要求受控多步行为在累计收益上达到 `teacher-equivalent or better`；支线才寻找
`beyond-teacher`，且必须由多步累计收益严格优于老师证明，不能用单步更近或单步不同代替。

新增产物：

- `configs/generated_sequential_long_horizon_teacher_skill_contract_alignment_v1.json`
- `scripts/run_generated_sequential_long_horizon_teacher_skill_contract_alignment.py/.sh`
- `scripts/run_generated_sequential_long_horizon_teacher_skill_contract_alignment_closure.sh`
- `outputs/path_feedback_batch_generated_sequential_long_horizon_teacher_skill_contract_alignment_v1/`

输出包括：

- `long-horizon-teacher-skill-contract-summary.json`
- `teacher-vs-policy-return-comparison.jsonl`
- `teacher-equivalent-episode-report.md`
- `beyond-teacher-opportunity-report.md`
- `dominated-raw-choice-diagnostics.jsonl`

实现上，runner 沿用现有 generated sequential episode/step records，不新增真实 planner。它在配置的 horizon 内分别计算
teacher cumulative return、controlled policy cumulative return 和 raw policy diagnostic return。return 至少考虑 path
cost、risk、safety/contract/source-selection penalty、progress/terminal proxy。分类口径包括
`teacher_aligned_active_choice`、`teacher_equivalent_episode`、`beyond_teacher_episode`、
`dominated_raw_choice` 和 `controlled_regression_episode`。

same-as-teacher 主动选择是正面证据，不再计为 missed opportunity；raw policy rejected choice 仍可作为 diagnostic 或
blocker，但不得混入 controlled cumulative regression。只有 summary `status=passed` 且
`verdict=long_horizon_teacher_skill_contract_aligned` 时，readiness 才允许解除 generated sequential contract blocker。
若 summary failed、输入缺失、return accounting inconclusive 或出现 controlled regression，readiness 继续保持
`needs_training_contract_refinement`。

current-HEAD rebaseline 结果为：

- long-horizon summary：`status=passed`、`reason_codes=[]`
- `verdict=long_horizon_teacher_skill_contract_aligned`
- `teacher_equivalent_episode_count=36`
- `beyond_teacher_episode_count=15`
- `dominated_raw_choice_count=6`
- `controlled_regression_episode_count=0`
- readiness summary：`status=passed`、`reason_codes=[]`
- `training_readiness_status=limited_quasi_real_ppo_update_smoke_evaluated`
- `training_blockers=[]`

这表示当前 HEAD 已经从 provenance blocker 中重新闭合：strict raw generated sequential takeover gate 仍是诊断信号，
但 long-horizon contract 已证明受控多步行为达到 teacher-equivalent or better，可以把准真实 limited PPO update smoke
标记为 evaluated。下一阶段才进入 `Quasi-Real Iterative PPO Mini-Loop Stability v1`，验证多次极小更新是否仍保持
teacher-skill 对齐和 no controlled regression。

非目标不变：不运行新的 PPO update，不发布 checkpoint，不替换 default policy，不修改 network/action space/default A*，
不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner
诊断当训练放行，也不删除 generated sequential gate。

### 10.63 Quasi-Real Iterative PPO Mini-Loop Stability v1

本阶段把 10.62 的 current-HEAD rebaseline 往前推进一步：不是正式 PPO rollout，也不是发布 checkpoint，而是验证连续
3 轮极小准真实 PPO update 是否仍保持 teacher-skill 对齐。它与 10.42 的 generated iterative mini-loop 共用 readiness
状态语义，但输入和验收口径不同：本阶段从准真实 teacher-distillation experimental candidate 出发，每轮重跑 quasi-real
teacher-following、quasi-real PPO collector、limited quasi-real PPO update，再用 compatibility diagnosis、accounting audit
和 long-horizon teacher-skill contract 解释 generated sequential evidence。

新增产物包括：

- `configs/quasi_real_iterative_ppo_mini_loop_stability_v1.json`
- `scripts/run_quasi_real_iterative_ppo_mini_loop_stability.py/.sh`
- `scripts/run_quasi_real_iterative_ppo_mini_loop_stability_closure.sh`
- `outputs/path_feedback_batch_quasi_real_iterative_ppo_mini_loop_stability_v1/`

输出文件为 `quasi-real-iterative-ppo-mini-loop-stability-summary.json`、
`quasi-real-iterative-ppo-mini-loop-rounds.jsonl`、
`quasi-real-iterative-ppo-mini-loop-drift-report.json` 与
`quasi-real-iterative-ppo-mini-loop-rejection-report.json`。summary 继续使用
`iterative-ppo-mini-loop-stability-summary/v1`，因此 readiness 仍通过
`--iterative-ppo-mini-loop-stability-summary` 进入统一状态
`iterative_ppo_mini_loop_stability_evaluated`，不会新增一个与 generated iterative stage 竞争的状态。

每轮只允许 train split 中 `ppo_trainable=true` 且
`controlled_choice_source in {policy_teacher_aligned, policy_safe_disagreement}`、无 gate reason 的 transition 进入
optimizer；validation/test、teacher fallback、none/not_scored、unsafe disagreement、source fallback 与非空 gate reason
全部只能作为 diagnostic。数值门禁包括 old `log_prob/value` 重算误差不超过 `1e-4`、loss/gradient/reward/return/advantage
全 finite、`parameter_l2_delta>0`、`abs(approx_kl)<=0.25`、`max_grad_norm_after_clip<=1.0` 与
`cumulative_parameter_l2_delta<=0.05`。

strict generated raw takeover gate 的 rejected choice 可以继续作为 diagnostic 存在，但不能混入 controlled rollout
regression。只有每轮 quasi-real teacher-following passed、quasi-real collector passed、accounting controlled path/risk
regression 为 0、long-horizon `verdict=long_horizon_teacher_skill_contract_aligned` 且
`controlled_regression_episode_count=0` 时，summary 才能 `status=passed`、`round_count=3`、`failed_round_count=0`、
`stability_passed=true`。

本阶段非目标保持不变：不启动 formal PPO rollout，不扩大训练数据，不发布或替换 checkpoint，不修改 network/action
space/default A*，不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，不把
IRIS/GCS/path-planner 诊断当训练放行，也不声明 formal training ready。

### 10.64 Return-Aligned Guarded Multi-Step PPO Collector Expansion v1

`Return-Aligned Guarded Multi-Step PPO Collector Expansion v1` 把 guarded PPO 证据从“一步干净”升级为“连续几步总收益可审计”。
形象地说，前面证明学员每次打方向盘都没有违规；本阶段开始看三步连起来的行车账本：不是某一步看起来最贪心，而是整段折扣总收益、
teacher-equivalent 关系和 controlled regression 是否说得清。

新增产物包括：

- `configs/return_aligned_guarded_multi_step_ppo_collector_expansion_v1.json`
- `scripts/run_return_aligned_guarded_multi_step_ppo_collector_expansion.py/.sh`
- `scripts/run_return_aligned_guarded_multi_step_ppo_collector_closure.sh`
- `outputs/path_feedback_batch_return_aligned_guarded_multi_step_ppo_collector_expansion_v1/`

输出文件为 `return-aligned-ppo-episodes.jsonl`、`return-aligned-ppo-transitions.jsonl`、
`return-aligned-reward-audit.json`、`return-aligned-rejection-report.json` 与
`return-aligned-collector-summary.json`。runner 读取已通过的 guarded pilot collector transitions，按默认 `horizon=3`
聚合 episode，并计算 discounted episode return。step 级 PPO trainable 仍然严格：只能是 train split、
`controlled_choice_source=policy`、原 collector 已标记 `ppo_trainable=true`、gate reason 为空、reward finite 的 transition。
validation/test、source fallback、teacher fallback、none/not_scored、raw rejected probe 和非空 gate reason 都只能作为
diagnostic。

episode 级 trainable 是新的 return-audit 口径：必须 horizon 完整、全部 train split、无 source fallback、return/advantage
finite，且没有 controlled safety/contract/path-risk/source-selection regression。safe-better 不要求和老师选择不同；same-as-teacher
如果多步总收益达到 teacher-equivalent，也可以是正面证据。reward audit 显式拆出 `teacher_following_return`、
`teacher_equivalent_return`、`safe_better_return`、`controlled_regression_penalty`、`discounted_episode_return` 与
`advantage_reference_value`，并标记 `uses_multistep_discounted_return=true`、`not_single_step_best_action=true`。

当前真实 closure 结果为：

- `status=passed`、`reason_codes=[]`
- `horizon=3`
- `episode_count=36`、`step_count=108`
- `trainable_episode_count=31`
- `trainable_transition_count=30`
- `diagnostic_transition_count=78`
- `source_fallback_trainable_count=0`
- `non_finite_reward_count=0`
- `non_finite_return_count=0`
- `non_finite_advantage_count=0`
- `controlled_regression_count=0`
- readiness：`training_readiness_status=return_aligned_guarded_multistep_collector_evaluated`、`training_blockers=[]`

这表示 PPO 输入证据已经从单步 transition 扩展到多步 return 审计层：31 个 episode 可作为 full-horizon return-aligned 训练证据，
30 个 step 仍是严格 PPO trainable transition。该阶段不启动新的 PPO update，不发布或替换 checkpoint，不修改
network/action space/default A*，不放宽 gates，不宣称性能提升或 formal training ready，也不宣称 Ackermann-feasible
trajectory。

### 10.65 Return-Aligned Guarded PPO Update Smoke v1

`Return-Aligned Guarded PPO Update Smoke v1` 是 10.64 的下一步：不是继续扩大数据，也不是 formal PPO rollout，而是把已经通过的
多步回报账本真正接到一次极小 PPO optimizer smoke 上。形象地说，10.64 先把三步行车账单算清楚；10.65 则让学员按这张账单
拧一次很小的参数螺丝，然后马上检查螺丝有没有拧歪。

新增产物包括：

- `configs/return_aligned_guarded_ppo_update_smoke_v1.json`
- `scripts/run_return_aligned_guarded_ppo_update_smoke.py/.sh`
- `scripts/run_return_aligned_guarded_ppo_update_smoke_closure.sh`
- `outputs/path_feedback_batch_return_aligned_guarded_ppo_update_smoke_v1/`

实现上，runner 不直接把 `return-aligned-ppo-transitions.jsonl` 当作模型输入，因为该文件是审计账本，不携带完整
`PolicyObservation`。它先按 `(episode_id, step_index)` 把 return-aligned trainable row 与原始 guarded collector 的
`ppo-rollout-episodes.jsonl` 对齐：原始 rollout 提供 observation、action、old `log_prob/value`；return-aligned row 提供
`ppo_return`、`ppo_advantage` 与多步 reward 审计字段。通用 `run_limited_ppo_update_smoke.py` 新增兼容入口：
当 `training.return_source=transition_info` 时，PPO batch 从 `info.extra.ppo_return` /
`info.extra.ppo_advantage` 读取训练目标；未配置该字段时旧的一步 reward 折扣逻辑保持不变。

训练过滤仍然严格：只有 `split=train`、`controlled_choice_source=policy`、`ppo_trainable=true`、gate reason 为空且
reward/return/advantage 全 finite 的 row 能进入 optimizer。validation/test、source fallback、teacher fallback、none/not_scored、
raw rejected probe、非空 gate reason 与 controlled regression 全部只能作为 diagnostic 或 rejection evidence。

当前 closure 结果为：

- input return-aligned collector：`status=passed`、`reason_codes=[]`
- optimizer 实际训练 transition：`30`
- validation/test/source-fallback optimizer transition：`0`
- old `log_prob/value` 最大误差：`0.0 / 0.0`
- `parameter_l2_delta=0.00042781765692363765`
- `approx_kl=-0.0008484522695653141`
- loss/gradient/reward/return/advantage finite
- post-update gates 已实际评估：`post_update_gates_evaluated=true`
- post-update raw generalization effective status：`passed`，`post_update_raw_test_regression_count=0`
- post-update generated sequential strict status 仍为 `failed`，reason codes 为
  `multi_step_accepted_episode_count_below_threshold`、
  `family_with_multi_step_accepted_episode_count_below_threshold`、
  `canary_rejected_policy_choice_count_above_threshold`；这仍是 generated sequential
  诊断信号，但不再被混记为 controlled rollout regression
- post-update generated collector：`status=passed`、`ppo_trainable_transition_count=30`
- post-update quasi-real teacher-following：`status=passed`、`teacher_agreement_rate=1.0`
- post-update quasi-real collector：`status=passed`、`ppo_trainable_transition_count=36`
- post-update long-horizon contract：`verdict=long_horizon_teacher_skill_contract_aligned`
- post-update return-aligned replay：`status=passed`、`trainable_transition_count=30`
- post-update controlled regression count：`0`
- readiness：`training_readiness_status=return_aligned_guarded_ppo_update_smoke_evaluated`
- `training_blockers=[]`

这相当于一次“小步拧螺丝后的全车复检”：optimizer 本身只移动了很小的参数量；generated sequential 的 raw/canary
诊断仍提醒策略还没有满足更严格的多步覆盖合同，但 controlled rollout、collector、准真实 teacher-following、准真实 collector、
long-horizon teacher-skill contract 和 return-aligned replay 都没有出现受控回归。因此本阶段可以推进 readiness，但仍不能把这次
smoke 解释成正式 PPO 训练完成或策略性能提升。

本阶段的一个实际修正是 base checkpoint 必须与 guarded collector 的采样 checkpoint 一致。最初默认使用
`outputs/path_feedback_batch_policy_training_cuda_device_support_v1` 时，old `log_prob/value` 重算误差为
`0.409609317779541 / 0.09753388166427612`，触发 `ppo_update_not_on_collector_policy`。修正为 guarded pilot summary
记录的 `outputs/path_feedback_batch_quasi_real_iterative_ppo_mini_loop_stability_v1/round-02/update` 后，误差回到
`0.0 / 0.0`，说明 optimizer 确实在 collector policy 上更新。

该阶段仍是本地 experimental smoke：不发布 checkpoint，不替换 default policy，不声明性能提升，不声明 formal training ready，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，
也不把 IRIS/GCS/path-planner 诊断当训练放行。下一阶段才应考虑更长链路的 guarded rollout 或稳定性验证，而不是直接扩大为正式 PPO。

### 10.66 Quasi-Real Guarded PPO Rollout Pilot v1

`Quasi-Real Guarded PPO Rollout Pilot v1` 是 10.65 之后的准真实多步受控试跑阶段。它不是再做一次 PPO update，也不是发布
checkpoint，而是把已经通过 return-aligned update smoke 的 experimental policy 放到 quasi-real teacher-following contexts 里，
按 `horizon=3` 连续走三步。形象地说，10.65 是在维修台上很小地拧了一下参数螺丝；10.66 是把车放回准真实训练场，由安全员
全程踩着副刹，看它连续几步是否仍按老师技能行驶。

新增产物包括：

- `configs/quasi_real_guarded_ppo_rollout_pilot_v1.json`
- `scripts/run_quasi_real_guarded_ppo_rollout_pilot.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_rollout_pilot_closure.sh`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_rollout_pilot_v1/`

实现上，每一步都先读取 post-update quasi-real teacher-following decision，重建 `PolicyObservation`，必要时从同一个
experimental checkpoint 重新计算 `log_prob/value`。如果 raw policy decision 属于 `policy_teacher_aligned` 或
`policy_safe_disagreement` 且 gate-clean，则 controlled source 规范化为 `policy`；若 gate 拒绝或进入 fallback，则只保留
diagnostic。只有 `split=train`、`controlled_choice_source=policy`、gate reason 为空、observation/action/log_prob/value
完整且 reward/return/advantage finite 的 transition 才能成为 PPO trainable。validation/test、source fallback、teacher fallback、
unsafe、非空 gate reason 均不得进入训练篮子。

当前真实输出为：

- pilot summary：`status=passed`、`reason_codes=[]`
- `episode_count=36`、`step_count=108`
- `trainable_transition_count=36`、`ppo_trainable_transition_count=36`
- `diagnostic_transition_count=72`
- validation/test/source-fallback trainable：`0`
- missing observation/log_prob/value：`0`
- non-finite reward/return/advantage：`0`
- `controlled_regression_count=0`
- controlled safety/contract/path-risk/source-selection regression：全部 `0`
- `teacher_agreement_rate=1.0`
- quasi-real collector replay：`status=passed`、`ppo_trainable_transition_count=36`
- post-pilot long-horizon verdict：`long_horizon_teacher_skill_contract_aligned`
- readiness validate-only：
  `training_readiness_status=quasi_real_guarded_ppo_rollout_pilot_evaluated`、`training_blockers=[]`

这一步把“单步跟老师”和“参数小步更新”推进成“准真实多步受控执行”：策略可以连续提出动作，但每一步仍由 gate 兜底；
最终训练证据看的是几步 discounted return，而不是一步贪心最优。支线的 safe-better 仍可继续发展，但不能覆盖主线教师技能对齐；
主动做更好选择也不要求必须和老师选择不同，只要求在连续几步总收益上更合理且不破坏 safety/contract/path-risk/source-selection
约束。

本阶段仍不启动正式 PPO rollout，不执行新的 PPO 参数更新，不发布或替换 checkpoint，不声明性能提升，不声明 formal training ready，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，
也不把 IRIS/GCS/path-planner 诊断当训练放行。下一阶段才应考虑 `Quasi-Real Guarded PPO Rollout Progress/Freeze` 或更小规模的
准真实稳定性复跑，而不是直接扩大为正式训练。

### 10.67 Quasi-Real Guarded PPO Evidence Freeze v1

`Quasi-Real Guarded PPO Evidence Freeze v1` 是 10.66 之后的证据封存阶段。它不是再跑一次 PPO，也不是把 checkpoint 发布出去，
而是把已经通过的准真实 guarded rollout pilot、collector replay、long-horizon contract、return-aligned update smoke 和
readiness validate-only 结果装进同一个可复查证据盒。形象地说，10.66 完成了带安全员的准真实试跑；10.67 做的是把行车记录仪、
检验单、车架号、签字页和哈希封条放在一起，防止之后拿旧文件误判当前状态。

新增产物包括：

- `configs/quasi_real_guarded_ppo_evidence_freeze_v1.json`
- `scripts/run_quasi_real_guarded_ppo_evidence_freeze.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_evidence_freeze_closure.sh`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-evidence-freeze.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_evidence_freeze_v1/`

freeze 输出 `quasi-real-guarded-ppo-evidence-freeze-summary.json`、
`quasi-real-guarded-ppo-evidence-manifest.json`、
`quasi-real-guarded-ppo-readiness-validate-only.json` 与
`quasi-real-guarded-ppo-evidence-freeze-report.md`。closure 可以先刷新 quasi-real guarded rollout pilot 到当前 git
provenance，再执行 freeze；这个刷新只重放受控 pilot，不会运行新的 PPO update，也不会改变模型参数。

当前真实 closure 结果为：

- freeze summary：`status=passed`、`reason_codes=[]`
- pilot summary：`status=passed`
- `episode_count=36`、`step_count=108`
- `ppo_trainable_transition_count=36`
- `diagnostic_transition_count=72`
- `controlled_regression_count=0`
- `teacher_agreement_rate=1.0`
- collector replay：`status=passed`、`ppo_trainable_transition_count=36`
- long-horizon verdict：`long_horizon_teacher_skill_contract_aligned`
- readiness validate-only：
  `training_readiness_status=quasi_real_guarded_ppo_rollout_pilot_evaluated`、`training_blockers=[]`
- manifest required artifact missing count：`0`
- `stale_written_readiness_summary_detected=true`
- `runs_ppo_update=false`
- `publishes_checkpoint=false`
- `replaces_default_policy=false`
- `performance_claimed=false`
- `formal_training_ready_claimed=false`

这里最重要的工程修正是区分“显式 validate-only readiness”和“批次根目录里旧写入的 readiness summary”。后者可能因为工作区文件变化而
过期，freeze 会把它标成 stale diagnostic，但不会让旧 summary 覆盖新的显式验收结果。这样后续推进时，读者不用猜哪一个 JSON 才是
可信来源：准真实 guarded pilot 的当前验收结论以 freeze 包中的 validate-only artifact 和 manifest 为准。

该阶段仍然只是证据固化：不启动新的 PPO update，不扩大 batch，不发布 checkpoint，不替换 default policy，不声明策略性能提升，
不声明 formal training ready，不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，
不宣称 Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。下一阶段应从这个封存证据包出发，选择更窄的
准真实稳定性复跑、return-aligned guarded rollout 扩展，或正式训练前的 acceptance-contract 精炼，而不是绕过 freeze 直接扩大 PPO。

### 10.68 Quasi-Real Guarded PPO Stability Replay & Acceptance Contract Refinement v1

`Quasi-Real Guarded PPO Stability Replay & Acceptance Contract Refinement v1` 是 10.67 的直接后续。它不是继续 PPO，不是把
batch 放大，也不是宣布策略性能提升，而是拿已经封存的证据盒做三次复跑。形象地说，10.67 把一次准真实试跑的证据封箱；
10.68 则把同一辆车、同一个试车场、同一份安全规则再开三遍，看三次行车记录是否和封存样本一致。

新增产物包括：

- `configs/quasi_real_guarded_ppo_stability_replay_v1.json`
- `scripts/run_quasi_real_guarded_ppo_stability_replay.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_stability_replay_closure.sh`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-stability-replay.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_stability_replay_v1/`

runner 读取 freeze summary 与 manifest，确认输入 evidence 仍 passed 后，分别把 guarded pilot 重放到 `replay-00/`、
`replay-01/`、`replay-02/`。每次 replay 只运行 quasi-real guarded rollout pilot，不调用 PPO update closure，不改变模型参数。
`stability-replay-progress-events.jsonl` 记录每次 replay 的阶段、replay index、episode/step 计数、耗时和失败 reason；
`stability-replay-comparison.jsonl` 逐次比较 baseline 与 replay 的 summary hard gates，并在存在 steps JSONL 时比较 step
signature：episode、step、context/scenario、split、controlled source、trainable mask、action index、gate reason、controlled
regression reason、reward、discounted return 与 advantage。

当前真实 closure 结果为：

- summary：`status=passed`、`reason_codes=[]`
- `replay_count=3`、`passed_replay_count=3`
- readiness：`training_readiness_status=quasi_real_guarded_ppo_stability_replay_evaluated`、`training_blockers=[]`
- `episode_count=36`、`step_count=108`
- `ppo_trainable_transition_count=36`
- `diagnostic_transition_count=72`
- validation/test/source-fallback trainable：`0`
- missing observation/log_prob/value：`0`
- non-finite reward/return/advantage：`0`
- controlled safety/contract/path-risk/source-selection regression：全部 `0`
- `teacher_agreement_rate=1.0`
- `baseline_replay_behavior_drift_count=0`
- comparison 三行均 `status=matched`、`step_signature_mismatch_count=0`
- collector replay：`status=passed`
- long-horizon verdict：`long_horizon_teacher_skill_contract_aligned`
- `acceptance_contract_refined=true`
- `runs_ppo_update=false`
- `publishes_checkpoint=false`
- `replaces_default_policy=false`
- `performance_claimed=false`
- `formal_training_ready_claimed=false`

这一步的核心价值是把“通过一次”提升成“重复通过且账本一致”。acceptance contract 明确区分 hard gates 与 diagnostic-only：
freeze passed、三次 replay passed、36/108 覆盖、trainable >=24 且目标 36、validation/test 不泄漏、fallback 不进训练、材料化完整、
reward/return/advantage finite、controlled regression 为 0、teacher agreement >=0.9、collector replay passed、long-horizon
contract aligned、readiness validate-only passed 都是 hard gate；validation/test、source fallback、teacher fallback、raw policy
probe rejection、非空 gate reason、IRIS/GCS/path-planner 诊断仍只能是 diagnostic-only。

该阶段仍然不启动新的 PPO update，不扩大 batch，不发布或替换 checkpoint，不声明策略性能提升，不声明 formal training ready，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，
也不把 IRIS/GCS/path-planner 诊断当训练放行。下一阶段才应考虑更长 horizon 或更大 quasi-real batch 的 guarded rollout 扩展，
并继续以连续多步总收益和 teacher-skill contract 为核心，而不是回到单步贪心或直接发布策略。

### 10.69 Quasi-Real Guarded PPO Horizon-5 Batch Expansion v1

`Quasi-Real Guarded PPO Horizon-5 Batch Expansion v1` 是 10.68 后的第一阶段扩容。它不是正式 PPO，也不是新的参数更新；
它像把上一阶段已经封存并复跑稳定的短试车路线，重新铺成 5 步连续跑道，并把车队扩成 96 组。目标是验证更长 horizon、更宽
batch 下，split 隔离、trainable mask、多步回报、gate accounting 和 replay 稳定性仍能对齐。

新增产物包括：

- `configs/quasi_real_guarded_ppo_horizon5_batch_expansion_v1.json`
- `scripts/run_quasi_real_guarded_ppo_horizon5_batch_expansion.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_horizon5_batch_expansion_closure.sh`
- `tests/test_quasi_real_guarded_ppo_horizon5_batch_expansion.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-horizon5-batch-expansion.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_horizon5_batch_expansion_v1/`

runner 读取 stability replay summary、acceptance contract、freeze summary 与 freeze manifest，确认输入 evidence passed 后，
沿 freeze manifest 找回同一 quasi-real guarded pilot 的 step JSONL。随后按 `horizon=5` 生成 expanded episodes/steps，
重新计算 discounted return 与 advantage，并写出 reward audit、rejection report、comparison、progress events、readiness
validate-only 与 markdown report。它还做 3 次确定性 replay，对比 expanded step signature 与 counters，确认扩容账本没有漂移。

当前真实 closure 结果为：

- summary：`status=passed`、`reason_codes=[]`
- readiness：`training_readiness_status=quasi_real_guarded_ppo_horizon5_batch_expansion_evaluated`、`training_blockers=[]`
- `horizon=5`
- `episode_count=96`
- `step_count=480`
- `ppo_trainable_transition_count=162`
- `diagnostic_transition_count=318`
- `replay_count=3`、`passed_replay_count=3`
- validation/test/source-fallback/teacher-fallback trainable：`0`
- missing observation/log_prob/value：`0`
- non-finite reward/return/advantage：`0`
- controlled safety/contract/path-risk/source-selection regression：全部 `0`
- `teacher_agreement_rate=1.0`
- `baseline_replay_behavior_drift_count=0`
- collector replay：`status=passed`、trainable `162`
- long-horizon verdict：`long_horizon_teacher_skill_contract_aligned`
- `runs_ppo_update=false`
- `publishes_checkpoint=false`
- `replaces_default_policy=false`
- `performance_claimed=false`
- `formal_training_ready_claimed=false`

这一步的核心价值是把“36 条短路线稳定”推进为“96 组、5 步连续账本稳定”。PPO trainable 不是简单把所有 step 都放行；
只有 train split、controlled policy、gate-clean、材料化完整且 reward/return/advantage finite 的 transition 才进入训练候选池。
validation/test、source fallback、teacher fallback、raw policy probe rejection、非空 gate reason 和 path-planner/IRIS/GCS 诊断仍保持
diagnostic-only。

该阶段仍不启动正式 PPO rollout，不执行新的 optimizer update，不发布或替换 checkpoint，不修改 network/action space/default A*，
不放宽 distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行。
正式 PPO preflight 仍需后续至少扩到 512 条 trainable 以上，并补多 seed 稳定性证据。

### 10.70 Quasi-Real Guarded PPO Scale-512 Multi-Seed Preflight v1

`Quasi-Real Guarded PPO Scale-512 Multi-Seed Preflight v1` 是 10.69 之后的正式 PPO 前置体检。它仍不是正式 PPO，
也不是宣布策略已经学会；更像是在真正上高速前，先检查油量、轮胎、刹车和三次不同驾驶员试车记录是否都够格。它要求
准真实 guarded PPO 证据至少达到 `512` 条 PPO-trainable transition、`512` 个唯一 trainable context，且 horizon 不低于 5；
随后才允许做 seeds `[0,1,2]` 的极小 full-batch PPO smoke，并逐 seed 复查 teacher agreement、controlled regression、
old log_prob/value 重建误差、loss/gradient/reward/return/advantage finite、KL 与 clipped grad norm。

新增产物包括：

- `configs/quasi_real_guarded_ppo_scale512_multiseed_preflight_v1.json`
- `scripts/run_quasi_real_guarded_ppo_scale512_multiseed_preflight.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_scale512_multiseed_preflight_closure.sh`
- `tests/test_quasi_real_guarded_ppo_scale512_multiseed_preflight.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-scale512-multiseed-preflight.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_scale512_multiseed_preflight_v1/`

runner 先读取 Horizon-5 summary 与 expanded steps，确认输入已经 passed、`horizon>=5`、3 次 replay 全过、teacher agreement
不低于 0.95、controlled regression 为 0。随后它只统计 train split、controlled policy、gate-clean、材料化完整且
reward/return/advantage 有限的 transition；validation/test、source fallback、teacher fallback、raw policy probe rejection、
非空 gate reason 和 path-planner/IRIS/GCS 诊断仍保持 diagnostic-only。最关键的新门禁是
`unique_trainable_context_count>=512`：不能把同一批 context 反复复制来假装 batch 变大。

当前真实证据下，这个门禁会正确拦住下一步：Horizon-5 虽然有 `162` 条 trainable transition，但追溯到 quasi-real 输入后，
唯一 trainable context 只有 `36` 个，远低于 `512`。因此 Scale-512 preflight 会输出 failed summary，reason code 为
`insufficient_quasi_real_trainable_capacity`，并跳过 multi-seed PPO smoke。这不是 PPO 数值失败，也不是 teacher-skill 退化；
它说明训练样本池还像只有 36 张不同路况照片，不能靠复印这些照片冒充 512 个不同路况。

readiness 已扩展 `--quasi-real-guarded-ppo-scale512-multiseed-preflight-summary`。只有当 Scale-512 summary 自身
`status=passed`、`reason_codes=[]`、512 唯一 context、3 个 seed smoke 全过、post-update guarded collector 每个 seed
仍至少 512 trainable、且所有不发布/不替换/不宣称正式 ready 标志都保持 false 时，readiness 才会进入
`quasi_real_guarded_ppo_scale512_multiseed_preflight_evaluated`。当前真实 closure 的合理结论应是继续扩展准真实 trainable
context 容量，而不是启动正式 PPO。

该阶段仍不启动正式 PPO rollout，不发布或替换 checkpoint，不修改 network/action space/default A*，不放宽
distance/path-risk/source-selection gate，不宣称 Ackermann-feasible trajectory，不把 IRIS/GCS/path-planner 诊断当训练放行，
也不声明策略性能提升或 formal training ready。下一阶段目标应是 `Quasi-Real Trainable Context Expansion v1`：补足至少
512 个真实不同、train split、gate-clean 的准真实可训练 context 后，再重跑 Scale-512 preflight。

### 10.71 Quasi-Real Trainable Context Expansion v1

`Quasi-Real Trainable Context Expansion v1` 把 10.70 的容量 blocker 往上游拆开。它不是正式 PPO，也不是继续更新参数；
它像清点训练场地图册：同一张地图跑很多步仍只算一个不同路况，只有真实不同、train split、controlled policy、gate-clean、
材料完整且 reward/return/advantage finite 的 context 才能进入扩容池。

新增产物包括：

- `configs/quasi_real_trainable_context_expansion_v1.json`
- `scripts/run_quasi_real_trainable_context_expansion.py/.sh`
- `scripts/run_quasi_real_trainable_context_expansion_closure.sh`
- `tests/test_quasi_real_trainable_context_expansion.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-trainable-context-expansion.md`
- `outputs/path_feedback_batch_quasi_real_trainable_context_expansion_v1/`

runner 读取 Horizon-5 summary 与 Scale-512 summary，要求 Horizon-5 已 passed，且输入 Scale-512 只因
`insufficient_quasi_real_trainable_capacity` 阻塞。随后它扫描配置中声明的 quasi-real source ledger，并把 train split
teacher-distillation raw slice 通过同源 path-feedback candidates 真实物化为 PPO transition：重建 `PolicyObservation`，
用同一个 experimental candidate checkpoint 刷新 `log_prob/value`，再写入 source audit、capacity audit、rejection report、
去重后的 trainable context ledger，以及容量达标后供 Scale-512 重跑的 `expanded_horizon5/` 账本。validation/test、fallback、
teacher fallback、raw probe rejection、非空 gate reason、unsafe/contract/path-risk/source-selection regression 和
path-planner/IRIS/GCS 诊断仍保持 diagnostic-only。

当前真实 closure 结果为：

- summary：`status=passed`
- `reason_codes=[]`
- `source_row_count=1128`
- `materialized_source_row_count=648`
- `materialized_trainable_context_count=648`
- `unique_trainable_context_count=684`
- `ppo_trainable_transition_count=684`
- `duplicate_trainable_context_count=0`
- `duplicate_source_trainable_context_count=126`
- `scale512_status=passed`
- Scale-512：`seed_count=3`、`passed_seed_count=3`
- `seed_max_old_log_prob_abs_error=0.0`
- `seed_max_old_value_abs_error=0.0`
- `seed_max_abs_approx_kl≈1.01e-5`
- `seed_max_grad_norm_after_clip=1.0`
- `readiness_status=quasi_real_guarded_ppo_scale512_multiseed_preflight_evaluated`

这个结果说明：原先的容量 blocker 已解除，准真实训练池已经从去重后的 36 个 context 扩到 684 个真实不同、train split、
gate-clean、PPO 可消费的 context。Scale-512 三个 seed 的极小 PPO smoke 也通过，且受控 regression 仍为 0。这里的关键修正
是 seed smoke 写 collector 前会用同源 checkpoint 刷新 `log_prob/value`，避免 materialization 与 optimizer 重算之间出现
微小 on-policy 误差。

该阶段仍不启动正式 PPO，不发布或替换 checkpoint，不修改 network/action space/default A*，不放宽
distance/path-risk/source-selection gate，不复制样本凑 512，不宣称 Ackermann-feasible trajectory，不把
IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升或 formal training ready。下一步可以进入正式 PPO 前的
guarded rollout/mini-loop 设计，但仍必须保留多 seed、受控 gate、teacher skill 和回滚边界。

### 10.72 Quasi-Real Guarded PPO Iterative Mini-Loop Stability v1

`Quasi-Real Guarded PPO Iterative Mini-Loop Stability v1` 是 10.71 之后的连续小步更新稳定性检查。当前 684 条准真实
trainable context 已经足够做这个阶段；本阶段不优先下载更多原始数据，也不启动正式 PPO。它更像让同一辆车在训练场连续绕
三圈、换三位驾驶员复查：不是为了宣布车已经能上月面高速，而是确认连续小步更新后方向盘、刹车和护栏都没有漂。

新增产物包括：

- `configs/quasi_real_guarded_ppo_iterative_miniloop_stability_v1.json`
- `scripts/run_quasi_real_guarded_ppo_iterative_miniloop_stability.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_iterative_miniloop_stability_closure.sh`
- `tests/test_quasi_real_guarded_ppo_iterative_miniloop_stability.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-iterative-miniloop-stability.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_iterative_miniloop_stability_v1/`

runner 读取 10.71 的 context expansion summary 和 Scale-512 rerun summary，要求二者都 `status=passed`、`reason_codes=[]`、
provenance 可追溯。随后只允许 684 条 train split、gate-clean、controlled policy、PPO-ready transition 进入 optimizer；
validation/test、diagnostic、source fallback、teacher fallback、非空 gate reason 和受控 regression 仍不能训练。

默认执行 `seeds=[0,1,2]`、`iteration_count=3`。每个 seed 都从同一个 experimental base candidate 出发；每轮使用
full-batch PPO smoke，`epochs=1`、`learning_rate<=1e-5`、`clip_ratio=0.2`、`max_grad_norm=1.0`。每轮都会从当前 base
candidate 重算 old `log_prob/value`，误差必须 `<=1e-4`；reward/return/advantage 继续使用多步 discounted return，不回到
单步贪心最优口径。同一 seed 的下一轮从上一轮 experimental 输出继续，但仍不发布、不替换默认 policy。

本阶段新增 progress tracking：`iterative-miniloop-progress.jsonl` 逐轮记录 seed、iteration、optimizer count、KL、clipped
grad norm、teacher agreement、controlled regression 和 behavior drift。summary 只有在 3x3 全部通过、每轮 optimizer
训练条数正好为 684、所有 finite/on-policy/gate/teacher 条件都满足时才 passed。

readiness 新增 `--quasi-real-guarded-ppo-iterative-miniloop-stability-summary`。通过时状态推进到
`quasi_real_guarded_ppo_iterative_miniloop_stability_evaluated`，且 `training_blockers=[]`、`reason_codes=[]`。该状态仍不是
formal PPO ready；它只是说明当前 684 条准真实证据能承受多 seed、连续三轮极小 PPO smoke，而没有 teacher skill 退化或
controlled regression。

该阶段仍不启动正式 PPO rollout，不下载新原始数据，不发布或替换 checkpoint，不修改 network/action space/default A*，
不放宽 distance/path-risk/source-selection gate，不复制样本凑规模，不宣称 Ackermann-feasible trajectory，不把
IRIS/GCS/path-planner 诊断当训练放行，也不声明策略性能提升或 formal training ready。

### 10.73 Quasi-Real Guarded PPO Iterative Mini-Loop Evidence Freeze v1

`Quasi-Real Guarded PPO Iterative Mini-Loop Evidence Freeze v1` 是 10.72 之后的成果冻结层。它不再训练、不再扩大 batch，
而是像把刚通过验收的实验箱贴上封条：把当前 mini-loop summary、逐轮 progress JSONL、iteration summaries、readiness
validate-only 结果、阶段报告、README、系统架构报告和关键测试一起写入 SHA256 manifest，作为后续正式 PPO 前的可复查
baseline。

新增产物包括：

- `configs/quasi_real_guarded_ppo_iterative_miniloop_evidence_freeze_v1.json`
- `scripts/run_quasi_real_guarded_ppo_iterative_miniloop_evidence_freeze.py/.sh`
- `scripts/run_quasi_real_guarded_ppo_iterative_miniloop_evidence_freeze_closure.sh`
- `tests/test_quasi_real_guarded_ppo_iterative_miniloop_evidence_freeze.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-ppo-iterative-miniloop-evidence-freeze.md`
- `outputs/path_feedback_batch_quasi_real_guarded_ppo_iterative_miniloop_evidence_freeze_v1/`

freeze summary 为
`quasi-real-guarded-ppo-iterative-miniloop-evidence-freeze-summary.json`，manifest 为
`quasi-real-guarded-ppo-iterative-miniloop-evidence-manifest.json`。通过条件是：mini-loop summary 仍为
`status=passed`、`reason_codes=[]`，readiness 仍为
`quasi_real_guarded_ppo_iterative_miniloop_stability_evaluated`，`input_trainable_transition_count=684`，
`unique_trainable_context_count=684`，`seed_count=3`，`iteration_count=3`，`passed_iteration_count=9`，
progress JSONL 与 iteration summaries 都正好有 9 行通过记录，controlled regression 和 behavior drift 都为 0，并且不发布
checkpoint、不替换 default policy、不声明策略性能提升或 formal training ready。

这个阶段的意义是把“现在这套证据是什么”固定清楚。后续若进入正式 PPO preflight 或更大 rollout，就能拿这个 freeze
manifest 做对照：哪些输入、哪些测试、哪些文档和哪一个 git 提交共同构成了当前 baseline。该阶段仍不启动正式 PPO、不下载新
原始数据、不修改 network/action space/default A*、不放宽 distance/path-risk/source-selection gate、不宣称
Ackermann-feasible trajectory，也不把 IRIS/GCS/path-planner 诊断当训练放行。

### 10.74 Quasi-Real Guarded Formal PPO Preflight v1

`Quasi-Real Guarded Formal PPO Preflight v1` 是冻结 baseline 之后的正式 PPO 入场体检。它不是正式 PPO rollout，
而是像发车前的整车检查：拿 10.73 封存的 684 条 train split、gate-clean、准真实 PPO transition，分别用 seeds
`[0,1,2]` 做极小 full-batch PPO smoke，并逐 seed 检查 old `log_prob/value` 重算、loss/gradient/return/advantage
有限性、KL、梯度裁剪、teacher agreement 和 controlled regression。

新增产物包括：

- `configs/quasi_real_guarded_formal_ppo_preflight_v1.json`
- `scripts/run_quasi_real_guarded_formal_ppo_preflight.py/.sh`
- `scripts/run_quasi_real_guarded_formal_ppo_preflight_closure.sh`
- `tests/test_quasi_real_guarded_formal_ppo_preflight.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-formal-ppo-preflight.md`
- `outputs/path_feedback_batch_quasi_real_guarded_formal_ppo_preflight_v1/`

当前 closure 已通过：formal preflight summary 为 `status=passed`、`reason_codes=[]`，
`input_trainable_transition_count=684`、`optimizer_train_transition_count=684`、
`unique_trainable_context_count=684`、`seed_count=3`、`passed_seed_count=3`，
`teacher_agreement_rate=1.0`，old `log_prob/value` 最大误差均为 `0.0`，
`max_abs_approx_kl≈1.98e-5`，`max_grad_norm_after_clip<=1.0`，controlled regression 为 0。
readiness 新增 `--quasi-real-guarded-formal-ppo-preflight-summary`，通过时进入
`quasi_real_guarded_formal_ppo_preflight_evaluated`。

这一步的意义是证明“冻结证据可以被正式 PPO 前置门禁消费”，而不是宣称训练完成。它仍不启动正式 PPO rollout，不发布或替换
checkpoint，不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不下载新原始数据，
不声明策略性能提升或 formal training ready，也不把 Ackermann、IRIS/GCS 或 path-planner 诊断当作训练放行依据。

### 10.75 Quasi-Real Guarded Formal PPO Rollout Canary v1

`Quasi-Real Guarded Formal PPO Rollout Canary v1` 是 10.74 之后的第一次受控 formal PPO 小试车。形象地说，
10.74 是车辆上路前的体检；本阶段是在低速封闭道路上跑几圈，旁边仍坐着安全员，任何越界都会记录并阻断 readiness。

本阶段读取 10.74 的 formal preflight summary、freeze manifest 与 rollback manifest，只允许 684 条 train split、
gate-clean、controlled policy transition 进入 optimizer。validation/test、diagnostic、source fallback、teacher fallback、
raw probe rejection、非空 gate reason 一律不能伪装成 trainable。每个 seed 从同一 approved experimental base candidate
出发，执行保守 PPO canary：默认 seeds `[0,1,2]`，`epochs<=3`，`learning_rate<=1e-5`，`clip_ratio=0.2`，
`discount_factor=0.99`，`max_grad_norm=1.0`。

新增产物包括：

- `configs/quasi_real_guarded_formal_ppo_rollout_canary_v1.json`
- `scripts/run_quasi_real_guarded_formal_ppo_rollout_canary.py/.sh`
- `scripts/run_quasi_real_guarded_formal_ppo_rollout_canary_closure.sh`
- `tests/test_quasi_real_guarded_formal_ppo_rollout_canary.py`
- `docs/superpowers/specs/2026-06-14-quasi-real-guarded-formal-ppo-rollout-canary.md`
- `outputs/path_feedback_batch_quasi_real_guarded_formal_ppo_rollout_canary_v1/`

输出包括 canary summary、seed summaries、training curves、progress JSONL、gate audit、rollback manifest、readiness
validate-only 结果和 markdown report。readiness 新增
`--quasi-real-guarded-formal-ppo-rollout-canary-summary`；通过时进入
`quasi_real_guarded_formal_ppo_rollout_canary_evaluated`。

验收重点是：684 条输入和 optimizer transition 完全一致，3 个 seed 全部 passed，old `log_prob/value` 重算误差
`<=1e-4`，reward/return/advantage/loss/gradient 全部 finite，`abs(approx_kl)<=0.25`，
`max_grad_norm_after_clip<=1.0`，teacher agreement `>=0.95`，train/validation/test controlled safety、contract、
path-risk、source-selection regression 全为 0，rollback manifest 可追溯到 preflight baseline。

它仍不是正式训练完成，也不是发布动作。Canary candidate 只能是 experimental；不发布 checkpoint，不替换 default policy，
不修改 network/action space/default A*，不放宽 distance/path-risk/source-selection gate，不下载新原始数据，不声明策略
性能提升或 formal training ready，也不把 Ackermann、IRIS/GCS 或 path-planner 诊断当作训练放行依据。
