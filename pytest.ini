[pytest]
addopts = -p no:launch_testing -p no:launch_ros
markers =
    drake: optional pydrake integration probes; skipped from Windows/non-Drake platform profile
    external_evidence: immutable evidence replay that requires artifacts outside the repository
